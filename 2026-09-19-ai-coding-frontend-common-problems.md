# 前端开发转向 AI Coding 的常见问题全景：概率 × 影响程度全量清单

> 调研整理：2026-09-19
> 适用对象：使用 Cursor / Claude Code / Copilot / vibe coding 平台的专业前端工程师与团队

## 数据底座

业界调研与生态信号：

- **Uplevel 研究**（800+ 工程师，2025）：AI 辅助代码 bug 率 **+41%**，代码缺陷 2.74x
- **GitClear**：2024 起代码库重复块翻倍增长，AI 时代"复制粘贴"取代"抽象复用"
- **DORA 2025**：AI 采用率↑ 但交付稳定性↓，吞吐与稳定的剪刀差扩大
- **生态信号**：CC-Canary、adamsreview 等"抗回归"工具批量出现 = 回归问题已普遍到催生新品类

---

## A. 视觉与 UI 层（前端最痛区）

### A1. 像素级还原失败（概率 85% · 影响 高）

布局、间距、字号、颜色与设计稿差 10-30%。AI 对"看起来对"敏感、对"精确对"无感。根因：LLM 无视觉参照系，靠 token 猜视觉。

缓解：截图回传（视觉闭环工具）、给设计 token 表、用 Figma-to-code 管线而非纯文本 prompt。

### A2. CSS 级联灾难（概率 75% · 影响 高）

`!important` 滥用、内联样式、重复样式块、以"追加覆盖"代替"重构"，样式表几个月后无人能改。根因：AI 倾向最小改动 = 叠加而非重构。

缓解：强制 CSS Modules/Tailwind 约束、lint 禁 `!important`、定期样式审计。

### A3. 响应式/移动端适配粗糙（概率 70% · 影响 中高）

桌面很完美、手机一塌糊涂；breakpoint 只会 3 档；`vh`/`dvh`、safe-area、触控目标尺寸被忽略。

缓解：prompt 强制 "mobile-first"、真机预览 checklist、断点规范写进 AGENTS.md。

### A4. 交互细节失真（概率 65% · 影响 中）

hover/active/focus/loading/空态/错误态五态只做一态；动画缺失或突兀；时序（先 loading 后内容）错乱。

缓解：状态清单化交付要求，交互验收清单进 PR 模板。

### A5. 设计系统碎片化（概率 60%（团队场景）· 影响 高）

不用既有组件库，每次都"重新发明"一个看起来差不多的 Button；视觉多代同堂。根因：AI 上下文里组件库约束权重低。

缓解：组件库写进全局规则 + lint 强制引用 + 代码评审盯"重复造轮子"。

---

## B. 浏览器与运行时

### B1. 浏览器兼容盲区（概率 40% · 影响 高）

Chrome 中心主义——Safari 的 `backdrop-filter`、旧内核的 `:has()`、`aspect-ratio`、WebKit 前缀问题。训练数据偏现代 Chrome 导致。

缓解：BrowserStack 矩阵测试、caniuse 规则注入、目标浏览器清单进 prompt。

### B2. Hydration 不匹配（概率 55%（仅 SSR 项目）· 影响 高）

Next.js/Nuxt 项目随机 `hydration mismatch`、Waterfall 闪烁、事件丢失。根因：AI 不懂客户端/服务端执行差异，`Date.now()`/`window` 直接用。

缓解：SSR 安全规则（`useEffect` 后才碰浏览器 API）、mismatch 报错必查。

### B3. 性能回归（概率 55% · 影响 高）

巨型 bundle、未 memo 列表、`useEffect` 里 setState 死循环、图片不懒加载、动画占满主线程。根因：AI 优化的是"能跑"不是"快"。

缓解：Lighthouse CI 门禁、bundle 体积 diff 检查、性能预算写进验收。

### B4. 状态管理混乱（概率 60% · 影响 中高）

同数据三份 state、props drilling 到 6 层、竞态（请求乱序覆盖）、`useEffect` 依赖数组缺项。

缓解：状态管理选型写死（Zustand/Redux）+ 架构规则进 prompt，code review 盯状态流。

---

## C. 代码质量与正确性

### C1. 回归雪球（修 A 坏 B）（概率 65% · 影响 高）

AI 只看到局部上下文，改共享组件/工具函数时破坏 3 处调用方；修复本身又带新 bug。这就是 CC-Canary 存在的原因。

缓解：**测试先行**、`git diff` 逐行 review、改共享代码强制全局搜索调用方。

### C2. 重复代码激增（概率 75% · 影响 中）

同一逻辑三处实现，微调各不同；新需求不复用旧组件而是新写。GitClear 已量化此趋势。

缓解：duplication lint（jscpd）、review 时追问"为什么不用已有的 X"。

### C3. 框架 API 幻觉/过时 API（概率 50% · 影响 中高）

React 19 用旧生命周期写法、Next.js App Router 用 Pages 写法、记错 hook 签名——运行时才炸。根因：训练数据时间滞后 + 高频 API 记忆混淆。

缓解：锁版本、文档片段注入 prompt、`tsc --strict` + 运行时 smoke test。

### C4. 类型安全逃逸（概率 60% · 影响 中）

`any` 泛滥、`as` 断言硬掰、接口与实现漂移。

缓解：`no-explicit-any` 开成 error、类型测试（type-level tests）。

### C5. 测试缺失或"假测试"（概率 70% · 影响 高）

要么不写，要么写 tautology——断言实现细节而非行为（"函数被调用"而非"结果正确"），全绿但无保护力。

缓解：行为驱动测试模板、覆盖率 + 变异测试（stryker）双门禁。

### C6. 边界条件与错误处理缺失（概率 85% · 影响 高）

happy path 完美，空数组/超长文本/断网/后端 500/并发点击全裸奔。这是 "last mile problem" 的核心：**原型能跑 ≠ 产品能扛**。

缓解：边界条件清单（输入、网络、权限、并发）写进验收定义，异常路径要有 UI。

---

## D. 安全与供应链

### D1. XSS / 危险 HTML 注入（概率 35%（无防护习惯时）· 影响 灾难）

`dangerouslySetInnerHTML` 直接塞用户输入、`v-html` 滥用、`target="_blank"` 无 `rel`。

缓解：禁 dangerouslySetInnerHTML 规则、CSP 强制、`npm audit` + SCA 扫描。

### D2. 依赖膨胀 + 幻觉包（概率 45% / 幻觉包 15% · 影响 中高）

20 行代码引 30 个依赖；AI 幻觉出**不存在的 npm 包名**（typosquatting 攻击面，2025 年已有多起真实投毒事件）。

缓解：装包前验证（npm view）、锁文件 + 完整性校验、依赖最小化规则。

### D3. 敏感信息硬编码（概率 30% · 影响 灾难）

API key/token 写进前端代码或 .env 提交。

缓解：pre-commit 密钥扫描（gitleaks）、前端密钥零容忍规则。

### D4. Prompt injection 供应链污染（概率 20% · 影响 高）

AI 从 README/issue/网页抓上下文时被注入恶意指令，生成带后门的代码或诱导提交敏感操作。

缓解：只喂可信上下文、agent 工具权限最小化、代码评审不因"AI 生成"而降级。

---

## E. 工程流程与协作

### E1. 不可 review 的巨型 diff（概率 55% · 影响 中高）

一次改动 2000 行、重构与功能混在一起、commit message 无信息量。Review 形同虚设 → 质量闸门失效。

缓解：PR 大小硬限制、agent 指令要求小步提交、review 工具强制。

### E2. 大库全局一致性漂移（概率 65% · 影响 中）

上下文窗口装不下整个仓库，AI 只见局部——命名风格、错误处理模式、目录结构逐渐分裂成"缝合怪"。

缓解：架构规则文档（AGENTS.md）+ 规则做 lint 化，定期全库一致性检查。

### E3. 技术债加速累积（概率 60% · 影响 高）

AI 让"快速交付"变得太容易，todo/半成品/hack 代码堆积速度超过清理速度；重构被无限推迟直到炸。

缓解：债务预算（每 PR 附带清理项）、定期重构 sprint。

### E4. Agent 空转烧钱（概率 35% · 影响 中）

同一个 bug 修 8 轮不收敛、无限循环探索、token 账单爆炸但产出为零。

缓解：任务超时与步数上限、明确"5 轮不收敛就停下来问人"。

---

## F. 个人与团队长期

### F1. 技能退化（skill rot）（概率 长期 80%+ · 影响 高）

三个月全 AI 写码后，手写 CSS/调试能力明显生疏；离开工具寸步难行。生态佐证：Fata（防技能腐化复习工具）出现。

缓解：刻意练习（每周手写）、理解而非盲签每一段 AI 代码。

### F2. 理解债——修不动 AI 写的代码（概率 70% · 影响 高）

代码能跑但没人真正理解；出问题时无法 debug，只能"让 AI 再改改"，陷入放大器循环（不懂→改错→更不懂）。

缓解：**AI 产出必须过自己的脑子**——review 时能讲清每段逻辑才合入；关键模块禁止纯 AI 生成。

### F3. 高估产出、低估维护（概率 75% · 影响 高）

demo 惊艳、上线即地狱。报告佐证：有人花 **$43k 重构仅 $11k MRR 的 AI MVP**；"前 90% 免费，后 90% 付费"。

缓解：成本归因透明——把"AI 节省的开发时间"和"后续重构/维护成本"放进同一张表。

### F4. 团队"过墙投掷"摩擦（概率 50%（混合团队）· 影响 中高）

非技术成员用 Lovable/Bolt 造出原型扔给工程团队，30K 行无人 review 的代码成为负担。

缓解：建立"原型→生产"转换流程（重写阈值、技术评审门）。

---

## 风险热力总表（按危害排序）

| 等级 | 问题 |
|------|------|
| 🔴 灾难级 | D1 XSS、D3 密钥泄漏、C6 边界缺失（生产事故）、F2 理解债（项目失控） |
| 🟠 高危害 | A1 视觉还原、A2 CSS 灾难、A5 设计系统碎片、B1 兼容、B3 性能、C1 回归、C5 假测试、E3 债累积、F1 技能退化 |
| 🟡 中危害 | A3 响应式、A4 交互细节、B2 hydration、B4 状态、C2 重复、C3 API 幻觉、C4 类型、E1 巨型 diff、E2 一致性、F4 团队摩擦 |
| ⚪ 低危害 | D2 依赖膨胀、D4 注入污染、E4 agent 空转 |

---

## 十条防御清单（按性价比排序）

1. **测试先行是唯一硬防线**——回归问题（C1）和假测试（C5）都靠它
2. **PR 逐行 review 不降级**——"AI 写的"不是免检理由
3. **边界条件清单进验收定义**（C6 是最大单点漏洞）
4. **架构规则写进 AGENTS.md 并 lint 化**（A2/A5/B4/E2 一网打尽）
5. **视觉闭环**：截图回传 + 设计 token 表（A1）
6. **安全扫描自动化**：gitleaks + npm audit + CSP（D 组）
7. **小步提交**：PR 上限 400 行（E1）
8. **性能预算进 CI**：Lighthouse + bundle diff（B3）
9. **关键模块禁止纯 AI 生成**，必须人能讲清（F2）
10. **每月一次"手写日"**对抗技能退化（F1）

---

## 结论

AI coding 对前端的真实风险不在"写不出来"，而在**视觉细节、边界条件、回归控制和长期理解债**——四个都是"看起来能跑、上线才疼"的问题。工具越强，人工把关的杠杆越大。