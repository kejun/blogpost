# 当 2.8T 参数的模型从 SSD 里"流"出来：Deltafin 把 Kimi K3 塞进 MacBook 的工程启示

> 2026 年 9 月 8 日，开源项目 Deltafin 的一个 fork（argonautlabsai/deltafin）在 Hacker News 上晒出了一组数据：**Kimi K3——2.78T 参数的 MoE 模型、1.45 TB 专家权重——在 128GB 内存的 M5 Max MacBook Pro 上，用四块外置 SSD 流式读取专家权重，跑出了 1.0 token/s 的稳定解码速度**。193 分、90+ 条评论，评论区挤满了工程师：有人算 40 块 MacBook 能不能替代 16 节点 GPU 集群，有人在问 Optane 是不是更快，还有人不屑地丢下一句"这是 quadratic slop"。但真正值得关注的不是 1 token/s 这个数字本身，而是这个项目揭示的：**当模型大到内存装不下时，存储系统如何成为推理引擎的一部分**。

---

## 一、背景：K3 官方的"最低配置"是 16 台节点

先复习一下目标模型。Kimi K3 是 Moonshot AI 在 2026 年 7 月发布的开源模型：总参数量 2.78T，MoE 架构，每层有 896 个专家的共享专家库（expert bank），每个 token 每层只路由激活 16 个专家；1M token 上下文窗口。官方推荐的推理配置是 **16 节点、约 4.8 TB 聚合显存、200 万美元级的基础设施**。

Deltafin（gavamedia/deltafin，MIT）存在的意义就是挑战这个前提：能不能在一台消费级设备上跑**完整**的 K3——不是量化到 3-bit 的"差不多版"，而是 Moonshot 原样发布的权重。

这里的数字天然是反直觉的：K3 有 92 层 × 896 个专家 = 82,432 个专家文件，每个文件 17.5 MB（MXFP4 精度），合计约 1.45 TB。加上注意力主干（attention trunk），完整模型约 1.7 TB。128GB 内存的 MacBook Pro 连零头都装不下。

## 二、设计哲学：全量、零妥协、"K3 决定每一个 token"

Deltafin 的第一性原则在 README 里写得非常直白：

> **All 16 experts, every single token. No shortcuts, no "close enough." It's exactly what Moonshot shipped.**

项目刻意与另一派路线划清界限：社区里有项目宣称"能跑全量 K3 且更快"，但 Deltafin 的作者指出，那些项目把专家权重重新编码到了 ~3 bit——"那些权重已经不是 Moonshot 发布的权重了，而且没有人测量过这种妥协的代价"。

Deltafin 的架构选择：

- **单一原生二进制**（Rust 核心 + C-ABI provider），无 Python 运行时，附带 OpenAI 兼容的本地 server（/v1/chat/completions、/v1/completions、/v1/models，SSE 流式，强制 greedy + 可复现）。
- **驻留主干（resident spine）**：注意力层等共享参数约 50.7 GiB 常驻内存，采用 row-int8 编码（作者明确声明这不是 bit-exact，因为主干不是 BF16 原值）。
- **专家权重流式读取**：每个 (层, 专家) 一个 17.5 MB 文件，用 `pread` + `F_NOCACHE` 直读（绕过 page cache），每层 16 个并发整文件读取。
- **投机解码**：内置 Inferact 的 Kimi-K3-DSpark 草稿模型（6.6 GiB 磁盘 / 4.49 GiB 内存），K3 校验每一个草稿 token；可选 Qwen 0.6B/1.7B 加速纯续写场景（实测 17-token 任务快 2.7×）。
- **两种安装模式**：`--full`（1.7 TB 全量落地，最快）或 `--stream`（215 GB 起步，按需抓取专家，缓存随使用增长——本质上是一个"用空间换时间"的自建专家缓存层）。

这套设计里最漂亮的隐喻是：**Deltafin 把 SSD 当成了一级"虚拟显存"，而 MoE 的稀疏性恰好让这个方案成立**——每个 token 只需要 16/896 的专家，稀疏路由天然把"必须在线"的数据量缩小了近两个数量级。

## 三、存储物理课：MoE 层的延迟是 max，不是 sum

这是整个项目最反直觉、也最具普适价值的技术发现。

一个 MoE 层需要读取 16 个专家文件才能开始计算，**而这一层的完成时间取决于 16 次读取中最慢的那一次，而不是它们的总和**。这意味着：

1. **总带宽不是瓶颈，尾延迟才是**。单文件 17.5 MB，在 7 GB/s 的 Thunderbolt 5 硬盘盒上耗时约 2.5 ms（队列深度 1）。作者实测：四块盘各自的读取速率维持在自身天花板的 90–100%，解码速度却仍受调度约束。
2. **RAID-0 是错误答案**。条带化让每一次读取都触及每一块盘，慢的那块盘成为每次读取的公共瓶颈。作者原话："RAID-0 是最早尝试的方案之一，它输了。"评论区有人问 Optane 是否有优势，作者的回答同样干脆：LLM 在乎的是顺序带宽，17.5 MB 整文件读取的耗时由吞吐决定而非延迟，Optane 的低延迟优势在这里毫无用武之地。

drive ladder（同一天、同一引擎、同一提示词的冷启动实测）：

| 配置 | 读取能力 | 128 token 解码（draft on） | 相对 4 盘 |
|------|----------|---------------------------|-----------|
| 1 盘（内置 SSD 全镜像） | 13.6 GB/s | 0.5350 tok/s | ~52% |
| 2 盘（内置 + 外置全镜像） | 20.7 GB/s | 0.7596 tok/s | ~73% |
| 3 盘（用量加权布局） | ~27 GB/s | 0.9375 tok/s | ~90% |
| 4 盘（基准布局） | ~33 GB/s | 1.0377 tok/s | 100% |

结论清晰得近乎残酷：**解码速度与"合并读取带宽"近似线性相关——直到尾延迟取代带宽成为瓶颈**。1 盘到 2 盘 +21 个百分点，2 盘到 3 盘 +17 个百分点，3 盘到 4 盘只剩 +10 个百分点。这是典型的边际递减曲线，而曲线的拐点位置由"最慢读"决定。

## 四、ARGODRIVE：仪器化带来的四个"反直觉"发现

这个 fork 最值钱的产出可能不是 1 token/s，而是配套的测量基建 **ARGODRIVE**：一个 10ms 粒度的每设备读监控器、一个记录"哪个盘服务哪个专家、每轮谁最后到达"的逐轮 barrier 追踪器、以及一个配置断言框架（被测配置没真正生效就拒绝记录基准）。

作者在 HN 上分享了四个由仪器化发现的、靠直觉永远猜不到的优化：

1. **读线程池拆分（+14%）**：一个常量把 reader 线程上限同时钉死在 16，导致 demand 池和 prefetch 池共享同一个容量。拆开后，demand 排队占阻塞时间的比例从 70% 降到 7.5%。
2. **热专家副本分裂（+10%）**：把热门专家的读取拆分到两块盘上的两个副本。这个 knob 之前在"每个专家只有单一归属"的布局上测了六次，每次都是 -6%——直到有了副本可拆，才从负转正。
3. **预取均衡器（+11%）**：prefetch 路径原本按固定目录顺序取第一个命中，导致所有复制流量倾泻到同一个硬盘盒。改成"最小期望完成时间"调度、并与 demand 路径共享 in-flight 计数器后，此前所有被判定为失败的复制布局全部翻盘。
4. **一条过期的"定律"（+8%）**：某条"草稿深度 X 更慢"的经验法则，其实是在一个已经不存在的草稿模型上测出来的。重新测试后直接 +8%。

真正震撼的是这个项目的**失败目录**——约 1000 次计时运行的"什么不work"清单：RAM 专家缓存 8–40 GB（-4% 到 -48%）、单副本条带化（-7% 到 -25%）、两块盘共享一条 Thunderbolt 链路（-11%）、注意力主干从 SSD 流式读取（-60%）、Metal 的文件加载 API（-19% 到 -22%）。

**这个目录的存在，比任何单个优化都更有价值。** 它把"本地跑前沿模型"从玄学变成了工程：每一个决策都有测量背书，每一个改进都有回归验证。在 AI 基础设施领域，"我们试过的所有方法"通常只在 PR 里存活 24 小时，而这里是持续维护的一等公民。

## 五、现实的另一面：prefill 的 6.2× 读放大与 4.4k token 的上下文天花板

1 token/s 的解码数字很性感，但作者把不光彩的数字放在了同一段话里：**512-token 的 prompt，首 token 延迟约 6.3 分钟（373–376 秒）**。

PREFILL.md 给出了精确的归因：

- prefill 窗口内四块盘总计读取了 **8,977 GB**（聚合 24.1 GB/s），而整个专家集只读一遍是 1,443 GB——**读放大 6.2×**。
- 时间分解：等待专家读取落地 282.8s（76%）、专家 kernel 约 60s（16%）、attention 20.2s（5%）、其他 2%。
- 根因：`layer_passes=744` = 8 × 93。512 行 prompt 被切成 8 个 64 行的 row-pass，**每一个 pass 都会把该层的专家并集重新读一遍**。被路由到 k 个 pass 的专家就被读 k 次，实测平均 6.2 次。

修复方案已经明确（expert-major 调度：按层聚合所有行的路由、按专家分组、每个专家只读一次）：读取量从 9.0 TB 降到 ~1.44 TB，首 token 有望从 6.3 分钟压到 2–2.5 分钟。作者还给出了一个严谨的验收门禁：`requested_bytes` 必须降、`layer_passes` 必须从 744 掉到 93，并且输出必须保持 token 级一致。

另一个被诚实披露的硬约束：**KV cache 约 2.8 MiB/token**，在 128 GB 内存（减去 50.7 GiB 主干和投机校验保留区）下，引擎当前只肯受理约 4.4k token 的上下文。K3 号称 1M 上下文，但在这种硬件上，长上下文首先死于 KV cache，其次死于 prefill——"200k token 的 prompt 要按天计算"。

## 六、1 token/s 到底有没有用？社区吵翻了

HN 评论区的争论本身就是一篇社会学论文。正方："1 token/s 对人类来说仍然很快——一夜能干完人半周的工作"、"相当于免费雇了个员工"。反方："你平时 10 分钟的 prompt 会变成 7 小时"、"40–50 tok/s 的模型做 X 要多久，这个就要 40–50 倍"。还有人拿打孔卡做类比："我父母学编程时，写一天程序、跑一小时，只为得到一个语法错误。"

我的判断偏向"有用，但不是你现在想的那种用法"。1 token/s 的价值不在交互式对话，而在：

- **离线批处理**：夜间跑完 10 万 token 的分析任务，第二天早上看结果；
- **隐私与主权**：权重、上下文、输出全部留在本地，不经过任何 API；
- **正确性叙事**：作者在每次版本晋升前都验证"与自家参考实现 token 级一致"，甚至如实披露了 512-token 生成中观测到的一次 near-tie token 翻转（两块盘上同一个 token 位点的浮点累加顺序不同导致），并验证了 40/40 抽查专家文件字节一致——这种"我测过，这是我测到的"的诚实，恰恰是当前 AI 生态最稀缺的东西。

另外注意一个细节：这个 fork 在 M5 Max 上把 upstream 在 M1 Max 上的 0.29 tok/s 推到了 1.0 tok/s——**约 3.4× 的提升里，硬件换代只是一部分，剩下的是读路径重构和调度修复**。而 upstream 自己的进步轨迹同样惊人：7 月 27 日 0.0141 tok/s → 7 月 28 日 0.1311（+829%）→ 7 月 30 日 0.2660（+102%）→ 8 月 2 日 0.2847 → 最新 0.2901。一个月内两个数量级，纯粹靠工程。

## 七、启示：内存层次结构正在成为推理 API 的一部分

把 Deltafin 放进更大的坐标系里看，它和几条线交汇：

- **先例**：antirez 的 ds4/DwarfStar 项目早就演示了"精确专家流式 + 缓存所有权 + 正确性优先测量"；JustVugg 的 colibri 做了激进的 MoE 流式与 router-lookahead。Deltafin 是这条"存储即显存"路线的集大成者。
- **对照**：vLLM 团队为 K3 做了数据中心级的集成与 recurrent/attention cache 研究（vllm.ai/blog/2026-07-27-k3）——同一模型，一头是 16 节点 GPU 集群，另一头是 4 块 SSD 的笔记本电脑。**两者之间的缝隙，就是未来两年推理基础设施的战场**：CXL 内存扩展、存储级内存、异构内存调度，本质都是在重画"哪一层内存放什么权重"这张图。
- **范式**：Deltafin 证明了一件事——当模型超过内存容量时，**调度器先于计算器决定性能**。16 个并发读取的 max 延迟、prefetch 与 demand 的池隔离、专家副本的放置策略，这些是存储系统的经典问题，如今以"推理引擎内部组件"的身份重新登场。数据库工程师看到 KV cache 2.8 MiB/token 会会心一笑：这就是一个 buffer pool 命中率问题。

对开发者而言，最值得带走的三句话：

1. **测量先于优化**。四个优化里至少三个是"测出来的"，而 ~1000 次失败实验目录说明"不 work 的知识"同样昂贵且值得记录。
2. **质量妥协的隐性成本无法预知**。3-bit 重编码的 K3"看起来更快"，但没人量化过代价；Deltafin 选择保留原权重，把性能问题全部转化为调度问题——这个 trade-off 立场本身值得尊敬。
3. **慢推理会催生新的交互范式**。当首 token 以分钟计，prompt 工程从"写清楚"变成"一次写对"，而批处理、异步、checkpoint 恢复这些老计算机系统的词汇，会重新流行起来。

## 八、接下来看什么

Deltafin 的路线图上有三件事值得跟踪：prefill 的 expert-major 重构（预计首 token 减半以上）、更大内存设备的适配（192GB 的 M 系列、DGX Spark 的 273GB 统一内存已被 PR #2 验证）、以及 ARGODRIVE 类仪器化工具向通用推理引擎的扩散。而下一次"不可能"的演示，很可能不是跑得更快，而是**跑得更完整**——直到 1M 上下文在消费级硬件上成为默认值。

到那一天，回头看 2026 年 9 月这个"1 token/s"的时刻，我们大概会想起 Kennedy 那句被 README 引用的台词："我们选择在本地跑全量 2.8 万亿参数模型，不是因为它容易，而是因为它难。"

---

### 参考来源

- HN 讨论：https://news.ycombinator.com/item?id=49616257
- Deltafin（上游引擎，MIT）：https://github.com/gavamedia/deltafin
- ARGODRIVE fork + 基准包：https://github.com/argonautlabsai/deltafin（k3-public-bench/，含 PREFILL.md、SCALING.md、原始日志）
- ARGODRIVE 仪器工具：https://github.com/argonautlabsai/argodrive
- Moonshot AI Kimi K3：https://huggingface.co/moonshotai/Kimi-K3
- vLLM 的 K3 集成研究：https://vllm.ai/blog/2026-07-27-k3
- 先例：antirez/ds4（DwarfStar）、JustVugg/colibri
- 作者在 HN 上的补充说明（硬件、测量口径、失败目录细节）均来自 item 49616257 评论区