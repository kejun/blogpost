# 当 29MB 的模型开始"操控"你的家：Needle 3 与自动化基础模型的微型化突围

> 2026-09-20 · AI技术 · 本文约 4800 字

## 一次"反规模"的发布

9 月 20 日，GitHub Trending 上出现了一个异类：`cactus-compute/needle`，单日 +234 星、累计 1.16 万星。它发布的不是又一个千亿参数模型，而是一个**整个模型只有 8-29MB** 的"自动化基础模型"——目标设备是手机、手表、扫地机器人、智能家居、汽车座舱，甚至单片机（MCU）。

名字叫 Needle（针），暗示得很直白：在海量参数的大模型时代，它要做那根"针尖"——小到能扎进任何设备，又足够锋利到完成真正的任务。

为什么值得认真看？因为它押注了一个与主流叙事相反的判断：**端侧 Agent 的第一性需求不是"会聊天"，而是"会干活"**——精确调用工具、可靠抽取结构化数据、本地完成语义检索。这三件事不需要 100B 参数，需要的是为"自动化"而不是"对话"重新设计整个模型栈。Needle 3 正是这套思路的完整工程化呈现。

先看它的核心数字：

| 维度 | Needle 3 |
|------|---------|
| 模型规模 | 29-121M 参数（Laddered Simple Attention Network） |
| 二进制体积 | 8-29 MB（CQ2 量化，2.125 bits/weight） |
| 训练数据 | 360B tokens 专有结构化数据 |
| 推理速度 | 树莓派 5 上 400-4k tok/s decode、1-10k tok/s prefill |
| 运行时体积 | 每平台预编译 engine < 1 MB |
| 能力范围 | 工具调用 + 结构化抽取 + 文本嵌入（无通用对话） |
| 许可 | Apache-2.0（权重托管于 Hugging Face） |

## "阶梯式"架构：一个模型，19 种尺寸

Needle 3 最有原创性的设计叫 **Intelligence Laddering（智能阶梯）**：训练出的网络从第 2 层到第 20 层，**每一层深度都是一个独立可部署的子模型**，能力随层数单调递增。开发者按设备算力选 2L、8L 还是 20L，权重文件是同一个。

这解决了端侧模型长期以来的"部署悖论"：传统上你训练一个大模型，然后靠剪枝/蒸馏得到小版本，两者行为还不一致；而 Needle 的每一级子网络在训练时就共享同一套表示，剪出来的不是"残次品"，而是出厂设计的一部分。

架构上的几个关键选择值得拆开看：

**1. Monarch Hadamard MLP 取代 FFN。** 前馈层被替换为基于 Hadamard 变换的 Monarch 分解矩阵，把大矩阵乘拆成结构化的小矩阵运算——在保持容量的同时大幅降低算力需求。这是它能在树莓派上跑出 4k tok/s 的重要原因。

**2. Engram n-gram 记忆。** 这是最反直觉的设计：模型把大量参数花在一个可被 gather（索引读取）的 n-gram 记忆存储上。官方说法是"121M 模型做了 50M 模型的算术"——即大部分参数不参与逐 token 的矩阵乘，而是作为可寻址记忆被按需读取。这本质上是用**检索式记忆替代参数式记忆**，把知识存储的成本从"每次前向都要算"转移到"只在需要时读取"。

**3. GQA 注意力 + 因果卷积。** 分组查询注意力降低了 KV 缓存开销，因果卷积 tap 给注意力叠加局部时序建模，相当于给长程注意力配了一个廉价的短程通道。

**4. Multi-lane hyper-connections。** 多层间的高维捷径连接，让梯度在 20 层深的阶梯上依然能有效回传——这是"每一层都可独立部署"能成立的前提。

这套组合的总体哲学是：**把稀缺的算力预算花在决策上，而不是花在知识复述上**。知识交给 engram 记忆和外部 schema，模型本身只负责"理解当前输入 + 做出正确动作"。

## 把"自动化"当第一公民

Needle 3 砍掉了通用对话能力，换来三项为自动化定制的核心能力，每一项都对应一个真实的端侧场景：

**工具调用（Tool Calls）。** 给定应用暴露的函数集合，模型负责选对函数、从用户话语中填充每个参数。关键行为契约是"不猜"：用户一次要两件事，就顺序返回两个调用；请求没有对应工具覆盖，就**返回空列表而不是编造一个调用**。这在工程上极其重要——幻觉式的工具调用在云端也许只是浪费一次 API 调用，在智能家居里可能就是误开煤气灶。

**结构化抽取（Structured Extraction）。** 声明一个 schema（甚至直接传 Pydantic 模型），把杂乱文本变成类型化字段：发票的商户/金额/日期、通知的意图分类、消息的回复草稿。关键机制是 **byte-level grammar-constrained decoding**：解码器从你的 schema 编译出文法，每一步 token 生成都被文法约束，**输出在数学上保证可解析**——结构错误从"概率问题"变成"不可能事件"。

**文本嵌入（Embedding）。** 同一个模型输出句向量，支持完全离线的语义搜索、工具路由、重复告警合并。对一个手表应用来说，"数据不出设备"就是安全合规本身。

真正体现工程成熟度的是**置信度系统**：每个响应由可学习的校准头输出一个已校准的置信度分数，引擎内置 0.1 的拒绝阈值——低于阈值的调用被扣留在 `suppressed_calls` 字段，`function_calls` 保持为空。上层应用可以按分数做三档路由：**高分直接执行、中分先展示给用户确认、低分视为拒绝**。这相当于把"Agent 该不该动手"这个通常靠 prompt 模糊暗示的问题，变成了一个明确的、可编程的数值接口。

配套的还有 **triggers（触发器）**机制：工具描述无法穷举用户措辞时，可以给工具挂正则表达式。命中即把解码空间限制到该工具并强制产生调用——"把风扇打开并调暗厨房灯"这种多意图长句，可以靠精心设计的 catch-all 正则同时命中两个工具。这是典型的确定性工程与概率模型的杂交，和之前写过的"混合确定性 Agent 架构"（2026-07-24）一脉相承。

## 微调的杠杆：29M 参数逼近云端模型？

Needle 在 GitHub 和发布页上最出圈的论点是：**在 DroidCall 工具调用基准上，用 LoRA 针对单一产品的工具集微调一个 epoch，每个阶梯子网络提升 18-36 个百分点；从 4 层（约 29M 参数）起，微调后的子网络超过 DeepSeek V4 Flash。**

这个对比需要冷静拆解：

- **合理之处**：约束容量到"单一产品工具集"这一窄任务，是 Agent 落地时的真实形态——你的应用只有 20 个函数，不需要 100B 模型知道全世界的知识。领域约束 + 小模型 + 定向微调，本就是小模型逼近大模型的最可靠路径，之前 GLM-5.3"仅后训练"、3B 模型超越前沿（2026-05-29）的案例都在验证同一规律。
- **保留之处**：这是 Cactus Compute 自家报告的基准，对比对象是通用的云端模型而非同样针对 DroidCall 微调过的对手；"超过"的幅度也依赖具体子任务。把它理解为"端侧专用模型在窄任务上可达云端通用模型水平"的**方向性证据**更准确，而非普遍结论。

值得注意的还有微调路径的设计：LoRA 冻结基座、在完整 20 层上训练，然后导出**任意子网络**的 4-bit `.cact` 权重。也就是说"调大模型、部署小模型"——微调的计算发生在开发机上，最终落地的还是那个 8MB 的二进制。这让"2 层子网络用在更小的设备上"成为可能：阶梯 + LoRA，等于把模型选择从"买哪个"变成了"编译时参数"。

## 部署矩阵与 .cact 格式

量化层面，Needle 用自研的 **Cactus Quants**，2.125 bits/weight，权重以 `.cact` 格式**内存映射（mmap）原地读取**，不需要反序列化加载——冷启动几乎为零。每个部署目标（macOS、Linux x86/arm/riscv64/mipsel、Windows、Android、iOS/watchOS/tvOS、浏览器 WASM、WASI 组件）都附带一个 <1MB 的预编译 engine，`needle build --platform xxx` 一条命令把权重放到 engine 旁边即可运行。

这套部署矩阵的现实意义：**Agent 的下一个战场不是数据中心，而是你口袋里、车里、客厅里没有 GPU 的设备。** 8 月 Anthropic 发布 Model Hardware Standard（MHS）时我写过，物理世界 Agent 需要硬件层面的标准化；Needle 则从另一个方向切入——不要求硬件变强，而是让模型小到现有硬件随便装。两种哲学，同一个终点：Agent 进入物理世界。

## 生态坐标：三条"小模型自动化"路线

把 Needle 放在 9 月的时间切片里，能看到端侧自动化模型正在分化出三条清晰路线：

1. **通用端侧模型**：LFM2.5-2.6B（2026-08-05）代表的"本地 Agent 无处不在"——保留对话能力，靠 2.6B 参数在边缘设备上做通用 agentic 推理。
2. **超窄决策专家**：CUA-S1（706K 参数、2.8MB）代表的 System One 路线——不做生成，只对给定选项打分，专攻表单填写等单一决策。
3. **自动化专用基础模型**：Needle 的路线——几十 MB 到 100MB 级别，砍掉对话，为工具调用/抽取/嵌入三项能力做全栈优化，并靠"阶梯 + 微调"覆盖从单片机到手机的全部设备谱系。

三条路线并不互斥，反而可能组合：通用模型负责规划，Needle 负责执行层的工具调用，CUA-S1 负责更细粒度的决策打分。**"大模型想，小模型做"正在从论文概念变成真实的分层架构。**

## 隐忧与批判

作为工程产物，Needle 有几个点值得警惕：

**一是遥测默认开启。** 二进制默认上报遥测数据，需要显式设置 `NEEDLE_TELEMETRY=0` 和 `DO_NOT_TRACK=1` 关闭。对于一个主打"数据不出设备"、"空气间隙部署（air-gapped）"的端侧模型，默认开遥测在隐私叙事上自相矛盾——团队应该默认关闭、让用户显式开启。这几乎是端侧 AI 产品的第一课。

**二是基准自证问题。** "超过 10 倍大模型""匹配 2-3 倍大模型"均来自自家评测与自家数据集（360B tokens 专有数据也训练于 Cactus 平台），外部复现门槛高。社区需要像看待所有厂商自报基准一样看待它。

**三是复杂度上限。** 工具调用精确匹配依赖"工具集足够小"这一前提（官方自己也建议每轮工具集尽量小），当工具数量到几百个时，embedding 路由 + 文法约束的组合能否保持精确度，仍是未知数。Needle 是"单一产品工具集"的最佳解，不一定是"通用计算机使用 Agent"的解。

**四是最务实的：混合架构。** 现实部署大概率不是"本地 or 云端"的二选一，而是：本地 Needle 处理低延迟、隐私敏感、离线必须的调用；遇到超出能力范围的需求，把请求升级到云端大模型。Needle 的置信度分数恰好为这种升级路由提供了现成的信号——这可能才是它作为基础设施最值钱的部分。

## 结论

Needle 3 的意义不在于"又一个小模型"，而在于它第一次把"为自动化而生的微型基础模型"做成了完整产品：阶梯式架构解决了尺寸选择、文法约束解决了输出可靠性、校准置信度解决了执行决策、2.125-bit 量化 + mmap 解决了部署体积。它和 CUA-S1、LFM2.5 一起，勾勒出 2026 年端侧 Agent 的轮廓——**未来大多数 Agent 调用，可能根本不经过云端**。

对开发者而言，值得立刻动手验证：`pip install cactus-needle`，用 `@needle.tool` 装饰三个函数，跑一遍 `agent.run()`。当那个 29MB 的模型在树莓派上离线、精确地完成一次多工具调用时，你会直观感受到：规模竞赛的尽头，是效率的回归。

---

**参考链接：**
- GitHub: [cactus-compute/needle](https://github.com/cactus-compute/needle)（Apache-2.0，11.6k stars）
- 发布页: [cactuscompute.com/needle](https://cactuscompute.com/needle)（架构图、交互式基准）
- 权重: [Hugging Face: Cactus-Compute/needle3](https://huggingface.co/Cactus-Compute/needle3)
- 文档: [Designing tools for Needle](https://cactuscompute.com/blog/designing-tools-for-needle) / [Leveraging Needle's confidence](https://cactuscompute.com/blog/needle-confidence) / [The .cact format](https://cactuscompute.com/blog/cact-format)