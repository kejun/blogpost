# LLM 驱动的形式化验证：当证明自动化跨越实用化门槛

> 2026 年 7 月 26 日，Adam Langley 发表了一篇标题极其直白的博文："We have proof automation now"。这位 TLS 1.3 的联合设计者、前 Google 密码学工程师，用 Lean 4 实现了一个经过完整形式化验证的 Zstandard 解压缩器。他的核心论断是：LLM 与证明无关性（proof irrelevance）的结合，正在让依赖类型编程从学术象牙塔走向工程实践。这不是一篇"AI 好厉害"的布道文——它是一个严肃的工程实验报告，背后是形式化验证领域三十年未解的成本困境。

## 一、三十年未解的证明开销问题

形式化验证的愿景从未改变：用数学证明软件的正确性，让机器而非人类审查者来保证不变量（invariant）不被违反。依赖类型语言（Coq/Rocq、Lean、Agda）将这个愿景推向极致——类型系统强大到可以编码任意精细的程序性质。

但代价是什么？

seL4 项目给出了最权威的数据。这个由澳大利亚 Data61 团队完成的微内核验证项目，是人类历史上最大规模的形式化验证工程之一。其回顾论文（Klein et al., 2009）记录了一组令人窒息的数字：

- **证明时间 ≈ 10 × 设计与实现时间**
- **证明代码行数 > 20 × C 源代码行数**

这意味着什么？一个 10,000 行的 C 内核，需要超过 200,000 行的 Isabelle/HOL 证明代码。即使团队在项目中积累了相当的经验，证明开销仍然维持在 10 倍量级。这不是新手的问题——这是结构性的。

Langley 在博文中坦承："我可以证明，花一整天去证明一个相当简单的命题，是完全可能的。"更令人沮丧的是那种"galling experience"——花了数小时之后，发现你要证明的命题根本就是错的。

这就是为什么依赖类型编程至今仍然极其小众。不是因为它不好，而是因为太贵了。

## 二、SMT 求解器：一个"复杂而善变的神"

证明开销问题并非无人尝试解决。F\*（F-star）项目是最知名的尝试之一。F\* 的策略是让 SMT（Satisfiability Modulo Theories）求解器自动 discharged 证明义务（proof obligations）。

对简单情况，这确实有效。但 Langley 的描述非常精准：

> "很容易构造出某种东西，让 SMT 求解器跑进外太空，运行数小时，让你怀疑它是否永远不会结束。我见过那些大量使用这些语言的人，他们必须发展出一种第六感，知道什么会让求解器高兴，然后围绕它来构造一切。这可以有所帮助，但在某种程度上，它把问题变成了神秘主义：你最终在侍奉一个复杂而善变的神。"

这段话值得反复品味。SMT 求解器的问题不是能力不足，而是**不可预测性**。你无法建立可靠的工程直觉——同样的代码结构，换一个引理的表述方式，求解器可能从 2 秒变成 2 小时。这不是工程，是占卜。

## 三、关键洞察：证明无关性 × LLM

Langley 文章中最核心的技术洞察，是关于**证明无关性**（proof irrelevance）的：

> "一个关键事实是，至少在理论上，一旦命题是正确的，证明的内容就是无关紧要的：只有它的存在才重要。"

这是类型论中的一个经典性质。对于命题 `P`，如果你有两个证明 `p1 : P` 和 `p2 : P`，那么在逻辑上 `p1 = p2`。证明的"内容"不影响程序的行为——你只需要知道证明存在。

这个性质对自动化意味着什么？

**它意味着证明不需要"优雅"，不需要"可维护"，甚至不需要"人类可读"。** 你只需要一个能通过类型检查器的证明项（proof term）。这彻底改变了自动化的约束条件：

- SMT 求解器需要找到"正确路径"——一条从公理到目标的推理链
- LLM 只需要生成一个"能通过检查"的证明项——暴力搜索、模式匹配、甚至"猜测+验证"都可以

Langley 指出了两个实际限制：

1. **证明工程**（proof engineering）：seL4 团队发现，证明需要被结构化，以便代码变更后重新对齐的成本可控。但如果 LLM 可以廉价地重新生成证明，这个问题的重要性大幅降低。
2. **类型检查器爆炸**：过于复杂的证明项可能让类型检查器消耗大量内存。但 Langley 的有限测试表明，LLM 可以避免这种情况。

换句话说：LLM 不是更好的 SMT 求解器。它是一种完全不同的证明自动化范式——从"搜索推理路径"变成"生成可验证的证书"。

## 四、案例：用 Lean 4 验证 Zstandard 解压缩器

Langley 选择 Zstandard 作为实验对象并非偶然。Zstd 正在赢得取代 gzip 成为标准压缩工具的竞赛——它提供更好的熵编码和极快的解压速度。更重要的是，它有一个 RFC（RFC 8878），这意味着存在一个精确的规范可以作为验证目标。

### FSE：一个精妙的熵编码器

Zstd 的核心创新之一是 FSE（Finite State Entropy）编码器。Langley 在文中给出了一个清晰的解释：

FSE 是一个状态机。状态数多于符号数，每个符号获得与其出现概率成比例的状态份额。每个状态有三个值：该状态对应的符号、从比特流中读取的比特数、以及一个基线状态号。

关键技巧在于：Huffman 编码只能使用整数比特，但 FSE 通过让同一符号的不同状态读取不同数量的比特，在**统计平均**上实现分数比特编码。如果一个符号的理想编码长度是 1.68 比特，那么它的部分状态读 1 比特，部分读 2 比特，加权平均恰好逼近目标值。

更反直觉的是：FSE 编码必须**从后向前**工作。要编码序列 C, D，你需要先知道 D 之后是什么，才能确定从哪个 C 状态开始。这意味着 Zstd 压缩器反向编码符号，但增量写出输出，解压缩器必须跳到块的末尾，反向读取比特流。

### 为什么这适合形式化验证？

这类代码有几个特征，使其成为形式化验证的理想目标：

1. **规范精确**：RFC 8878 给出了完整的算法描述，不存在歧义
2. **不变量丰富**：状态表的构造必须满足"每个符号都能到达每个状态"的可达性约束
3. **边界条件密集**：比特读取、状态转换、概率量化——每一步都有溢出和越界风险
4. **正确性关键**：解压缩器的 bug 可能导致数据损坏或安全漏洞

在传统语言中，这些不变量只能以注释形式存在。Langley 指出："在常规语言中，这些东西最终（最好情况下）变成注释，随着团队规模的增长很快丢失。然后你得到微妙的误解和不完全匹配的组件。"

在 Lean 4 中，这些不变量被编码为类型。状态转换的合法性、比特读取的范围约束、概率量化的精度保证——全部由类型检查器强制执行。而 LLM 负责生成那些繁琐的证明项。

## 五、从 seL4 到 Zstd：成本曲线的坍塌

让我们做一个粗略的对比：

| 维度 | seL4 (2009) | Langley's Zstd (2026) |
|------|-------------|----------------------|
| 证明语言 | Isabelle/HOL | Lean 4 |
| 证明生成 | 手工 + 少量自动化 | LLM 辅助生成 |
| 证明/代码比 | > 20:1 | 显著降低（LLM 承担大部分） |
| 时间开销 | 10× 实现时间 | 大幅压缩（未给出精确数据） |
| 证明工程需求 | 高（需结构化以应对变更） | 低（LLM 可廉价重新生成） |
| 目标规模 | 微内核（~10K LOC） | 解压缩器（~数千 LOC） |

注意：这不是一个完全公平的对比。seL4 验证的是一个完整的微内核，包括所有系统调用、内存管理、中断处理。Zstd 解压缩器是一个相对独立的组件。但关键不在于规模对比，而在于**成本结构的根本变化**。

当证明生成的边际成本趋近于零（LLM API 调用的成本），证明工程的经济逻辑完全改变：

- 过去：证明是昂贵的资产，需要精心维护
- 现在：证明是一次性证书，可以按需重新生成

这类似于从手工编写汇编到编译器优化的转变。你不会去"维护"编译器生成的汇编代码——你修改源代码，重新编译。LLM 辅助证明正在让形式化验证获得同样的"可重新生成性"。

## 六、Agent 时代的验证需求：一个被低估的交汇

这篇文章的发表时机值得注意。2026 年 7 月，AI 编码 Agent 已经不再是新鲜事。Cursor、Claude Code、Codex 每天生成数百万行代码。但一个根本性问题正在浮出水面：

**当 AI 生成代码的速度远超人类审查的速度时，信任锚点在哪里？**

当前的答案是：测试。但测试只能证明 bug 的存在，不能证明 bug 的缺席。Dijkstra 在 1969 年说的这句话，在 Agent 时代获得了新的紧迫性。

形式化验证提供了另一种可能：**不是验证 AI 生成的代码"看起来对"，而是证明它"数学上对"。**

这创造了一个有趣的闭环：

```
AI Agent 生成代码 → LLM 生成形式化证明 → 类型检查器验证证明 → 数学确定性
```

在这个链条中，LLM 出现了两次：一次作为代码生成器（可能出错），一次作为证明生成器（也可能出错）。但关键区别在于：证明生成器的输出可以被**确定性验证**。类型检查器是一个纯粹的、无歧义的判定程序。证明要么通过，要么不通过。没有"差不多对"。

这意味着：即使 LLM 生成的证明有 90% 是错的，只要类型检查器拒绝了错误的证明，系统的可靠性就不受 LLM 幻觉的影响。这是一个**错误可以被完美过滤**的系统。

对比之下，LLM 生成的代码没有这样的过滤器。代码可以"看起来对"但包含微妙的 bug。测试可以捕获一部分，但永远不是全部。

## 七、实际影响与局限

### 哪些领域会首先受益？

1. **密码学与协议实现**：Langley 的老本行。TLS、Signal Protocol、加密库——这些代码的正确性直接关系到数十亿人的安全。形式化验证在这里的 ROI 最高。

2. **压缩与编解码器**：Zstd、AV1、protobuf——规范精确、实现复杂、bug 后果严重（数据损坏）。

3. **编译器与运行时**：LLVM、V8、JVM 的核心转换——一个优化 bug 可能影响所有下游软件。

4. **智能合约**：代码即法律，bug 即资金损失。形式化验证在这里已经是刚需（Certora、K Framework），LLM 辅助可以大幅降低成本。

5. **AI Agent 的工具层**：当 Agent 调用外部工具时，工具的正确性是 Agent 可靠性的基础。经过验证的工具层可以成为 Agent 信任链的锚点。

### 当前的局限

诚实地说，这个方向仍然面临重大挑战：

- **规模问题**：Zstd 解压缩器是数千行代码。Linux 内核是 3000 万行。从前者到后者的路径并不清晰。
- **规范获取**：形式化验证需要一个精确的规范。很多现实软件没有 RFC，只有"代码即规范"。
- **性能开销**：经过验证的实现通常比手工优化的 C 代码慢。Langley 的 Lean 解压缩器不太可能达到 Yann Collet 的 C 实现的速度。
- **LLM 证明能力的上限**：对于需要深度数学洞察的证明（如四色定理级别的推理），当前的 LLM 仍然力不从心。

但这些是工程问题，不是原理性问题。原理已经成立：LLM + 证明无关性 + 确定性类型检查 = 一个错误可以被完美过滤的验证系统。

## 八、一个更大的图景

回到 Langley 文章的标题："We have proof automation now"。这个"now"不是夸张。它标记的是一个相变（phase transition）：

- **之前**：形式化验证是"正确但昂贵"的选择，只在最高安全级别的系统中经济可行（航空航天、军事、芯片设计）
- **之后**：形式化验证可能成为"默认正确"的工程实践，成本降低到普通项目可以承受

这个相变的驱动力不是 Lean 4 本身（它已经存在多年），不是 LLM 本身（它也不是为证明而生），而是两者的组合产生了一个新的经济等式：

**证明的生成成本 → 0，而证明的验证成本保持为 0（类型检查器）。**

当生成成本为零而验证成本为零时，正确性不再是奢侈品。

这对 AI Agent 时代有一个深刻的启示：我们可能不需要"更可靠的 AI"来构建可靠的软件系统。我们需要的是**可验证的架构**——让 AI 的不确定性被确定性的验证层所包围。LLM 可以犯错，只要错误会被类型检查器拒绝。Agent 可以生成错误的代码，只要错误的代码无法通过形式化证明。

这不是对 AI 的不信任。这是对数学的信任。

---

*参考来源：*
- *Adam Langley, "We have proof automation now", imperialviolet.org, 2026-07-26*
- *Klein et al., "seL4: Formal Verification of an OS Kernel", SOSP 2009*
- *RFC 8878: Zstandard Compression and the 'application/zstd' Media Type*
- *Nigel Tao, "Zstandard Decompression Concepts", nigeltao.github.io, 2022*
- *Jarek Duda, "Asymmetric Numeral Systems: Entropy Coding Combining Speed of Huffman Coding with Compression Rate of Arithmetic Coding", arXiv:1311.2540*
