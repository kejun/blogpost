# 当 Agent 开始复现整个学术会议：ICML 2026 Open Reproductions 深度拆解——2200 篇论文、23% 证伪率与科学出版的信任基础设施

> **摘要：** 2026 年 7 月 15 日至 8 月 2 日，Hugging Face 与 alphaXiv 联合发起了一场史无前例的实验：让 1,221 名社区成员带着各自的 Coding Agent，对 ICML 2026 全部 6,352 篇录用论文逐条复现。19 天内，参与者发布了 6,816 份可审计的 Trackio logbook，覆盖 2,226 篇论文（占全会 34%），35,908 条科学主张被逐一判定。结果触目惊心：51% 的论文至少一条主张被独立验证，23% 至少一条被证伪，49 篇论文全军覆没，242 篇被独立复现团队给出相反结论——"可复现性不是二元的，它是对抗性的"。本文拆解这场"论文复现工业化"实验的方法论、精彩证伪案例、Agent 的局限与人类的不可替代性，以及它对 AI 时代科学出版信任基础设施的深远启示。

---

## 引子：当审稿人亲口承认"我没检查证明"

ICML 2026 收到了 **23,918 篇投稿，录用了 6,352 篇**——几乎比上一年翻了一倍。这个指数级增长背后，有一个略带讽刺的推手：正是 AI Agent 让"跑实验、写论文"变得更快，才撑起了这轮论文洪流。

但审稿能力没有同步翻倍。大会上大多数审稿人是义务劳动，既没时间也没能力逐条验证一篇论文的每个主张。一位审稿人对某篇 Spotlight 论文的评审意见是：

> "My low confidence score is because I did not check all the proofs carefully."（我给低置信度分，是因为我没有仔细检查所有证明。）

这篇论文拿到了高分和 Spotlight 席位。请注意这个细节——我们后面会回来找它算账。

这里存在一个极其尖锐的结构性矛盾：**供给端（论文生产）已经被 AI 指数级加速，而需求端（论文审查）还停留在人力时代**。审稿人周末才能仔细核查一篇论文，而论文在以每天上百篇的速度涌入。传统同行评审这座"信任基础设施"正在被论文洪流冲垮。

Hugging Face 的回应很直接：既然 Agent 加速了论文生产，那就让 Agent 来加速论文审查。于是有了这场 ICML 2026 Open Reproductions 挑战。

---

## 一、实验设计：把"复现"变成一场可审计的工业化流水线

这场挑战的核心设计理念是：**复现不是终点，可审计性才是**。整个流程围绕"每个结论都要能追责"来构建。

### 1.1 从论文到可检查的主张（Claims）

直接让 Agent 去读 40 页 PDF 效率太低且不可控。主办方先将全部 6,341 篇录用论文的摘要和核心科学主张（claims）结构化提取出来，让 Agent 从"一个具体、可验证的目标"出发，而不是面对一整篇论文。同一篇论文允许多人同时复现——**多团队独立复现同一主张，本身就是对抗性验证的雏形**。

### 1.2 自带 Agent（BYO Agent）

参与者使用 Claude Code、Codex、Cursor、OpenResearch 的 orx 等各类 Coding Agent，主办方提供统一接口：一条命令拉取论文、主张和挑战说明。HF 给每位参与者发放 20 美元算力额度，全程共启动了 **2,962 个 HF Jobs** 云端任务。

### 1.3 Trackio Logbook：审计过程本身可审计

每次复现都会产出一份 Trackio logbook——一个静态 Hugging Face Space，包含：复现过程叙述、运行的代码、产出的 artifact，以及（可选的）完整 Agent 执行轨迹（以 Hugging Face Dataset 形式公开）。最终 **274 份完整 Agent trace 被公开**。

这里有个关键的设计哲学：**信任不能建立在"Agent 说自己做了"上，而要建立在"任何人可以回放 Agent 做了什么"上**。执行轨迹即证据——这是把 Agent 引入科学验证时最容易被忽略、却最重要的一点。

### 1.4 自动裁判 + 对抗性重验

每份 logbook 由自动化 Logbook Judge（运行开源模型 GLM-5.2）重读，对每条主张给出四类判定：**verified（验证）/ falsified（证伪）/ toy（降规模证据）/ inconclusive（无结论）**。裁判被明确指示：**将 logbook 的自我评估视为不可信**。

而最精彩的一环在最后：**35 名参与者正式声称自己证伪了某些论文**，主办方对每一项证伪声明进行了对抗性重验——重读论文、重读 logbook、从论文原文重新推导数学或重新实现实验。**"证伪"本身也要被证伪。**这一层设计让整个实验从"众包实验"升级为"元科学实验"。

---

## 二、结果：一张让人坐不住的成绩单

挑战最终交出的一组数字，很可能是迄今对 ML 顶会最大规模的一次"体检"：

| 指标 | 数值 |
|------|------|
| 参与成员 | 1,221 人 |
| 发布 logbook | 6,816 份 |
| 尝试复现论文 | 2,226 篇（全会 34%） |
| 被判定主张（claims） | 35,908 条 |
| 云端实验任务（HF Jobs） | 2,962 个 |
| 公开完整 Agent 轨迹 | 274 份 |

按论文维度聚合主张判定结果：

- **51%（1,103 篇）**至少有一条主张被独立验证；其中 **266 篇被完全复现**（所有主张全部验证），另有 632 篇部分复现且零证伪。共有 **3,978 条主张通过真实实验得到确认**。
- **23%（496 篇）**至少有一条主张被证伪或质疑；其中 **49 篇所有主张全部证伪、无一可验证**。
- 最耐人寻味的一组：**242 篇论文被独立复现团队对同一主张给出相反结论**。
- 其余 502 篇仅有降规模（toy）证据，280 篇什么结论都无法得出（最常见原因是缺失 artifact——代码、数据、checkpoint 没公开）。

三个值得展开的观察：

**第一，验证与证伪并存于同一篇论文。**"可复现"与"不可复现"不是论文的标签，而是主张（claim）的属性。266 篇完全复现与 49 篇完全证伪之间，是 632 + 496 篇"部分正确、部分错误"的论文。**科学论文的正确性是分布式的，不是二元的**——这句话对科研评价体系的影响是深远的。

**第二，对立结论不是噪声，是信号。**242 篇论文出现"独立复现团队结论相反"，说明在大量情况下，复现结果取决于实验细节的选择（数据切分、超参、检查的时序长度……）。这直接指向了"复现悖论"：**你复现出的结果，可能只是你实验设置的函数**。

**第三，缺失 artifact 是最大的复现杀手。**280 篇论文因"什么东西都建立不起来"而无法判定，主因是代码/checkpoint 未公开。这比"结果有误"更值得警惕——**不可验证与可证伪同样致命**。一个不公开 artifact 的论文，等于把验证成本全部转嫁给读者，而读者（人力）已经无力承担。

---

## 三、精彩案例：五个"抓现行"现场

### 3.1 回到开头那篇 Spotlight 论文：Hk+O(1) 的伪装

还记得那位"没检查证明"的审稿人吗？他审的正是《Towards Optimal Robustness in Learning-Augmented Paging》。论文声称其算法实现了 **Hk + O(1)** 的鲁棒性——这是该领域的最优值附近。

一位参与者的 logbook 测量到加法项实际按 **0.38·ln k** 增长，并精确定位了证明中出错的步骤。主办方随后自己把实验扩展到 k = 1,024，确认增长显著（约 **9 sigma**）。真实鲁棒性是 **Hk + Θ(log k)**——而不是论文声称的 Hk + O(1)。**"审稿人没检查的证明"，被 Agent 在几个小时内抓住了。**

这个案例有个特别讽刺的细节：logbook 显示，不少独立复现团队给出了 "verified" 判定——因为他们**检查的 k 范围太小，log k 的增长还没来得及显现**。这正是元科学层面的重要教训（我们稍后展开）。

### 3.2 在第 224 步崩塌的定理：有限时域检查的陷阱

《Attention's forward pass and Frank-Wolfe》证明了一个定理：只要原点初始位于 token 粒子凸包内部，粒子就会坍缩到原点。**三个独立团队都找到了反例**，且违反最早出现在 **t = 224、约 3,800、6,416 步**——这三个步数完美解释了为什么其他团队"验证"了它：**有限时域检查全都停得太早**。最干净的反例用精确有理数算术表述，没有任何浮点误差可以推诿。论文作者当天确认了问题，正在修复。

这个案例是本场挑战最有教学意义的发现之一：**"在测试范围内正确"与"正确"是两回事**。它和 7 月的 SWE-Bench Pro 审计（34% 任务存在缺陷）、以及 8 月 15 日那篇 LLM 生成 GPU Kernel 通过测试却仍然错误的故事，指向同一个根源——**基于有限样本的验证系统，天然会被"测试盲区"欺骗**。区别在于：这次是数学模型，反例可以被精确定位到具体步数。

### 3.3 理论写 Reverse KL，代码算 Forward KL

《Self-Distillation Enables Continual Learning》论文的中心方程和整个理论章节分析的是 **reverse KL 散度**，但发布代码的默认配置——按作者说法，论文所有结果都由它产生——计算的是 **forward KL**。抓到这个错位的 logbook 同时发现：在作者自己的代码和数据上，论文头条的 **+4pp 提升无法复现**。作者已经在 arXiv 上传了澄清版本。

这是工业界最熟悉的病：**文档（论文）与实现（代码）漂移**。过去它藏在软件仓库里靠 code review 抓，现在 Agent 把它从论文里挖了出来。它揭示了一个尴尬的现实：**论文的"理论"部分和"实验"部分可能根本不是同一个东西在支撑**。

### 3.4 被 EOS Padding 稀释三倍的困惑度

《Do Transformers Need Three Projections?》的评估中发现：**约 66% 的被评估标签位置是 EOS padding token**，而这些 token 被训练到接近零损失，把困惑度人为压低了约三倍。摘要中"以 50% 缓存缩减换取 3.1% 质量代价"的真实数字，修正后大约是 **9.4%**。

这个案例的杀伤力在于：**不是造假，而是评估基建的隐性缺陷**。没有复现实验，根本不会有人注意到 66% 的评估位置是填充符。它说明了一个残酷的事实：**论文声明的数字，是论文的"测量系统"产出的；当测量系统有偏差，所有下游引用都在传播这个偏差。**

### 3.5 反例：一次被推翻的"证伪"

挑战并非一边倒地抓论文错。一个 logbook 曾高调声称"论文方法比基线慢 2 倍"，结果被主办方重验为**复现者的算术错误**：拿单条轨迹的时间去对比 50 条一批的时间。正确归一化后，参与者自己的数据反而确认了论文声称的 **8 倍加速**。

这个案例与前面的证伪一样重要：**Agent 时代的错误是双向的——模型会幻觉结论，复现 Agent 也会**。它证明了"对抗性重验证伪声明"这一设计不可或缺：没有这一层，一场失败的复现就可能被当作论文的错误传播出去，造成对作者的诽谤性误伤。

---

## 四、核心洞见："可复现性不是二元的，它是对抗性的"

把 242 篇"结论相反"与 49 篇"全证伪"放在一起，本场挑战最深刻的元结论浮出水面：

**复现不是执行一个协议，而是一场博弈。**论文作者与复现者之间存在信息不对称——作者知道实验的所有隐藏细节（数据清洗、随机种子、未公开的超参、评估基建的坑），复现者只能从论文文本中重建这些细节。每一步重建都是一次猜测，而每一次猜测都可能改变结论。这就是为什么复现是"对抗性"的：**复现者的任务是找出能让结果翻盘的那个细节**。

对于读者（包括审稿人），这意味着一个残酷但必须接受的事实：**一篇论文的"可复现性"不是一个静态属性，而是一个置信度区间**——取决于有多少独立团队、用多少种不同的实现路径、检查了多长的时域/多大的规模。单一复现成功（或失败）的证据价值，远低于多团队交叉验证。

这也解释了为什么本场挑战中最受信任的论文（Flat Minima 论文）有 **20 个独立团队复现、12 个验证全部主张**——信任不是来自"最聪明的人验证过"，而是来自"最多种路径殊途同归"。

---

## 五、Agent 的局限与人类的角色：重新定义"科学家的管理工作"

挑战最大的意外收获，是对"人在 Agent 时代做什么"的一次实证回答。主办方毫不掩饰 Agent 的失败模式：

- **局部死循环**：Agent 会在某个错误假设里打转，无法自己跳出。
- **尺度误读**：多个对 Paging 论文给出 "verified" 的团队，是因为**检查停在了 log k 增长显现之前**——Agent 不懂得"该把实验跑多大才够"。
- **单位/归一化错误**：那次被推翻的"2 倍更慢"证伪，本质是 Agent 把不同粒度的时间混为一谈。
- **前提错误的坚持**：有的团队在实验前提就错了的情况下，仍投入一周算力。

而表现最好的工作流，无一例外是**人在方向盘上**：重新指路、质疑假设、在烧掉一周算力之前判断"这个实验的前提是错的"。

最典型的案例是 human-in-the-loop 获奖作品：一篇关于量化后 Stable Diffusion 图像生成的论文，数值指标说"没有崩溃"，但**图像到底能不能用是感知问题**。参与者的 Agent 构建了一个专门的评审 UI，人类亲自逐一判断全部 **128 组图像对**，标注提交进仓库，Agent 再事后验证标注一致性——完整的交互记录连同执行轨迹一起公开，甚至包括参与者那句朴素的"我把 csv 放进仓库了，请检查"。

主办方给出了一个精炼的总结，我认为它值得成为 Agent 时代科学工作的座右铭：

> **人类的角色是"管理智能"（manage intelligence）**——就像教授/PI 为研究生搭建环境（算力、harness、数据权限、关键时刻的针对性反馈），然后让研究生放手去跑。

换句话说：**Agent 时代的科研管理，从"管理人的时间"变成了"管理 Agent 的智能与注意力"**。人不再亲自做每步实验，但人对"什么值得验证、验证到什么规模、什么证据才算数"的判断力，恰恰是唯一不可外包的部分。

---

## 六、启示：科学出版的信任基础设施正在被重建

把这场挑战放在 2026 年的宏观语境里，它其实是三件大事的交汇：

**第一，论文洪流需要"验证即服务"。** ICML 2026 投稿翻倍靠的是 Agent 加速实验与写作，那么唯一能与供给速度匹配的审查方式，也只能是 Agent 加速验证。HF 这场实验证明：**用 Agent 复现一篇论文的成本已经从"审稿人一个周末"降到"一个下午 + 20 美元算力"**——而且可以并行、可以追踪、可以重放。这是同行评审体系第一次有机会"跑赢"论文生产速度。

**第二，评估信任危机蔓延到了科学论文。** 此前系列文章讨论过 SWE-Bench Pro 34% 任务缺陷、Kernel 通过测试却错误——评估基准的信任危机正在向科学论文领域蔓延。而本场实验给出了与基准审计不同的答案：**科学论文不需要"完美"，需要的是让错误可见**。49 篇全证伪论文的作者如果收到结构化证据，多数选择了确认并修正（已有多篇 arXiv 更正在途，还有一位作者在挑战开始前一个月就已悄悄修复——被 HF 称为"独立收敛"）。**可见的错误比隐藏的错误安全得多。**

**第三，Trackio 模式可能成为新常态。** "复现 + 完整执行轨迹 + 自动裁判 + 对抗性重验 + 全部公开"这套流水线，本质上是为科学验证建立了一套**可追溯的证据链**。它把科学从"相信作者"推向"相信证据链"。一旦这种模式从 hackathon 变成学术出版的常规附件，审稿人、读者、甚至未来的 Agent 审稿人都能在几秒钟内回放一篇论文的实验全过程。

当然，也有必须泼的冷水：本次挑战的裁判本身是 GLM-5.2 这样的开源模型，**"裁判的判定"本身也会引入偏差**；toy-scale 复现占 502 篇，说明算力/数据门槛依然高企；而这些验证大多只覆盖了可以自动验证的部分——**感知、审美、临床意义这类问题，128 组图像对的人工评审才是答案**。验证的边界，最终由人类定义。

---

## 结语：把论文当代码来 review

软件工程过去五十年学到的最大教训是：**代码的正确性不能靠作者声明，要靠 CI、测试、可复现构建和 code review**。今天，科学论文的处境和八十年代的软件一模一样——靠作者声明、靠有限抽样的审稿人、靠不可复现的构建环境。

ICML 2026 Open Reproductions 的深层意义，是第一次把软件工程的信任体系搬进了科学出版：**论文即代码仓库，主张即测试用例，logbook 即 CI 日志，Agent 即并行测试机，对抗性重验即 code review。**6,816 份 logbook、274 份执行轨迹、35,908 条主张判定——这些数字象征着一个新纪元的开始：科学结论的信任，不再来自"作者的名气 + 审稿人的良心"，而来自**可回放、可交叉验证、可对抗的证据链**。

HF 在结语里说："我们希望这个纪录不会被保持太久。"我也希望如此。下一场复现挑战，应该由数学、物理、生物领域的顶会来接力——**因为当 Agent 能审计科学时，科学才有资格信任 Agent 生产的结论。**

---

## 参考链接

- [What We Learned by Reproducing 2,200 papers from ICML（Hugging Face Blog, 2026-08-13）](https://huggingface.co/blog/icml-2026-open-reproductions)
- [ICML 2026 Open Reproductions Challenge Space](https://huggingface.co/spaces/ICML-2026-agent-repro/challenge)
- [复现 logbook 画廊（6,816 份）](https://icml-2026-agent-repro-challenge.static.hf.space/gallery.html)
- [Trackio 文档](https://huggingface.co/docs/trackio)
- 相关阅读：[SWE-Bench Pro 信任危机：当 34% 的编码任务被证实"坏了"](https://github.com/kejun/blogpost/blob/main/2026-07-09-swe-bench-pro-crisis-ai-evaluation-benchmark-quality-audit.md)