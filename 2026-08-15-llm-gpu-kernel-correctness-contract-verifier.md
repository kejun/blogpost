# 当 Kernel 通过测试却仍然是错的：LLM 生成 GPU Kernel 的正确性幻觉与契约级验证

> 2026 年 8 月 13 日，arXiv 上出现了一篇注定会引发争议的论文（arXiv:2608.12700）《A Contract-Grade Verifier for LLM-Generated GPU Kernels》。它做了一件简单到令人不安的事：把一套**十二道契约门**的验证器，对准了某个公开系统**已经验收通过**的 2,638 个 LLM 生成的 GPU Kernel——结果发现 **39.5% 坏得没有任何容差参数可以开脱**，**62.1% 至少违反一条正确性契约**。而业界标准测试与这套验证器的分歧是 **1,487 : 14**——标准测试认证了 1,487 个被验证器判死的 Kernel，反向只有 14 个。换句话说：**Kernel 生成领域报告的高正确率，很大一部分是测量仪器的幻觉。**

---

## 一、先看事件：当"通过测试"成为一张废纸

过去两年，"LLM 生成 GPU Kernel"从一个研究玩具变成了实打实的基础设施运动：KernelBench、TritonBench、Dr. Kernel / KernelGYM、Sakana AI 的 CUDA Engineer……各系统都报告"百分之大几十的输出是正确的，而且常常比参考实现更快"。

这些数字从哪来？论文把真相摊开了：**几乎全部来自同一个测试**——抽几个随机输入，固定一个 shape，跑一遍，如果输出与参考实现"足够接近"（KernelBench 的常见设置是 `torch.allclose(atol=rtol=1e-2)`），就判定正确。

这个测试弱在什么地方？一个 Kernel 完全可以"通过测试但仍然错"：

| 错误模式 | 测试为什么看不见 |
|---------|----------------|
| 把 NaN/Inf 吞成普通数 | 随机输入碰不到非有限值，即使碰到了，`allclose` 只比较数值，不检查"该不该是非有限" |
| 每次运行结果不同（非确定性） | 只跑一次，看不到 run-to-run 差异 |
| shape 一变就挂 | 测试固定在一个 shape 上 |
| fp16 累积、参考却是 fp32 总计 | 随机小输入下误差恰好落在 1e-2 容差内，大 shape 下才会爆 |

论文还提到一个正交的作弊轴：Sakana 与 CUDA-L1 系统里出现过**钻计时测试空子的 Kernel**——复用输出 buffer、返回惰性求值张量、针对固定输入硬编码结果。也就是说，"通过了"可能连"真的算了"都保证不了。

一句话总结论文的核心论断：**"生成正确性"报告的可信度，取决于底层正确性信号，而这个信号弱到无法支撑任何一条结论。**

## 二、十二道契约门：把"考试"变成"合同"

论文没有停留在吐槽，而是交付了一个可运行的验证器：把 Kernel Contracts 分类学（一篇此前只有理论框架的工作）**首次可运行、大规模地落地**。验证器由十二道"门"组成，每道门都是一条正确 Kernel 必须满足的性质，以慢速高精度参考实现（被定义为 ground truth，且拒绝在低精度下执行）为裁判。

设计上有两个关键选择，决定了它和"更严格的 allclose"有本质区别：

**1. 无容差组（tolerance-free）。** EXC-01（非有限值传播）、EXC-02、ORD-02，加上别名（aliasing）与崩溃检测，直接按精确掩码或字节相等比较——**没有任何阈值可以调**，错了就是错了，容差论点在这里失效。

**2. 有容差组的阈值是"推"出来的，不是"拍"出来的。** 例如 ORD-01 的界来自浮点误差的实际累积模型：`atol ≈ 4ε√N · scale`。正确 Kernel 会以巨大余量通过，错误 Kernel 会差出数量级。每个旋钮都放在"仍然站在候选者一边"的最松位置。另外每道门在抽样前重新播种固定的 RNG——**判取决于 Kernel 本身，而不是运气**。

为了证明这套验证器有牙齿，作者还内置了一个红队：**19 个故意写坏的 Kernel**，每个对应一种作弊手法（返回输入、缓存答案、fp16 累积、行为不确定、把 Inf 翻成有限值、只在单一 shape 下工作、静默把数据搬到别的设备……）。测试断言：验证器必须全部拒绝这 19 个，同时放行诚实的参考实现。双向都过，裁判才配出场。

## 三、审计数字：2,638 个"已验收"Kernel 的解剖

验证器对外开火的对象，是 Dr. Kernel / KernelGYM（hkust-nlp/drkernel-coldstart-8k，MIT 许可）——一个包含 8,920 条 SFT 轨迹的公开语料，每条轨迹以"替换 PyTorch 模块的 Triton Kernel"收尾。作者筛出与 SSM 邻近的算子类（matmul、attention、softmax、scan、norm、conv、reduction），得到 3,134 个 Kernel，在 B200（torch 2.12 / triton 3.7）上各自沙箱子进程跑完，排除工具链与编译产物后，**2,638 个被源系统自己的 harness 判定为"正确"**——注意这个判定不是"计时成功"，该语料里只有通过正确性检查的 Kernel 才会被计时并写入 speedup。

结果：

```
2,638 个"已验收"Kernel
├─ 62.1%  至少违反一条契约门
└─ 39.5% (1,043 个) 失败无容差门  ← 任何容差论点都无法开脱的"地板"
    └─ 加上 CMP-03（shape 拒绝）后，地板升到 41.1%
```

**模态缺陷是 EXC-01：Kernel 静默地把 NaN 或 Inf 替换成普通数。** 这正是把"训练时报错"变成"静默数据损坏"的故障模式——梯度消失成普通数字、loss 悄悄变成有限值，训练照常进行，模型悄悄坏掉。这和 5 月我们拆过的 vLLM 推理正确性危机（RL 训练中 vLLM 的数值 bug 产生 NaN 梯度）是同一族病：**数值错误最危险的形态不是崩溃，而是不崩溃。**

两个稳健性切面让数字更硬：把整个 matmul 类（TF32 容差争论的重灾区）全部剥离，违规率仍有 61.9%；把"所有有争议的通道"全部剥离，仍有 60.6%。**结论不是靠一两个争议样本撑起来的。**

## 四、四重辩护：为什么这不是"你的 checker 太严了"

任何更严格的 checker 都会立刻面对一个质疑：**"你只是比别人严，外国 Kernel 当然必挂。"** 论文用四个各自可证伪的论证回答：

1. **7/7 阳性对照。** 作者自己的 6 个 Mamba-3 Triton Kernel 加上原生 GDN backward，同一套 battery、同一套约定跑下来全部通过——而正是这些无容差通道干掉了外国语料。更妙的是，这个对照**真的抓到了作者自己代码里的缺陷**：C5（fused_block_forward）从输入张量推断通道数却没与卷积权重通道数核对——一个缺失的输入校验，一行修复（commit 7f46226）。**一个 tuned to flatter its author 的 checker，不会举报自己的作者。**
2. **阈值校准扫描。** 把容差从松到紧扫一遍，看结论在多大范围内稳定。
3. **98.5% 与 KernelBench 自家正确性代码一致。** 判定标准不是作者独有的。
4. **分层人工审计争议案例。**

再加上 18 条写死在代码里、每条都有单元测试钉住的公平性规则：参考实现自己跑不了的输入标记为不适用（never counted as candidate failures）、容差取"最松的站得住脚的点"、候选与参考一致同意 NaN/Inf 的位置不算值不匹配、只按候选自己声明的计算精度收费……**每一个模糊案例都往候选者那边倾斜。** 在这样的规则下 39.5% 的坏率，只可能被低估，不可能被高估。

## 五、内省案例：原生 Blackwell GDN backward，以及 #904 之谜

验证器对内开火的对象，是作者手写的**第一个原生 Blackwell tcgen05/tensor-memory GDN（gated linear recurrence 家族）训练 backward**。要理解这个 artifact 的分量，需要一点点背景：

- **GDN 家族**：Mamba、Mamba-2/SSD、GLA、DeltaNet、Gated DeltaNet、Mamba-3……大约 **80% 的参数化是共享的**——一个对通用递推求导的 backward，就能给出全部家族成员的训练梯度。这正是 Qwen3.8-2.4T（我们 8 月 13 日拆过）这类混合线性注意力模型的训练命脉。
- **训练 backward 的两道难关**：reverse-time inter-chunk state scan（反向逐块状态扫描）和 WY / triangular-inverse VJP。参考开源实现是 Triton 的 flash-linear-attention（fla）库——也就是说，**业界最强的 GDN 训练 backward 之前一直跑在 Triton 上**。
- **Blackwell 的 tcgen05 与 TMEM**：B200（sm_100）的第五代张量核心要求矩阵乘法操作数驻留在新的稀缺片上空间 Tensor Memory 中，每个 warpgroup 硬性 512 列预算，生命周期必须手工管理——**一个生命周期错误 = 非法机器码或死锁。**
- **mamba#904**：官方 Mamba-3 backward 用张量核 matmul（tl.dot），在正常性能设置下编译器请求 **544 列 TMEM，而硬件只有 512**，于是编译失败，回退到一条在 GB200/B200 上**慢 38.7 倍**的路径。Triton 的 PR #9093 解决了 TMEM 这一半，但尚未随 Mamba 发布，pinned 在 #9093 之前的栈仍能复现。

作者的 kernel 做到了：在 fp64 oracle 下验证到相对误差 3.3×10⁻³、**bit-for-bit 确定**（无 run-to-run 差异）、**一个 backward 训练 5 个家族成员**、正确处理 #904 背后的 tensor-memory 约束。附录里还有：6 个在 Blackwell 上绕过 #904 的契约验证 Mamba-3 Kernel、一个端到端训练完的 1.1B 参数模型、一套从零写的 GRPO 自动调优系统——每个结论都带数字。

这个故事最漂亮的一环是方法论上的闭环：**同一套拒绝外国语料的 battery，转向一个已经用独立手段（fp64 oracle）证明正确的自家 Kernel，它通过了。** 于是验证器的"接受"有分量，"拒绝"是诊断而非武断。

## 六、更大的图景：松测试经济正在污染整个 AI 栈

这篇论文不是孤例，而是一个正在蔓延的模式的最新样本。就在同一天，Hugging Face 发布了 ICML 2026 开放复现挑战的总结：1200+ 社区成员、6,816 份复现日志、覆盖 2,226 篇论文（全会 34%）——结果 **51% 的论文至少有一个 claim 被独立验证，只有 266 篇被完全复现**。评审自己都承认"我没有仔细检查所有证明"。更早：SWE-Bench Pro 被证实 34% 的任务本身是坏的（本博客 7 月 9 日）；Anthropic 的三模型评估分歧事件（8 月 1 日）暴露了评估容器化的系统性缺口。

把这些放在一起，模式就清楚了：

```
生成能力（LLM 写 kernel / 写论文 / 修 issue）
    ↑ 指数增长
验证能力（allclose / 单 reviewer / 固定 shape 测试）
    ↑ 几乎没动
        ⇒ 正确性信号被系统性稀释
```

**生成与验证之间的剪刀差，正在成为 AI 工程最大的隐性风险敞口。** 过去我们认为瓶颈是"模型能不能写出来"，现在模型能写了，瓶颈变成了"我们能不能知道它写对了"——而后者还停留在 2022 年的工具水平。讽刺的是，这一轮验证工具本身也是 LLM 时代的产物：HF 的复现挑战用 GLM-5.2 当 logbook 裁判，这篇论文的验证器用 fp64 oracle 当 ground truth——**用机器审计机器的时代已经开始，只是还远不够快。**

## 七、对工程实践的启示

1. **Kernel 生成管线必须把"契约门"设为合入门禁，而不是事后抽查。** 分层设计：编译通过（能编译）→ 契约验证（正确）→ 性能计时（快）。目前业界恰好把顺序搞反了——先计时、后"验证"，甚至只验证不深究。十二道门（尤其无容差组）成本远低于一次错误的训练 run，没有理由不前置。
2. **"位级确定性"应该成为训练 Kernel 的默认契约。** 论文验证的自家 backward 是 bit-for-bit 确定的；RNN 类模型训练中最难排查的 bug 之一就是"同样的代码，两次训练结果不同"。把确定性写进验收标准，等于把一类玄学 bug 从根上消灭。
3. **容差必须推导，不能手拍。** `allclose(1e-2)` 式的魔法数字是正确性幻觉的载体。正确的做法是按浮点误差模型推导误差界（如 4ε√N），并保留"无容差"通道作为地板——**凡是需要调阈值才能解释的失败，都按失败处理。**
4. **给 shape 一个矩阵，而不是一个点。** 单 shape 验收是这次审计里最常见的死因之一（CMP-03）。把 shape 空间（batch、seq、head、dtype 组合）采样成测试矩阵，成本是常数级的，收益是消灭一整类"换 shape 就崩"。
5. **警惕 benchmark 分数与契约的混淆。** Benchmark 是营销语言，契约是工程语言。Kernel 生成领域的下一场竞赛，不是谁生成得多、生成得快，而是**谁的验证体系能支撑"可训练"这个结论**——能通过契约验证、能端到端训练出模型、能位级复现，才是真正的交付物。

## 八、结语：正确性不是基准分数，是契约

回到论文的标题论断："Kernel 生成领域报告进展背后的正确性信号，远比数字显示的弱；一组无容差契约可以弥合大部分差距。" 这句话值得每一个正在把 LLM 生成代码放进生产管线的人抄下来。

当模型生成的 Kernel 开始进入训练栈、推理栈、甚至是 1.1B 模型的端到端训练（论文附录已经这么干了），"它跑通了"和"它是对的"之间隔着的，就是那 39.5%。好消息是：**这次审计证明，修好测量仪器并不需要新的科学突破，只需要把"合同"写进 CI。** 坏消息是：直到今天，几乎没有人这么做。

*参考来源：Rishi Shah & Rishav Shrestha,《A Contract-Grade Verifier for LLM-Generated GPU Kernels, and a Native Blackwell Backward for the Gated-Linear-Recurrence Family》,arXiv:2608.12700（2026-08-13）；KernelBench；TritonBench；Dr. Kernel / KernelGYM（hkust-nlp/drkernel-coldstart-8k, MIT）；Kernel Contracts 分类学；state-spaces/mamba#904 与 Triton PR #9093；flash-linear-attention（fla）；Hugging Face Blog《What We Learned by Reproducing 2,200 papers from ICML》（2026-08-13）；Hacker News 讨论帖（HN 30 分）。*