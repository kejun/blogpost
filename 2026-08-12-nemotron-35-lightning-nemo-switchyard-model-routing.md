# 当 Agent 学会"按需分配"模型：Nemotron 3.5 Lightning 与 NeMo Switchyard 如何重写 Agent 经济学

**文档日期：** 2026 年 8 月 12 日  
**标签：** Nemotron 3.5 Lightning, NeMo Switchyard, 模型路由, System of Models, MoE, Agent 经济学, 开源模型, 本地推理, RLVR, LangChain

---

## 一、引言：一个尴尬的默认值

想象一个典型的 Agent 工作流：它先读一个文件，再查一个 API，然后思考"为什么这个测试挂了"，最后写一段代码。在过去两年里，绝大多数团队的实现方式惊人地一致——**把每一步请求都发给同一个模型，按同一个单价计费**。

这个默认值曾经是合理的：模型之间能力差距不大、价格差距也不大，"无脑用最强的"损失有限。但 2026 年的现实已经完全不同：前沿模型变得更贵、更强，而开源权重模型变得又快又便宜，足以承担 Agent 工作中相当一部分（虽然不是全部）任务。LangChain 在 8 月 11 日发布的基准测试中问了一个尖锐的问题：

> *"How many of your agent's calls actually need a frontier model?"*
> （你的 Agent 调用里，究竟有多少真的需要前沿模型？）

答案只有 **7%**。

8 月 11 日，NVIDIA 同步发布了两件互相咬合的东西：**Nemotron 3.5 Lightning**——一个专为长时间运行 Agent 工作负载设计的 30B MoE 开源模型；以及 **NeMo Switchyard**——一个让"把请求路由到最合适的模型"变成一行配置的开源路由库。前者是"系统之模型"（system of models）架构里的合格执行者，后者是让这套架构从"PPT 概念"变成"生产事实"的调度层。

本文将从模型设计、路由技术、实测数据、生态案例四个维度深度拆解这次发布，并讨论一个更本质的问题：**当"用哪个模型"从人工决策变成系统决策，Agent 经济学的游戏规则被改写了什么。**

---

## 二、背景：为什么"单模型默认"正在系统性失效

要理解这次发布的价值，先要理解它解决的问题。过去 18 个月，模型生态发生了两个被低估的结构性变化：

**1. 能力差距与价格差距同步扩大。** 前沿推理模型（如 Claude Opus 4.8、GPT-5.6）的定价逐年走高，而开源 MoE 模型（30B 总参数、3B 激活这一类）的性价比在持续改善。以 LangChain 实测为例：一次 Opus 调用约 0.0324 美元，而 Nemotron 3.5 Lightning 一次调用仅 0.00037 美元——**单价差约 87 倍**。与此同时，二者的能力差在真实 Agent 任务上并没有 87 倍那么大（LangChain 的 145 个任务套件上仅差 8 个百分点）。

**2. 一个任务内部的"能力需求"是波动的。** NVIDIA 在技术博客里给出了一个经典例子：在 Terminal-Bench Hard 基准上，DeepSeek V4 总体准确率最高，但按任务组拆开看，Kimi K2.6 在 ML/RL 任务组上更优，Qwen3.5 397B A17B 在数学/科学任务组上更优。**"最好的模型"根本不存在，只存在"对某个任务组最好的模型"。**

更关键的是，这种波动发生在单个任务的内部：Agent 早期需要探索代码库、从错误中恢复（重推理），后期进入机械性的实现阶段（轻推理）。一个"读文件"的 turn 和一个"排查测试失败原因"的 turn，在今天被塞进同一个模型、按同一个单价计费——前者是浪费，后者是风险。

**"系统之模型"（system of models）** 正是对此的回应：让一个前沿推理模型负责规划与编排，让一群小型专用模型负责高吞吐的定向任务（代码审查、工具调用、安全告警监控、客服问答）。这个架构在业界早已是共识，但一直缺两块拼图：**一个足够好的"专用执行者"模型，和一个不被供应商绑死的路由层。** NVIDIA 这次一次补齐了两块。

---

## 三、Nemotron 3.5 Lightning：为"高吞吐专用任务"而生的执行者

### 3.1 架构与定位

Nemotron 3.5 Lightning 是一个 **30B 总参数、3B 激活**（30B-A3B）的 Mixture-of-Experts 模型。这个数字组合是 2026 年"性价比模型"的典型配方：总参数够大以容纳知识，激活参数够小以保证吞吐。它的定位非常明确——不是又一个通用聊天模型，而是**多 Agent 系统中承担高吞吐、定向、长时运行任务的执行者**：

- **4 倍输出速度**，带来 **30% 更快的 Agentic 任务完成时间**（相对同类模型）；
- 专为"always-on"场景设计：代码审查、工具调用、安全告警监控、账单问答这类量大、重复、延迟敏感的任务；
- **完全开放可定制**：可用 NVIDIA NeMo 栈基于企业自有数据、工具和工作流做后训练，精准适配领域任务。

### 3.2 从"模型"到"可训练资产"

与发布同步，NVIDIA 还放出了 **Nemotron-RL-Agentic-Terminal-Pivot** 数据集——一个用于后训练编码 Agent 能力的强化学习数据集。这透露出一个明确信号：**Lightning 不是终点，而是"可塑形的基础材料"。** 企业拿到的不只是一个模型权重，而是一整套"模型 + 训练数据 + 训练栈（NeMo AutoModel / NeMo RL）"的可定制资产。

CodeRabbit 的实践证明了这条路径的性价比（详见第五节）：用不到 3 小时实验、不到 100 美元成本，就把 Lightning 训练成了自家路由策略的专用模型，准确率反超原 GPT-class 方案约 4%，推理成本下降约 50%。

### 3.3 部署半径：从 Jetson 到数据中心

Lightning 可以跑在 **RTX PC、DGX Spark、DGX Station、Jetson** 等本地设备上，也可以横向扩展到 RTX PRO 工作站、数据中心与云端。对高吞吐专用任务而言，"就近部署"直接改写延迟与隐私账本——这也是 NVIDIA 这次发布里最容易被忽视的一层：**模型、路由库、硬件三者被设计成一套闭环**（详见第六节）。

---

## 四、NeMo Switchyard：把"路由"从工程难题变成开源库

如果说 Lightning 是"系统之模型"的肌肉，Switchyard 就是神经系统。它解决的核心问题是：**路由决策的信号从哪来、在何时何地评估、以及如何不重写应用就完成模型切换。**

### 4.1 架构：三层解耦

- **switchyard-libsy**：与供应商无关的 SDK。每个模型目标有一个语义名，背后的 client 把语义名映射到具体供应商端点与模型 ID——**路由逻辑与供应商彻底解耦**，团队换模型、换端点、换供应商都不需要动路由集成。
- **Switchyard server**：一个模拟 LLM 网关的参考实现，接受 OpenAI、Anthropic、Responses API 格式的请求，翻译成内部路由格式并返回标准响应。它还会记录所选模型、决策理由、token 用量、延迟与调用结果——路由过程本身可观测、可审计。
- **会话级状态**：路由可以携带跨 turn 的状态（如"亲和性"决策、工具结果），也可以无状态。这为"一个任务中途换模型"提供了基础设施。

### 4.2 信号来源：三个层次

Switchyard 的路由决策依赖三类信号，这也是理解路由能力的框架：

1. **请求本身**：分类器判断主题/难度，或用 embedding 提取特征；
2. **模型状态**：logprobs、残差流（residual stream）、注意力矩阵等模型内部信号；
3. **系统信号**：定价、延迟、负载、Agent 错误率等运行时信号。

### 4.3 路由算法：从免训练到可学习

Switchyard 提供了三档路由策略，复杂度与收益递增：

**① LLM Classifier（免训练）**——一个小型 judge 模型为每个请求选择目标模型，并可在会话内保持"亲和性"。支持三种模式：*capability*（按调用选模型）、*escalation*（见下）、*custom*（自定义策略）。代价是每次决策多一次小型模型调用。

**② Stage Router（启发式，零额外调用）**——根据工作流阶段路由：严重错误、重复无效工作、长时间探索 → 推给强模型；稳定写入、测试通过 → 推给高效模型。不增加任何模型调用，延迟成本几乎为零，但依赖流量中是否出现它识别的信号。

**③ Prefill Router（可学习，研究阶段）**——训练时从 LLM 的残差流中提取特征，用一个共享主干 MLP 预测每个候选模型的准确率（arXiv:2603.20895）；推理时根据 prefill 状态打分，再混合成本/延迟约束做最终路由。**这是"路由信号从模型内部来"的探索方向**——不再只看请求文本，而是看模型"读到请求时的反应"。

### 4.4 Escalation：路由中最优雅的机制

LangChain 实际基准测试的是 escalation 模式，值得单独拆解：**每个任务从便宜模型开始，一个小型 judge 模型逐 turn 评估"是否在正轨上"，连续两次负面裁决后，任务被"升级"到前沿模型并保持到会话结束。** 这是一扇单向门（one-way door）——升级后不再降级，从而避免 judge 在每个 turn 都运行。

这个设计的精妙之处在于把"判断任务难度"的成本与"解决任务"的成本分开：**容易的任务几乎不付 judge 费，困难的任务只付一次升级代价。** 它把路由从"静态分类"变成了"自适应过程"——这也是它比其他路由策略更适合多轮 Agent 的根本原因。

---

## 五、实测数据：7% 的调用，74% 的成本削减，以及诚实的代价

### 5.1 LangChain 基准：145 个生产级多轮任务

LangChain 用自家 Deep Agents 评估套件（145 个多轮任务，平均每个任务 6.3 次模型调用，覆盖客服对话、on-call 事件调查、跨消息/工单/邮件的多步自动化）测试了"Opus 4.8 + Nemotron 3.5 Lightning + escalation 路由"的组合，结果如下（按缓存输入价计费）：

| 方案 | 准确率 | 每次运行成本 | 每完成任务成本 |
|------|--------|-------------|---------------|
| Opus 4.8 单独 | 86.0% | $11.45 | $0.092 |
| **Opus + Lightning（路由）** | **80.0%** | **$3.00** | **$0.026** |
| Nemotron 3.5 Lightning 单独 | 77.7% | $0.72 | $0.006 |

三个数字值得反复咀嚼：

**① 流量与账单的极端错位。** Lightning 承担了 **93% 的模型调用，却只占 10.4% 的花费**；Opus 只承担 7% 的调用，却吃掉 68.4% 的账单。**"最后 6 个百分点的准确率，花了 3.5 倍的单任务成本。"** 这就是为什么"全部用最强模型"在 2026 年是纯粹的浪费——你为 93% 的简单调用支付了前沿溢价。

**② Judge 模型是隐藏的第二大成本。** 路由方案中 judge 占了 21.2% 的花费——排在 Opus 之后、Lightning 之前。原因很反直觉：judge 在每个未升级的 turn 上运行，且**不像前沿模型那样享受 prompt caching 优惠**。这意味着优化路由成本的第一杠杆不是"升级率"，而是"judge 本身"——换个更便宜的 judge，或跳过明显正常的 turn，直接砍账单。

**③ 成本是分布，不是数字。** 五次运行中，路由到前沿模型的流量在 4.1%–9.1% 之间波动（均值 6.9%），账单在 $2.16–$3.61 之间摆动——**高达 67% 的波动**。路由降低的是平均成本，同时加宽了成本区间。**预算应该按区间的上限来做，而不是按均值。**

### 5.2 决策公式：什么时候该用路由

LangChain 给出了一个可操作的判据：

> **minimum offload = judge cost / (expensive cost − cheap cost)**
> （最低卸载比例 = judge 成本 ÷ 两个模型的价格差）

Judge 是每一轮都要付的固定税，所以问题从来不是"便宜模型够不够好"，而是"**两个模型的价差是否足够宽，宽到能养得起 judge**"。在 LangChain 的组合里：judge 每次运行 $0.64，价格差 $10.73，需要卸载 5.9% 的 turn 才能回本——实际卸载了 93%，超出 16 倍，所以"稳赚不赔"。反过来，如果两个模型价格接近，公式会要求卸载超过 100% 的流量——**路由直接出局**。唯一的例外是本地自托管便宜模型（比如跑在 DGX Spark 上），推理成本趋近于零，价差重新拉宽，路由又变得划算。

### 5.3 诚实的边界

LangChain 的结论里有一段罕见的老实话：**Nemotron 3.5 Lightning 单独跑也有 77.7% 的准确率、0.72 美元的成本**——路由只比它高 2.3 个点，却贵 4.2 倍。如果只看最低成本，直接全用 Lightning 反而更好。那路由的价值在哪？

答案是**不确定性**。这 145 个任务的结果是已知的（事后视角）；生产环境里你无法预知下一个请求是难是易——全用便宜模型，意味着在困难请求上"裸奔"。**路由是"为不确定性支付的保险费"**：它把成本上限从 $11.45 压到 $3.61（最差的一次路由运行也只有 Opus 单独的三分之一），同时保证困难请求能拿到前沿能力。另外两个明确的"不适用"场景：延迟敏感系统（judge 每次决策约 700ms，是第二重模型调用）和短对话（escalation 需要多轮轨迹才有信号可读）。

---

## 六、生态实证：路由从"demo"到"生产"的六个样本

Switchyard 的发布价值，最终要靠生态验证。发布当天披露的合作伙伴数据已经相当有说服力：

| 伙伴 | 场景 | 结果 |
|------|------|------|
| **Cognition** | Devin Desktop 集成 staged routing（Opus 5 ↔ Kimi K2.7） | FrontierCode Main 50.6%（距 Opus 5 仅 2.8 点），平均成本降 **28%** |
| **Ramp** | Ramp SWE-Bench 金融软件工程 | 匹配前沿模型性能，成本降 **58%**，运行时间降 **33%** |
| **Boomi** | Agent Garden 企业自动化 | 5 项路由能力 **100%** 领域路由精度，59% 流量转给 5 倍更快的微调模型，后期延迟降 21% |
| **Classmethod** | opencode + Fireworks 工作负载 | 成本降 **27%**，质量不变 |
| **Cadence** | ChipStack AI Super Agent 形式化验证 | 效率提升 **9.9%** |
| **NVIDIA 内部** | 综合基准 | 保持前沿精度，任务成本降至 Opus 4.8 单独使用的近 **1/3** |

Cognition 的案例特别值得注意：它路由的是 **Opus 5 与 Kimi K2.7 两个前沿模型之间**——不是"前沿 vs 便宜"，而是"不同前沿模型在不同任务阶段的互补"。这印证了 NVIDIA 在 Terminal-Bench Hard 上的观察：**模型优劣是任务组相关的，路由的价值不止于省钱，还在于"能力拼接"。**

### CodeRabbit：让路由决策本身成为可训练的

在所有案例里，CodeRabbit 的实践在概念上最领先：它没有把 Lightning 当作"被路由的模型"，而是**用 Lightning 训练了一个"路由决策者"**——每个代码审查请求先由一个模型判断复杂度标签，再据此生成审查配置。

- **方法**：两阶段后训练——先用 9,996 个蒸馏样本做 SFT（rank-8 LoRA，一个 epoch），再用 910 个精选 prompt 做 RLVR（GRPO）；
- **成本**：不到 3 小时实验、不到 100 美元；
- **效果**：精确路由一致率 75.8% → 80.4%（SFT）→ 80.7%（SFT+RLVR）；输出一致性 Cohen's kappa 0.429 → 0.461 → **0.544**；相对原 GPT-class 方案准确率高约 4%、推理成本降约 50%（同一批 1,000 个任务，OpenAI 公开价 $2.34 vs Baseten 单张 A100 $1.16），输出 token 还少了 63.4%。

这个案例的深层含义是：**路由策略本身正在成为企业知识资产**。当"什么请求该给什么模型"被蒸馏进一个小模型、跑在自托管 A100 上，路由就从"云厂商的计费策略"变成了"企业的竞争壁垒"——你可以教模型你的路由规则，而不是向供应商购买路由。

---

## 七、深度思考：这次发布改写了什么

### 7.1 从"选模型"到"设计模型系统"

过去一年，模型路由的文章写过很多（本博客 7 月 18 日就分析过模型路由的通用优化框架），但这次发布标志着三个实质性变化：

**第一，路由从"锦上添花"变成"架构决策"。** LangChain、Cognition、Ramp 的集成都不是实验性 demo，而是进入了生产链路（Devin Desktop 甚至直接部署给 NVIDIA 内部用户）。路由库开始内嵌进网关（Kong）、代理层（LiteLLM）、Agent 框架（LangChain、Hermes）——**它正在成为 Agent 基础设施的标配中间件**，就像 2025 年的 RAG 一样。

**第二，专用模型的经济性被"系统化地"证明。** 30B-A3B 这类模型单独跑已经能覆盖 77–93% 的 Agent 流量，而 NVIDIA 用"模型 + 路由 + 硬件 + 训练栈"把它变成了一个完整的产品叙事。**"先买一个便宜模型，再买一个路由器，再买一张本地 GPU"——卖铲子的人开始卖"分铲子的机器人"了。** 这套闭环正是 NVIDIA 的壁垒：模型（Lightning）引导你买路由库（Switchyard），路由库的价值又依赖本地硬件（DGX Spark）来放大价差，训练栈（NeMo RL）则让模型可定制。四者互为钩子。

**第三，路由与 RL 的合流。** CodeRabbit 用 RLVR 训练路由决策者，prefill router 用残差流学习难度预测——**"该用哪个模型"正在从一个手工规则问题，变成一个可学习的预测问题。** 未来最可能的形态是：路由策略在系统层面持续强化学习，把每一次"升级/不升级"的决策当作一次带奖励的 trial，让路由本身随业务流量自适应进化。

### 7.2 必须泼的冷水

这次发布也有一组不容回避的局限：

- **评估饱和问题**：LangChain 明说自己的套件"饱和"——30B 模型与前沿模型只差 8 个点，给路由留的证明空间很小。真实世界的长尾任务可能差距更大，路由的价值也可能更大——但"可能"需要自己验证。
- **延迟税与波动**：judge 的 700ms 和 67% 的成本波动，对延迟敏感、预算刚性（每月固定成本）的系统是硬约束。
- **精度权衡是不可消除的**：6 个点换 74% 成本削减在多数场景划算，但"最后 6 个点"恰好在合规、医疗、金融场景里可能就是红线。路由是工程决策，不是免费午餐。

---

## 八、结论：Agent 经济学的新常态

Nemotron 3.5 Lightning 与 NeMo Switchyard 的发布，本质上是 NVIDIA 对 2026 年 Agent 基础设施的一次系统性回答：**前沿模型负责"想"，专用模型负责"做"，路由层负责"分"，本地硬件负责"守"。**

对开发者而言，最值得带走的不是某个具体数字，而是一个思维转变：**"我的 Agent 用哪个模型"不再是一个一次性的选型决策，而是一个持续的、可学习的、可观测的系统决策。** 当 93% 的调用本可以用 1/87 的单价完成，当路由策略可以花 100 美元蒸馏进自己的小模型，坚持"一个模型走天下"就不再是简单，而是昂贵。

**"System of models is not a luxury. It's the new default."**——这句话正在从愿景变成账单上的现实。

---

## 参考来源

- NVIDIA 官方博客：*NVIDIA Nemotron 3.5 Lightning and NeMo Switchyard Deliver Faster, Smarter, More Efficient Agentic AI*（2026-08-11）
- NVIDIA 开发者博客：*Route AI Agent Workloads Across Models with NVIDIA NeMo Switchyard*（2026-08-11）
- LangChain 博客：*How many of your agent's calls actually need a frontier model?*（2026-08-11）
- CodeRabbit 博客：*Teaching NVIDIA Nemotron 3.5 Lightning to route code reviews*（2026-08-11）
- arXiv:2603.20895（Prefill router 相关：基于残差流的路由预测）
- Hugging Face 模型页：NVIDIA-Nemotron-3.5-Lightning-30B-A3B-NVFP4
- GitHub：NVIDIA-NeMo/Switchyard