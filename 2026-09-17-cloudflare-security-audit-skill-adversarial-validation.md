# 当安全审计变成"六阶段流水线"：Cloudflare security-audit-skill 深度拆解——对抗性验证、"发现者不验证"与 Agent 时代的审计方法论

> 9 月 16 日，Cloudflare 开源了 `security-audit-skill`：一个把通用编码 Agent 变成"安全审计员"的技能包。发布一天，GitHub 上就以 **927 星/日** 的速度冲到 7,135 星。它不是又一个"提示词合集"——它是一套**六阶段的结构化审计流水线**：侦察、覆盖驱动狩猎、候选验证、结构化输出、独立记录复核、目标中立报告。核心原则只有一句话：**发现漏洞的 Agent，永远不能是验证漏洞的那个 Agent。** 这篇文章拆解它的架构、为什么这么设计，以及 Cloudflare 如何把它从 450 行提示词演进成覆盖 128 个仓库的舰队级漏洞发现系统。

先看它站在什么浪潮上。这几天 GitHub Trending 上，Agent"技能"类项目集体爆发：`addyosmani/agent-skills`（生产级工程技能，9.5 万星，日增 658）、`SnailSploit/Claude-Red`（进攻性安全技能，5,758 星，日增 367）、`alphaXiv/OpenResearch`（把编码 Agent 变成研究 Agent，日增 1,017）。加上 Cloudflare 这个安全审计技能，信号已经很明确了：**2026 年下半年，Agent 的竞争焦点正在从"模型有多强"转向"技能有多专业"。** 而安全审计——一个对精确率要求苛刻、对幻觉零容忍、天然需要多角色协作的领域——成了检验"技能化"成色的最佳试验场。

---

## 一、为什么是"安全审计"：一个为 Agent 量身定做的难题

先回答一个基础问题：安全审计有什么特殊，值得 Cloudflare 专门造一套技能？

因为它是**所有软件工程任务里，最不适合"单 Agent 一口气干完"的那一类**：

1. **幻觉是致命的**。编码 Agent 写错一个函数，CI 会报错；审计 Agent 报一个假漏洞，会浪费安全团队几小时的研判时间，更糟的是——假阳性会淹没真阳性，让整个流水线失去信任。
2. **覆盖面要求极高**。代码审查可以"差不多"，安全审计必须覆盖每个输入面、每个信任边界。而 LLM 的上下文窗口有限，"读完全部代码再找漏洞"在物理上就不可能。
3. **需要多角色对抗**。找漏洞的人天然会"爱上"自己找到的漏洞——人类审计员如此，Agent 更严重。模型一旦认定某个路径有问题，就会用同样的"推理惯性"自我证实。**唯一可靠的纠偏手段，是让一个没有先入之见的独立 Agent 试图推翻它。**

Cloudflare 在 Project Glasswing（用前沿安全模型审计企业代码库的系列实验）中得出的结论更直接：**通用编码 Agent 干不了这活**——它们一次只能 hold 一个假设，上下文窗口在覆盖仓库一小块之后就满了，然后开始"吃掉"自己的记忆，把上午找到的漏洞忘得一干二净。

所以答案不是"更好的模型"，而是**把审计本身工程化**：拆阶段、强隔离、加校验、留证据。这就是 security-audit-skill 的由来。

## 二、六阶段流水线：一次审计就是一次"生产线"

这个技能把一个完整审计拆成六个阶段，每个阶段有专门的角色、产物和校验点：

| 阶段 | 做什么 | 核心产物 |
|------|--------|---------|
| ① 侦察 Reconnaissance | 绘制架构图：信任边界、输入面、已有证据、确定性覆盖 | `architecture.md` + `coverage-ledger.json` |
| ② 覆盖驱动狩猎 Coverage-led Hunting | 按账本单元分配**相互隔离**的 Hunter，记录检查项；coverage critic 找覆盖缺口 | 候选漏洞列表 |
| ③ 候选验证 Candidate Validation | 每个候选交给**全新的验证者**，任务只有一个：推翻它 | `confirmed` / `needs_validation` / `rejected` |
| ④ 结构化输出 Structured Output | 写入 `findings.json`，用 `report-schema.json` 做 schema 校验 | 机器可读的结构化结果 |
| ⑤ 独立记录复核 Independent Record Verification | 全新 Agent 逐一核对源码证据；被"实质性替换"的条目再换一个独立验证者 | 最终可信记录 |
| ⑥ 目标中立报告 Target-neutral Reporting | 由已验证记录派生 `REPORT.md`、`FINDINGS-DETAIL.md`、`NEEDS-VALIDATION.md` | 人可读报告 |

注意几个容易被忽略的工程细节：

**覆盖账本（coverage-ledger.json）是整个系统的"数据库"。** 它记录每个（区域 × 攻击类别）单元被谁查过、查到什么程度。跑完一轮，账本就永久沉淀下来——**同一仓库的多次审计是"增量式"的**：新审计基于旧账本，只补缺口、只重验变更过的源码，不会把已证实的工作推倒重来。这解决了 LLM 审计最大的浪费：每次从头读一遍代码。

**攻击类别被拆成了 12 个专项文件。** 除了常规的注入、内存安全，还专门有 `AI-AND-LLM.md`（提示注入、Agent/工具攻击、输出处理）、`SUPPLY-CHAIN-AND-RELEASE.md`（依赖、CI、签名、更新链）、`DATA-ISOLATION-AND-LIFECYCLE.md`（租户隔离、缓存、备份、删除）——这些是 2026 年真实的攻击面，不是教科书里的经典漏洞清单。Cloudflare 内部版的 Recon 甚至允许 Agent **动态发明仓库专属的攻击类别**：内置约 10 类打底，剩下的按代码库特性现场定制威胁模型。

## 三、核心设计哲学：对抗性验证与"三种裁决"

这个技能最值钱的地方不在流程，而在**三条原则**——它们直接针对 LLM 的已知缺陷设计：

**① 对抗性验证（Adversarial Validation）：检查者永远不是发现者。** "The agent that checks a finding is never the agent that found it." 每个候选漏洞都由一个全新的、无上下文的验证者 Agent 试图推翻。找不到反证才升级为 confirmed。这和人类安全审计的"四眼原则"同构，但它解决的是一个 LLM 特有的问题：**模型的推理是"惯性"的**，同一个模型基于同一段代码得出的结论，让它自己复查只会自我强化。换一个"干净大脑"，才能触发真正的证伪。扩展到系统层面，Cloudflare 更进一步：**发现阶段用一个模型，验证阶段用完全不同的模型**——让 Model B 用"另一套逻辑权重和训练数据"去审判 Model A 的输出，模型供应商在这里被当作可互换的耗材。

**② 三种裁决，把"不确定"显式建模。** 这是全篇最精巧的设计：

- **confirmed（已确认）**：有完整的源码证据链 + 有边界的可观测结果（比如 PoC 真的触发了越权）。
- **needs_validation（待验证）**：有一个**精确的、未解决的事实疑点**，而且**不给严重性评级**。
- **rejected（已否决）**：记录了被推翻的候选。

为什么 needs_validation 重要？因为它给了系统一个**诚实的中间态**。幻觉的根源是模型被迫在"是/否"之间二选一；三种裁决让模型可以合法地说"我不知道，但我知道我不知道什么"。这个中间态还承担了安全责任：没有沙箱执行环境时，工作流会主动把"执行目标代码才能确认"的线索降级为 needs_validation，而不是硬着头皮 confirmed。"Only confirm established boundary failures"——**只确认已确证的边界失效**。

**③ Severity requires impact（严重性必须由影响支撑）。** 评级公式是 `可能性 × 影响`，而不是"偏离了某个检查清单"。与此配套的是反误报原则：**"防御纵深缺口不是漏洞"**——如果 A 层已经挡住了攻击，B 层的缺失只是加固建议（hardening note），不是漏洞。这套"精确率优先"的取向，和 9 月 15 日阿里 open-code-review 的"召回率让位精确率"不谋而合——**在 AI 审计领域，假阳性是比假阴性更贵的错误**，因为它会耗尽人类研判的信任额度。

## 四、零依赖验证器：用确定性钳制非确定性

还有一层容易被忽略但极其重要的设计：仓库里两个 **零依赖的 Node.js 校验器**——`validate-coverage-ledger.cjs`（阶段 1-5 每次账本更新后跑）和 `validate-findings.cjs`（阶段 4 及每次记录替换后跑），外加配套的 schema 文件和测试用例。

为什么需要它们？因为**Agent 的输出是不可信的，但流程不能建立在"Agent 会自觉遵守格式"之上**。schema 校验确保 `findings.json` 里的每条记录都符合结构；校验器是 100% 确定性的代码，不经过任何模型。这形成了整个体系的"信任锚点"：**不确定的 Agent 产出，必须经过确定的机器校验，才能进入下一阶段。** Cloudflare 在博客里给了一个血泪教训：临时 API 错误有时会以文本形式混在 `200 OK` 的响应流里返回，对编排器来说"看起来像正常完成的任务"——必须显式分类响应文本，否则你会把空跑记录成成功。这类细节才是"生产级"和"玩具级"的分界线。

## 五、从技能到舰队：450 行提示词如何长成 128 仓库的扫描系统

security-audit-skill 不是凭空设计的，它是 Cloudflare 内部**漏洞发现流水线（VDH）的单仓库起点**——官方原话："the single-repo starting point it evolved from"。这段演进史比技能本身更有价值：

- **起点**：一个约 450 行的 `security-audit` 技能，跑单个仓库，不断调提示词直到能挖出真漏洞。当时是 7 阶段、单会话。
- **撞墙**：三个瓶颈——① **上下文耗尽**：一小时后模型开始"吃掉"自己的记忆，忘掉上午找到的 bug（解法：把状态完全外置，把 LLM 当作**无状态计算引擎**）；② **无持久化**：一次崩溃、一次限流错误就让几小时的工作归零（解法：SQLite，按 `(run_id, repo, stage)` 键控，任何阶段可恢复、可重试）；③ **单仓库盲区**：消费方仓库与依赖之间的接口 bug，单仓库扫描根本看不见。
- **跃迁**：把每个阶段"机械地"提升为独立 Agent，后面挂数据库、前面加编排器，映射几乎是一比一的。**从第一次斜杠命令到覆盖 128 个仓库的舰队扫描器，只用了六周。**

舰队版 VDH 有 8 个阶段：Recon → Hunt（每个攻击类别一个 Hunter）→ Validate（先机器校验 schema，再单 Agent 对抗性证伪）→ **Gapfill**（为覆盖薄的单元自动生成新狩猎任务）→ **Dedup**（专门用 Agent 按根因聚类重叠发现——"两个复杂逻辑缺陷是不是同一个根 bug"居然难到需要专职 Agent）→ **Trace**（沿依赖图在消费方仓库里播种新任务）→ **Feedback**（根据验证失败、浅层运行自动重写队列中的提示词）→ Report（纯脚本，无模型）。

两组数字最能说明规模：**兄弟分叉（sibling forking）**——Hunter 在主线外发现有趣路径时，fork 一个带结构种子的兄弟 Agent 去深挖，舰队范围内约占任务的 9%，且高度依赖模型（从接近 0 到五分之一）；**愿望清单（Wishlist）**——Agent 需要但不拥有的工具（某个 VM、某个构建环境）写进中央清单，等人类提供后自动重跑，**已在 128 个仓库里被写入 25,472 次**，是 Agent 与人类沟通的主渠道。文章里正好有一条实时例子："我需要一台 FreeBSD VM 来端到端确认这个 PoC。"

还有两个反直觉的经验值得记下：

- **持久化优先于并行化。** "你不该因为一个不可预见的错误丢掉五小时的运行。" 每个阶段都写 SQLite，崩溃只损失进行中的那一个任务。
- **上下文必须低于窗口的 25%。** 每个 Agent 的任务被刻意切到极小，一旦装满上下文，模型就开始幻觉。"读完全部文件"的朴素方案每次都爆。

## 六、冷思考：这个方案的天花板在哪

这套方法论很漂亮，但它的问题同样明显：

**第一，"单次运行只发现一半漏洞"是个残酷的基线。** 官方明确说：单次运行大概只能找到多次运行总和的一半，且找到的偏向简单、不微妙的 bug。这意味着**审计质量是"多跑几次"堆出来的**——成本是线性翻倍，而收益是亚线性增长。对个人开发者，跑十遍再手工 diff，性价比存疑（Cloudflare 自己的建议就是：先单仓库跑技能，只有"跑十次再 diff 太痛苦"时才上 harness）。

**第二，沙箱是刚需，不是可选项。** 技能明确要求 OS 级沙箱：禁用外部网络、白名单环境、资源限制、只允许写 scratch 路径。没有沙箱，工作流宁可把线索挂成 needs_validation 也不执行目标代码。对想在 CI 里跑审计的团队，这意味着基础设施成本先于收益到来。

**第三，"技能"的载体还在野蛮生长。** 这个技能依赖 Skills CLI（`npx skills add`）、依赖支持并行子代理的编码 Agent、依赖 AGENTS.md 式的触发机制——整个生态还没有统一标准。今天写的技能，明天换一个 Agent 平台可能就不兼容。

**第四，也是最本质的：对抗性验证消耗的是"模型多样性"。** 发现者与验证者必须真正"不同"才有意义；如果同一个供应商的模型共享训练数据与推理惯性，"换人验证"就退化成"自己查自己"。Cloudflare 用"发现/验证用不同模型"化解，但这恰恰说明：**这套体系的效果上限，绑死在模型生态的多样性上。** 模型越同质，审计越虚假。

## 结语：审计正在成为 Agent 的"元技能"

回到开头那个信号。安全审计技能之所以是 Agent 技能化的完美样本，是因为它把 Agent 的所有弱点（幻觉、上下文有限、自我证实偏差、不可复现）都变成了显式的工程问题，并且给出了可迁移的解法：**阶段隔离控制上下文、账本/数据库提供持久化、机器校验锚定信任、对抗性验证对抗幻觉、结构化裁决容纳不确定性。** 这些解法没有一条依赖某个具体模型——用 Cloudflare 的话说，**"harness 才是会留下来的部分"（The harness is the bit that lasts）。**

当安全审计（一个人类专家需要十年经验、且犯错的代价极高的工作）都能被拆成可复现、可验证、可增量累加的六阶段流水线时，其他专业领域——法务审查、合规检查、架构评审——被"技能化"只是时间问题。Agent 竞争的下半场，拼的不是谁会写提示词，而是**谁先把专业判断变成工程**。

---

*参考：Cloudflare security-audit-skill 仓库（MIT License，7,135★/420 forks，2026-09-17）；Cloudflare 官方博客《Build your own vulnerability harness》；Project Glasswing 系列文章；GitHub Trending 2026-09-17。*