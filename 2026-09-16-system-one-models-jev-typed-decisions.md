# 当模型放弃"说话"：TypeSafe System One Models 与 Jev 深度拆解——70ms 的决策、免费的输出与"不可能"的幻觉

> 9 月 15 日晚，一个在 stealth 里憋了两年的新实验室 TypeSafe AI 发布了一类全新模型：**System One Models**。创始人是前 OpenAI 研究员 Diogo Almeida——他参与构建的正是让 LLM"学会听话"的那套方法，也就是 ChatGPT 背后的指令跟随研究。他们的第一个模型 Jev 放弃了文本生成，只输出**类型安全的结构化决策**，附带校准过的概率。代价是"不能聊天"，换来的是：70-500ms 端到端延迟、输出 token 免费、以及**数学上不可能产生类型错误**。这篇文章拆解它凭什么做到、牺牲了什么，以及为什么这可能比又一个"聊天模型"重要得多。

一句话概括 Jev 的定位：**前沿智能版函数调用——非结构化状态进，带类型、带概率的决策出。**

在 Hacker News 上，这篇文章以 616 分、205 条评论成为当日第一。对一个没有明星团队背书、没有巨额融资新闻的实验室来说，这个热度说明一件事：**"模型和软件之间到底怎么对接"，已经是整个行业公认的未解难题。**

---

## 一、问题：为什么模型在聊天上超人类，自动化却迟迟不来？

Diogo Almeida 在发布文里写了这么一句："Models have been superhuman at chat for years, so where is all the automation?"（模型在聊天上超人好几年了，自动化呢？）

这句话精准戳中了 2026 年 AI 产业的荒诞现状：**模型越强，Agent 越火，但真正"跑在代码里、不需要人盯"的自动化，还是集中在少数几个赛道。** 为什么？

答案藏在 LLM 的默认输出形态里：**字符串**。

字符串是给人类看的。它灵活、万能、可以表达一切——但正因为可以表达一切，它也需要被"解析 + 验证"才能进代码。更糟的是，字符串天然携带三种毒性：

1. **幻觉**：模型可以自由地编造任何内容，包括你让它填的 JSON 字段。
2. **类型错误**：说好的整数返回了个字符串，说好的 enum 返回了个全新值——解析层直接崩。
3. **过度自信**：模型不会告诉你"我这次只有 60% 把握"。它 95% 的时候做对，但从不告诉你哪 5% 是错的——而这恰恰是自动化最需要知道的信息。

TypeSafe 的判断是：**这三种毒性不是 bug，是"文本生成"这个架构约束的必然产物。** 你让一个以"生成人类喜欢读的文本"为优化目标的模型去给软件做决策，接口从一开始就错位了。

这个洞察不新鲜——业界已经用 JSON mode、function calling、structured output、MCP 等一堆"约束层"来给 LLM 套笼子。TypeSafe 的激进之处在于：**不修 LLM，另起炉灶，训练一个原生就不输出文本的模型。**

## 二、System One Models：一次"减压"式的再设计

卡尼曼在《思考，快与慢》里把人类认知分成 System 1（快速、直觉、自动）和 System 2（缓慢、审慎、推理）。TypeSafe 直接借用了这个框架：

- **现有 LLM = System 2 型**：慢、贵、但能处理开放任务，适合人机交互。
- **Jev = System 1 型**：快、便宜、结构化，适合"嵌入软件做决策"。

但注意一个反直觉的点：卡尼曼的 System 1 是"容易出错"的。TypeSafe 声称 System One Models 恰恰相反——**因为放弃了自由度，它反而比通用 LLM 更可靠。** 这跟人类直觉相反，但逻辑上是自洽的：可靠性来自约束，不来自智能。

### 2.1 核心设计差异

| 维度 | 现有 LLM | System One + Jev |
|------|---------|-----------------|
| 训练方法 | RLHF / RLVR（对齐人类偏好或可验证奖励） | RLCD（面向校准决策的强化学习） |
| 优化目标 | 人类喜欢的回答 / 可程序验证的输出 | **校准的决策**（带诚实概率的回答） |
| 输入 | 非结构化文本，侧重对话序列 | 非结构化文本，侧重**结构化程序状态** |
| 输出 | 字符串（任意内容） | **类型安全的结构化值**（预先定义 schema） |
| 采样 | 顺序式（逐 token 生成） | **并行式**（一次查询产出全部输出） |
| 端到端延迟 | 3-329 秒（前沿模型） | **70-500ms**（快 40-200 倍） |
| 成本 | 输入 $0.20-10/MTok，输出约 5 倍于输入 | 输入 **$0.042/MTok**，输出**免费** |
| 置信度 | 过度自信、不稳定 | 每次输出都带**校准过的概率** |
| 幻觉 | 存在 | **结构上不可能**（schema 约束） |

三个最值得深挖的点：

**① 放弃字符串 = 结构性消灭幻觉。** Jev 的输出空间在推理前就被 schema 定义了——类型、取值范围、枚举项全部预先固定。模型只能在合法空间里给每个候选分配概率。这就像把模型从"自由写作"变成"做题"，且每道题都是选择题。TypeSafe 甚至声称"类型错误率 0% 是数学上必然的"——输出空间根本不存在非法值，想错都没地方错。注意这里偷换了一点概念：**"类型错误"归零 ≠ "决策正确"归零**，模型依然可能给出错误的决策，但它会诚实地用低概率告诉你。这恰恰是自动化最需要的契约。

**② 并行采样 = 延迟的两个数量级碾压。** 传统 LLM 是自回归的：逐 token 生成，后一个依赖前一个，推理延迟随输出长度线性增长。Jev 是"一次查询，全部输出"——所有问题的概率分布并行算出。这意味着延迟与输出规模基本解耦。70-500ms 这个量级意味着什么？**它可以进实时链路了**：支付风控、路由、广告竞价、游戏 NPC、实时监控告警分级……这些场景此前因为"LLM 要等 3 秒"而全部出局。

**③ 输出免费 = 成本结构反转。** 输出 token 免费听起来像营销噱头，但逻辑上是自洽的：没有自回归解码，就没有"生成成本随输出长度爆炸"的问题。输入 $42/十亿 token，比最便宜的前沿模型还便宜一个数量级。TypeSafe 自己承认"无法证明这不是补贴价"，但他们的预期是价格会继续下降而不是上涨——因为并行采样在硬件利用效率上有结构性优势。

### 2.2 RLCD：训练一个"诚实"的模型

训练方法 RLCD（Reinforcement Learning for Calibrated Decisions）是第三个关键拼图。RLHF 优化"人类觉得好"，RLVR 优化"答案对不对"，而 RLCD 优化的是**校准度（calibration）**：模型给出的概率要在统计意义上等于真实正确率——说 90% 的时候，就该真有 90% 的把握。

校准为什么是自动化的命门？TypeSafe 说得一针见血：**"如果一个模型 95% 的时候能做对，但从不告诉你它处在 5% 的错误区间里，那它就无法被自动化。"** 因为自动化系统需要知道什么时候该升级处理（转人工、走兜底逻辑、降低风险等级）。一个"永远自信"的模型，等于没有置信度信息；一个校准良好的模型，本身就是一套风控信号。

为了验证，他们发布了一套全新的评估方法——**Workflow Evals**：不测"分类对不对"，而是把模型放进真实代码工作流里，用最强模型（GPT-6 Astra 和 Fable 5.1）预测的均值作为参考概率，看模型在"延迟-质量"帕累托前沿上的位置。结果 Jev 领先了近两个数量级（主页上"快 193.6 倍、便宜 444.6 倍"的出处）。他们甚至承认这套基准偏向 OpenAI/Anthropic 的模型，且低估了 DeepSeek——这种坦诚在模型发布文里相当罕见。

## 三、它到底能干什么：从"智能 if 语句"到守卫层

放弃文本生成，意味着 Jev 不是拿来替代 ChatGPT 的，它是**给软件用的**。官方列了几个典型场景：

1. **智能 if 语句（AI-powered workflows）**：分类、路由、打分、抽取、分支——手写规则太脆、通用 LLM 太贵太慢的地方，塞一个 Jev 调用。外层代码约束它的自由度，反过来它给代码注入"模糊决策"能力。这是最性感的定位：**AI 不是 Agent，是语言构造（language construct）。**
2. **Map-reduce 大数据**：把 PB 级数据变成特征和洞察。传统 LLM 在这类任务上成本不可承受，Jev 的成本结构让它第一次在经济上成立。
3. **实时应用**：100ms 级别的延迟让 AI 决策可以进入 UX 关键路径。
4. **验证一切**：给其他 LLM 的输出打分、判定、守卫（guardrail）、检测越狱——**用决策模型去管生成模型**。这个场景特别有意思：你不需要另一个巨型 LLM 来当裁判，你需要的是一个便宜、快速、校准良好的裁判。

公开演示里最出圈的是用 Jev 打 Doom：每秒 10 次查询，约 $7/小时——按 LLM 的定价这得烧掉几千刀，按 Jev 的定价这只是个"玩具开销"。Wikiracing 则展示了高基数选择的优势：每一步从数百到数千个链接里选，Jev 支持最高 255 个候选的并行打分（再高的走两阶段：先独立打分再显式选择）。

## 四、冷静下来：三个必须打的问号

TypeSafe 的发布文是我见过的"自我免责声明"最完备的之一，但正因如此，更需要拆开看：

**① "不能幻觉"到底承诺了什么？** 严格来说，他们承诺的是"类型安全"：输出必然合法、必然落在 schema 内。这是数学保证，没问题。但**"决策正确性"依然是概率性的**——校准良好只意味着"低概率回答更可能错"，不意味着"低概率回答不出现"。自动化系统真正要面对的风险是：一个高置信度的错误决策。校准能压缩这种风险，但不能消灭它。把它宣传为"can't hallucinate"，在工程语义上成立，在业务语义上仍需要兜底设计。

**② 基准的公平性边界。** Workflow evals 用"最强模型的均值"当参考概率，这套方法论聪明（避免了对 ground truth 的过拟合），但有系统性偏差：参考本身就是 OpenAI/Anthropic 模型的意见，Jev 在与它们"对齐"的任务上天然占优。另外对比的 LLM 用的是结构化输出 wrapper——TypeSafe 承认这是"让 LLM 输出决策的最准方式，但更慢更贵"，这等于用 LLM 的劣势姿势做对比基线。真实世界的 LLM 集成不会都这么用。

**③ 定价与延迟的可持续性。** 输出免费 + $0.042/MTok，要么是结构性成本优势（并行采样 + 无 KV cache 压力），要么是补贴换市场。发布文自己承认"无法证明不是补贴价"。**在 2026 年这个"价格战 + 算力白刃战"的当口，一个新实验室的低价策略需要更长的观察窗口。** 延迟数据也标注了"从西海岸笔记本调用"——地理距离对 70ms 级延迟的影响是决定性的，国内开发者体验到的延迟会显著高于发布数据。

## 五、独到见解：这是"机器可读"的终局之战

把 Jev 放进 2026 年的时间线看，它实际上是整个 AI 行业"接口战争"的一个极端样本：

- OpenAI 的答案是 **function calling / Responses API**——让 LLM 学着输出结构化。
- MCP 的答案是**协议约束**——在模型外面套一层工具契约。
- TypeSafe 的答案是**模型原生结构化**——从架构上消灭"生成自由文本再解析"这个中间层。

这三条路线在抢同一个东西：**"模型 ⇄ 软件"之间的那个接口，到底该长什么样？** 前两条路线的共同痛点是：约束层是后加的，模型的核心优化目标（文本生成）没变，于是"幻觉 + 解析成本 + 置信度缺失"永远在漏。TypeSafe 的赌注是：**干脆把约束写进模型本体。**

这个赌注的代价也是结构性的：**Jev 永远写不了诗、编不了故事、做不了开放性对话。** 它是一把只有刀刃的刀。但反过来想——自动化系统需要的从来不是诗人，是**在延迟预算内、成本预算内、给出带概率的、类型正确的决策**的东西。整个 2026 年 AI 产业都在往"Agent 化"狂奔，但 Agent 的可靠性危机（幻觉、越狱、过度自信）本质上都是同一个问题：让一个为"取悦人类"训练的模型去承担"机器决策"。TypeSafe 选择从训练目标开始就为机器设计——这比任何 harness 工程都更釜底抽薪。

另一个值得玩味的点是命名。Jev 取自威廉·斯坦利·杰文斯（William Stanley Jevons），19 世纪的经济学家，他发现了**杰文斯悖论**：蒸汽机效率提升后，煤炭消耗不降反升——因为更便宜的动力解锁了更多用途。TypeSafe 的团队明说了：**智能的成本每下降一个数量级，就会解锁一个数量级的新用例。** 如果 Jev 真的把"每个决策"的成本打到可以忽略不计，那么"到处都是 AI 决策"就不是愿景，而是必然结果——正如 70ms 延迟让 AI 第一次能出现在游戏循环里。

最后说回那 616 个 HN 赞。社区的热度说明大家看懂了这件事的分量：**2026 年的 AI 竞争已经从"谁的模型更聪明"转移到"谁的模型更能被软件信任"。** Jev 是第一个认真回答"信任"这个问题的产品化尝试。它不一定赢——但"决策模型"这个品类，从今天起正式存在了。

---

**参考链接：**
- [Introducing System One Models & Jev — TypeSafe AI](https://typesafe.ai/blog/introducing-system-one-models-and-jev)
- [HN 讨论帖（616 分）](https://news.ycombinator.com/item?id=49717558)
- [Workflow Evals 数据](https://evals.typesafe.ai/)
- [system-one-adapter-python（LLM 结构化输出适配器）](https://github.com/typesafe-ai/system-one-adapter-python)