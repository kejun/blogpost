# 当 Agent 能装进口袋：LFM2.5-2.6B 与"本地 Agent 无处不在"的范式转移

> 2026-08-05 · AI技术

> 一个 2.6B 参数的模型，在 Apple M5 Max 上跑出 220 tok/s、在 Ryzen CPU 上跑出 113 tok/s、占用不到 2.5GB 内存，却能调用工具、完成多步任务、在指令遵循和工具使用上击败比自己大 4 倍的开源模型——这就是 Liquid AI 在 2026 年 8 月 4 日发布的 LFM2.5-2.6B。它背后真正值得研究的，不是那组惊艳的数字，而是一套"把 Agent 框架当成黑盒来训练模型"的全新工程范式。

---

## 一、引言：Agent 的"端侧时刻"

过去两年，Agent 的叙事几乎被"更大、更聪明的前沿模型"垄断。OpenAI、Anthropic、Google 把 Agent 能力绑定在云端 API 上，开发者通过一个网络请求调用推理，数据的每一次往返都要经过数据中心。这种中心化范式有一个难以回避的代价：**只要 Agent 的应用场景触及隐私、延迟、成本和离线，云端模型就力不从心**。

2026 年夏天，这个天平开始倾斜。先是 7 月 30 日 Liquid AI 用 LFM2.5-Encoder 证明"编码器可以跑在 CPU 上"，昨天（8 月 3 日）我们刚分析过它在 Agent 推理经济学上的意义；今天，Liquid AI 又放出了真正的主角——**LFM2.5-2.6B**，一个"为端侧设备而生的 Agent 模型"。

这不是一个简单的"小模型"发布。它代表着一种范式转移：**Agent 能力正在从数据中心的 GPU 上，下沉到手机、笔记本和浏览器里**。而这套能力的支撑，不是靠单纯堆数据，而是靠一套全新的强化学习训练管线——在真实的 Agent 框架（harness）内部训练模型。

---

## 二、LFM2.5-2.6B：规格与定位

先看硬指标：

| 属性 | LFM2.5-2.6B |
|------|-------------|
| 参数量 | 2.6B |
| 预训练数据 | ~34T tokens |
| 上下文窗口 | 128K（mid-training 扩展） |
| 峰值推理速度（CPU） | 220 tok/s（M5 Max）/ 113 tok/s（Ryzen AI Max+ 395） |
| 内存占用 | < 2.5GB |
| 手机端速度 | ~30 tok/s |
| 单卡 H100 并发吞吐 | ~15K tok/s（约 1.3B tokens/天） |
| 开箱即用生态 | llama.cpp、MLX、vLLM、SGLang、ONNX |

它的定位非常清晰：**不是要取代前沿模型，而是填补"不需要 700B 参数也能当一个合格 Agent"的空白**。Liquid AI 的官方口径是"AI 能运行在任何地方"（AI that runs anywhere），LFM2.5-2.6B 则是这个愿景的第一次完整落地。

值得注意的细节是：2.6B 的模型把上下文窗口做到了 128K。在 Agent 场景里，长上下文往往意味着"能记住更长的任务历史和工具调用序列"——这是端侧 Agent 能否真正干活的关键，而不仅仅是"能聊几句"。

---

## 三、四阶段后训练：如何"造"出一个端侧 Agent

LFM2.5-2.6B 的预训练用了约 34T tokens，但真正让它"成为 Agent"的，是后训练阶段的四步流水线。这四步揭示了一个越来越清晰的行业共识：**模型的 Agent 能力不是天生自带的，而是被"训练"出来的，而且训练方式正在从"教它回答"转向"教它干活"**。

### 1. 两轮 SFT：往 Agent 数据里灌

第一阶段是两轮监督微调（SFT），加权集中在 Agent 相关数据上：工具调用、网页搜索、以及**框架轨迹（harness trajectories）**——即真实 Agent 框架运行过程中产生的完整交互记录。

这里的"框架轨迹"是关键词。它意味着训练数据不再只是"问题和答案"的配对，而是"问题 → 思考 → 调用工具 → 观察结果 → 继续行动"的完整状态序列。模型学的不再是"怎么回答"，而是"怎么操作一个环境"。

### 2. 教师特化：术业有专攻

第二阶段为每个领域训练一个"专家教师"模型：数学一个、代码一个、工具使用一个。这些教师模型在各自领域达到更高水平，作为后续蒸馏的"上师"。

### 3. MOPD：多领域 on-policy 蒸馏

第三阶段是**多领域 on-policy 蒸馏（Multi-Domain On-Policy Distillation, MOPD）**——把多个领域专家教师的能力蒸馏进一个学生模型。这一步解决了端侧模型的一个核心矛盾：**模型要小（2.6B），但能力要全（数学、代码、工具、推理样样都要）**。单靠一个模型自己学，很难面面俱到；靠多个特化教师"喂"出来，则能在小参数下尽量逼近"全能"。

### 4. Agentic RL：在真实框架里练"干活"

第四阶段是整条管线里最有意思、也最"反常识"的部分——**Agentic Reinforcement Learning（Agentic RL）**：在真实的 Agent 框架（如 OpenClaw、Hermes Agent）里跑多轮强化学习，让模型学会在不同工具、不同系统提示、不同多轮任务环境里协作。

这和我们熟悉的 RLHF 有本质区别。RLHF 优化的是"回答是否符合人类偏好"；**Agentic RL 优化的是"在真实工具环境中能否完成任务"**——奖励信号来自任务是否真正完成，而不是人类对文本的喜好。这是从"对话模型"到"执行体"的关键一跃。

---

## 四、亮点工程：把 Agent 框架当"黑盒"来训练

如果说四阶段后训练展示了"做什么"，那么 Agentic RL 的工程实现则展示了"怎么做"——而这里藏着整篇文章最值得深挖的独到见解。

Liquid AI 把 Agentic RL 管线拆成了四个清晰组件：

```
┌─────────────────────────────────────────────────────────┐
│                    Agentic RL 管线                        │
├─────────────────────────────────────────────────────────┤
│  Training Engine   → 优化模型参数                         │
│  Rollout Engine    → 用最新策略生成动作                   │
│  RL Framework      → 调度 rollout、收集轨迹与奖励、更新    │
│  Sandbox Service   → 执行动作（隔离环境）                 │
└─────────────────────────────────────────────────────────┘
```

动作在 **Sandbox Service**（沙箱服务）里执行，其中 **Blackbox Harness**（黑盒框架）承载真正的 Agent（例如 OpenClaw 或 Hermes Agent），并协调与任务环境的交互。

关键在于 **Harness Proxy**（框架代理）：它让我们**无需修改任何框架代码**，就能把 OpenClaw、Hermes 这些成熟的 Agent 框架当成"黑盒"来使用，同时"透明地捕获重建 RL 训练样本所需的 token 级轨迹"。

这个设计的精妙之处在于——**它把"训练"和"框架"彻底解耦了**。

传统做法是：为了给 Agent 模型做强化学习，你得为一个特定框架写专门的训练适配层，框架升级了、换框架了，训练代码就得重写。而 Harness Proxy 的思路是：**框架本身是一台"黑盒机器"，我只需要从它进出的 token 流里提取训练信号，就能让模型学会在这台机器里干活**——不管这台机器是 OpenClaw、Hermes 还是未来的任何新框架。

这意味着两件重要的事：

1. **训练数据生态被打开了**：任何能跑 Agent 的框架，都能成为强化学习的"健身房"，不需要为每个框架重复造轮子。
2. **模型与框架的适配性成为训练目标**：模型不是"在某个框架里恰好能用"，而是"在通用框架里可靠地干活"——这正是端侧 Agent 落地最需要的属性。

从工程角度看，这几乎是端侧 Agent 训练基础设施的一次"标准化时刻"：**把 Agent 框架变成可插拔的训练环境**。它和当年 LLM 训练里"把数据加载器标准化"的意义类似——一旦框架可以被任意接入，整个训练生态的扩展速度会指数级提升。

---

## 五、基准数据：小模型如何"以小博大"

Liquid AI 给出了相当完整的基准对比，对照组是体量 2-4 倍的开源模型：

| 基准 | LFM2.5-2.6B (2.6B) | gemma-4-E2B (5.1B) | gemma-4-E4B (8B) | Qwen3.5-4B (4.7B) | Qwen3.5-9B (9.7B) |
|------|:---:|:---:|:---:|:---:|:---:|
| AIME25 | **51.87** | 26.33 | 34.27 | 49.33 | 56.07 |
| LiveCodeBenchv6 | 59.41 | 54.92 | 63.77 | 60.85 | **69.86** |
| IFBench | **59.17** | 34.08 | 39.24 | 48.40 | 56.47 |
| Multi-IF | **80.07** | 69.44 | 77.35 | 55.67 | 62.55 |
| IFStruct | **85.49** | 64.85 | 76.65 | 36.25 | 78.50 |
| BFCLv4 | 56.88 | 36.98 | 46.39 | 50.56 | **60.13** |
| ToolSandbox | **77.83** | 52.40 | 65.00 | 75.55 | 76.44 |
| τ³-Bench Banking | **5.67** | 3.35 | 4.12 | 5.45 | 5.15 |
| Claw-Eval avg (EN) | 62.85 | 53.14 | 58.02 | 62.28 | **66.53** |
| PinchBench | 68.22 | 44.24 | 55.09 | **71.26** | 71.45 |
| BrowseComp+ (OpenClaw) | 26.89 | 8.31 | 15.90 | 24.46 | **27.23** |

读这张表，能得出几个有价值的结论：

**第一，指令遵循（IFBench、Multi-IF、IFStruct）是它的绝对强项，全面碾压包括 9.7B Qwen 在内的所有对手。** 这背后正是"Agentic RL + 框架轨迹训练"的直接成果——在 Agent 场景里，模型最重要的能力就是"理解并忠实执行指令"，而这是它被专门训练的核心。

**第二，工具使用（ToolSandbox、τ³-Bench、BFCLv4）同样领先，仅次于 9.7B 的 Qwen。** 一个 2.6B 的模型在工具调用上能打赢 5-8B 的 Gemma 全家，说明"工具使用"这门手艺在端侧小模型上是可以被专项训练出来的，而不是大模型的专利。

**第三，数学（AIME25）和通用 Agent 能力（Claw-Eval、BrowseComp+）与 9.7B Qwen 基本打平甚至小幅领先。** 这进一步印证了"小模型在 Agent 任务上可以逼近大模型"的判断。

**第四，也是唯一诚实的短板——代码（LiveCodeBenchv6）。** 这是大模型唯一保持明确领先的领域，Liquid AI 自己也承认："编码是更大模型保持明确领先的地方，需要更大模型时请去拿更大的。"**这个诚实的标注非常关键**——它划清了端侧 Agent 的适用边界：**适合做"执行型 Agent"（指令遵循、工具调用、多步任务），不适合做"创作型 Agent"（复杂代码生成）**。

---

## 六、推理经济学：端侧 Agent 的成本革命

如果说基准数据证明的是"能不能打"，那么推理性能证明的是"划不划算"。这里的数据对开发者尤其有冲击力。

**CPU 端：** 220 tok/s（M5 Max）、113 tok/s（Ryzen AI Max+ 395），内存占用不到 2.5GB。这个速度意味着什么？一个 Agent 的"思考"节奏是每秒几十到几百 token，220 tok/s 足以让模型流畅地边思考边调用工具，几乎感觉不到"在等模型"。而在手机上，30 tok/s 也足够支撑一个"能干活但略慢"的 Agent。

**GPU 端：** 高并发下接近 15K 输出 token/s，**单张 H100 一天能产出约 13 亿 tokens**。这个数字放在"推理经济学"的语境下极具冲击力——它把端侧模型的规模化成本拉到近乎荒谬的低位。

结合我们昨天分析的 LFM2.5-Encoder，可以拼出一张更完整的图：**Encoder 负责把输入高效地"理解"成紧凑表示，Decoder（LFM2.5-2.6B）负责执行 Agent 动作，两者合起来让"整个 Agent 栈"都可以跑在本地 CPU 甚至浏览器里**。昨天那篇文章关注的还是"编码器的 CPU 推理经济学"，今天 LFM2.5-2.6B 则把"解码器 + 完整 Agent"也塞进了同样的经济模型里。

**WebGPU 浏览器演示**更是把"部署无处不在"推到了极致——Liquid AI 提供了一个纯浏览器 demo，一个研究 Agent 直接在浏览器里跑，零安装、零云端依赖。这是"本地 Agent 无处不在"最直观的注脚：**未来你的浏览器就是一个能跑完整 Agent 的运行时**。

---

## 七、局限与诚实的边界

任何范式转移都要面对现实的检验。LFM2.5-2.6B 的发布令人兴奋，但有几个边界必须划清楚：

**容量天花板是物理的。** 2.6B 的参数意味着它的"知识量"和"推理深度"有上限。它能在指令遵循和工具使用上以小博大，但面对需要深度推理或海量知识的任务，依然会输给更大的模型。它定位的是"高频、低延迟、隐私敏感"的执行型场景，而不是取代前沿模型。

**代码是其明确短板。** LiveCodeBenchv6 上它排倒数第二，被所有 5B+ 模型超越。如果应用场景是"让 Agent 写复杂代码"，LFM2.5-2.6B 不是合适的选择。

**生态成熟度仍需观察。** 虽然开箱即用支持 llama.cpp、MLX、vLLM、SGLang、ONNX，但"端侧 Agent 应用"的完整生态（工具库、框架集成、调试工具）还在早期。30 tok/s 的手机端速度也意味着复杂的多步任务在手机上会有可感知的延迟。

**Agentic RL 的泛化性存疑。** 在 OpenClaw、Hermes 等框架里训练出来的模型，能否无缝迁移到完全不同的框架？Harness Proxy 解决了"训练时接入"的问题，但"部署时换框架"的可靠性还没有被大规模验证。

---

## 八、结论：Agent 的"无处不在"不再是一句口号

LFM2.5-2.6B 的意义，不在于它是"又一个小的开源模型"，而在于它第一次把**"运行在真实 Agent 框架里的强化学习训练"**和**"端侧可部署"**这两件事在工程上完整地打通了。

它背后真正的范式转移可以概括为三点：

1. **训练目标变了**：从"教模型回答"到"在真实工具环境里教模型干活"（Agentic RL）。
2. **训练方法工程化了**：通过 Harness Proxy 把 Agent 框架变成可插拔的黑盒训练环境，框架与模型彻底解耦。
3. **部署边界消失了**：2.5GB 内存 + CPU 可跑 + 浏览器可跑，"Agent 无处不在"从口号变成了可交付的工程能力。

对开发者而言，LFM2.5-2.6B 意味着一个新的选择维度：**当你的 Agent 应用需要隐私、低延迟、低成本或离线能力时，第一次有了一个"够用"的端侧选项**。它不会取代云端前沿模型，但它会重新定义"哪些 Agent 任务根本不需要上云"。

当 Agent 能装进口袋，关于"Agent 该跑在哪"的讨论，才刚刚开始。

---

*参考来源：Liquid AI, "LFM2.5-2.6B: Deploy Agents Everywhere", Liquid AI Blog, Aug 2026；Hugging Face 官方博客同日发布。*