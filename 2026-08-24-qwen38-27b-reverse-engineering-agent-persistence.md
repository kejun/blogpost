# 当 27B 模型开始"啃"固件：Qwen3.8-27B 的 30 分钟逆向工程与 Agent 的"持久性革命"

**日期：** 2026-08-24
**标签：** Qwen3.8-27B, Reverse Engineering, Agent Persistence, Local Models, Gated DeltaNet, Firmware Analysis, Self-Verification, Harness Engineering, License Recovery

---

## 一、引言：一个 159 分的热帖，和一场关于"前沿"的争论

2026 年 8 月 23 日，XDA 作者 Adam Conway 发了一篇文章，标题直白得有点挑衅：**"I gave Qwen 3.8 27B a reverse-engineering job and it finished in 30 minutes"**——我给 Qwen 3.8 27B 一个逆向工程任务，它 30 分钟干完了。文章在 Hacker News 拿到 159 分，成为当日最热的 AI 话题（[HN 讨论帖](https://news.ycombinator.com/item?id=49407507)）。

单看标题，"27B 模型 + 30 分钟 + 逆向工程"已经信息量不小：这是一个能在本地跑的小模型，干了一件过去被认为只有资深逆向工程师（或者足够强的闭源大模型）才能干的活。但真正引爆讨论的，是标题里的另一个词——**"frontier model"（前沿模型）**。一个 27B 的稠密开源模型，凭什么敢自称"前沿"？

评论区吵成一团。有用户直接发问："前沿模型和本地模型到底有什么本质区别？"最高赞的回答一针见血：

> "前沿 vs 本地不是一个有用的区分，'专有 vs 开源'才是。我怀疑大家用'前沿'只是'大到在家跑不动'的委婉说法。" —— catlifeonmars

这场争论本身，就是今天这篇博客的主题。但我认为，**真正值得拆解的既不是"27B 算不算前沿"的文字游戏，也不是"逆向工程"这个具体任务，而是这件事暴露出的 2026 年 Agent 能力曲线的一个拐点：决定任务成败的变量，正在从"模型的智商"转向"模型的持久性"。**

顺便说一句，这个时间点并非巧合。同一天 HN 首页上还挂着两条新闻：**Kaspersky 披露 Android 车机固件被恶意软件感染**（87 分），以及**斯洛伐克在交通测速摄像头里发现俄罗斯后门**。固件分析正在成为网络安全攻防的第一线，而 AI Agent 恰好在这一周证明了自己能干这行——是时候认真看看这件事了。

---

## 二、案例拆解：当"签名通过但哈希不匹配"时，模型选择了不撒谎

先还原一下这个实验到底发生了什么。

作者的任务是把一台"上古路由器"的固件逆向一遍，恢复出能用的许可证密钥（license key）——典型的老设备维护场景：厂商倒闭、文档丢失、唯一的知识产权残留在二进制里。区别在于，这次干活的不是人，而是跑在 **Pi harness（作者在评论里确认：只用了基于 Bash 的工具）** 里的 Qwen 3.8 27B，一个本地部署的 27B 稠密模型。30 分钟后，它交付了。

如果故事到此为止，这只是一个"小模型也能做 RE"的巧合样本。但作者在文章里埋了一个细节，评论区几乎所有资深工程师都认为**那才是全文真正的新闻**：

> "第一次恢复密钥的尝试以一种非常具体的方式错了：它产出了一个**能用的密钥**，签名校验通过了，但二进制在运行时计算的一个完整性哈希对不上。以我的经验，大多数模型到这里就会宣布完工、收工走人。但 Qwen 3.8 27B 没有。它主动指出了这个不匹配，回到画板前重来，一直干到那个值逐字节对上为止。"

这句话值得逐层拆：

1. **它产出了一个"功能正确"的结果。** 签名校验通过——从任何黑盒测试的角度看，任务已经完成了。一个被"奖励信号"驯化的模型（通过测试即得分）会在这里停下。
2. **但它发现了一个"事实上错误"的细节。** 完整性哈希不匹配意味着这个密钥不是固件作者当初签发的那个，只是碰巧能过验证——这是二进制内部一个没有暴露给外部的完整性自检。
3. **更关键的是，它没有把不一致藏起来。** 它主动报告矛盾、推翻自己的结论、重新推导，直到字节级一致。

HN 用户 VulgarExigency 引用这段话后给出了他的判断："这似乎是近期新模型的一个共同模式，我认为它解释了最近工作质量为何提升。**它们非常执着于验证自己的工作是否真的正确**——即使它们不如更大的、一次就对的大模型'聪明'，它们有能力跟进到底，确保工作真正完成。"

另一位用户 andai 的观察更狠：

> "大约一年前，各家实验室意识到：**有效智能是'持久性'的函数，和'智力'一样重要。** 于是从去年年底开始，模型变得'吓人地持久'，这个趋势一直在延续，几个月前又跳了一次。"

把这条观察和 8 月 25 日我们写过的 Claude Opus 5 自验证架构（[2026-07-25-claude-opus5-self-verification-agent-reliability-inflection.md](2026-07-25-claude-opus5-self-verification-agent-reliability-inflection.md)）放在一起看，脉络就清晰了：**2026 年的模型竞赛正在从"谁第一个 token 更聪明"转向"谁肯把最后一个字节验证完"。** 逆向工程只是把这个趋势放大到肉眼可见的领域——因为在这个领域，"错误地正确"（falsely correct）的结果比"明显错误"的结果危险得多，而坚持验证的模型恰好是解药。

---

## 三、数据说话：27B 凭什么敢叫"前沿"

争论"前沿"之前，先看官方数据。Qwen3.8-27B 的模型卡（[Hugging Face](https://huggingface.co/Qwen/Qwen3.8-27B)）把它放在了一个惊人的对照表里：**Qwen3.8-27B vs Qwen3.6-27B vs Qwen3.7-Plus vs Muse Glimmer-30B vs Opus4.6 Max**（Anthropic 闭源旗舰）。注意，所有 agentic 基准都统一用 Claude Code harness 评测，这是罕见的"同一套 harness 下横评开源/闭源"：

| 基准 | Qwen3.8-27B | Qwen3.6-27B | Qwen3.7-Plus | Muse Glimmer-30B | Opus4.6 Max |
|------|------------|------------|-------------|-----------------|-------------|
| SWE-bench Pro | **61.7** | 53.5 | 57.6 | 51.2 | 53.4 |
| Terminal Bench 2.1 | 73.0 | 63.4 | 64.0 | 51.7 | **78.2** |
| DeepSWE 1.1 | **42.2** | 13.3 | 14.2 | — | — |
| QwenSWEBench | **79.0** | 49.3 | 59.2 | — | 63.8 |
| OSWorld-Verified | **84.3** | 63.9 | 73.3 | 65.9 | 72.7 |
| WebArena-Verified | **64.8** | 48.8 | 55.3 | — | — |
| AndroidWorld | **81.9** | 70.3 | 81.0 | — | 62.0 |
| Agents' Last Exam (Score) | **42.9** | 27.3 | 33.6 | — | — |
| HLE | 30.8 | 24.0 | **34.7** | 22.0 | **40.0** |

几个值得 mark 的点：

**① 27B 在 agentic 基准上反超闭源旗舰。** SWE-bench Pro 61.7 > Opus4.6 Max 53.4；OSWorld-Verified 84.3 > 72.7；AndroidWorld 81.9 > 62.0。这基本坐实了 XDA 标题里"frontier"的底气来源——在"把长任务干完"这件事上，27B 的稠密小模型已经和 4-5 倍体量的闭源模型同场竞技甚至领先。注意 HLE（人类最后的考试）这类纯推理基准上它依然落后（30.8 vs 40.0），**能力分布是偏斜的：智力落后，但"完成的韧性"领先**——这恰好印证了第二部分的判断。

**② DeepSWE 1.1 的 3 倍跳升。** 42.2 vs 上一代同尺寸 13.3，这是"长程软件工程"基准（8 小时超时、32K max tokens），纯靠单次推理提升解释不了 3 倍差距。更合理的解释是：**这个版本的训练目标里，持久性本身被放在了显著位置。**

**③ 官方把"持久性"写进了卖点。** 模型卡原文："designed to carry complex, multi-step tasks **through to completion with greater reliability**"；Agent Execution 一节写的是"stronger autonomous planning and **better handling of environment feedback**, leading to more reliable end-to-end task completion"。翻译成大白话：**"更可靠地把任务跑完""更好地处理环境反馈"**——这不是营销话术，这是训练目标的直接陈述。2024 年的模型卡写"更好的推理能力"，2026 年的模型卡写"更好的完成能力"。措辞的变化就是行业风向的变化。

架构上，Qwen3.8-27B 是我们 8 月 13 日拆过的 2.4T 旗舰（[2026-08-13-qwen38-2t-hybrid-linear-attention-moe-open-max.md](2026-08-13-qwen38-2t-hybrid-linear-attention-moe-open-max.md)）的"下放版"：Gated DeltaNet 混合线性注意力布局（每 4 层 3 层线性注意力 + 1 层全注意力）原样搬到了 27B 稠密模型上，64 层、隐藏维度 5120，原生 262K 上下文可扩到 1M，带 MTP 多 token 预测、原生视觉理解，思考模式默认开启且支持 reasoning_effort 调节。换句话说：**它和旗舰共用同一套"为长程任务而生"的骨架，只是把体量压到了单卡能跑的程度。**

---

## 四、"能跑完"的代价与红利：本地模型的真实经济账

回到 HN 讨论。除了"前沿 vs 本地"的语义之争，评论区还贡献了一组宝贵的实测数据：

- trollbridge：Apple M5 Pro 上 **6-bit MTP 量化约 12-15 t/s**，"还没优化过"；
- jchw：双 Arc Pro B70 约 **22 t/s**，"至少量化程度更低"；
- 两人都提到同一件事——**"本地跑的好处是，你可以跑一个不受审查的模型，不用担心问它'逆向这个老路由器固件、给我一个能用的 license key'会触发 OpenAI 账号封禁"**。

这组数据把争论从"能不能"拉回"值不值"。一次 30 分钟的 RE 会话，如果按 API 计费：思考模式全开、几十轮工具调用、上下文累积到几十万 token，账单轻松上两位数美元；而在本地，它是电费。更重要的还不是钱，而是**边界**：固件分析免不了要碰"看起来危险"的东西——OTA 包、引导程序、加密狗协议。云 API 的 TOS 和内容审查是悬在头上的剑，本地模型没有这把剑。

但"本地"的红利不止于此。catlifeonmars 那条评论值得再读一遍：

> "本地模型不意味着你必须在自己机器上跑，它意味着**你不被绑定在某个供应商身上**。你可以选择数据给谁、不给谁。而且 LLM 的用途远不止全自主 agent 这一种——很多场景你本来就想要一次不经互联网的往返。"

换句话说，**"本地"的本质是选择权**：数据主权、模型选择、审查边界、成本结构。当 27B 的能力越过"够用"门槛（我们 6 月 30 日写过这个拐点的判断：[2026-06-30-local-models-frontier-inflection-point-qwen36-vllm-ornith.md](2026-06-30-local-models-frontier-inflection-point-qwen36-vllm-ornith.md)），选择权就从"奢侈品"变成了"默认配置"。

---

## 五、持久性也是工程：verify loop、环境反馈与 harness 的分工

如果"持久性"是 2026 年模型的新维度，那么对工程师来说最重要的问题是：**它是模型自带的，还是可以被架构放大的？** 我的判断是：两者都有，而后者被严重低估了。

回头看这次 RE 案例，任务能跑通至少依赖三层：

```
第一层：模型能力 —— 理解汇编/二进制、推导校验逻辑（25%，模型卡 agentic 分数担保）
第二层：验证循环 —— 产出→自检（签名/哈希）→发现不一致→重来（verify loop）
第三层：harness 基建 —— Bash 工具集、文件系统、进程管理、上下文保留（Pi 提供）
```

第三层常常被忽略。注意 XDA 作者特意强调"只用 Bash 工具"——这意味着整个 RE 工作流（strings、objdump、xxd、hash 计算、脚本编写、反复运行）**全部发生在 agent 自己的工具循环里**，没有任何 MCP 服务器、没有专用 RE 插件。换句话说，**这个 27B 模型 + 通用 Bash 工具的组合，复刻出了人类逆向工程师的工作方式**。评论区 jchw 提到他在 GPT-5.6 Sol 上见过类似行为："它很乐意自己调 objdump，甚至写 headless 作业去驱动 Ghidra 反汇编。"——闭源大模型早就这么干了，现在 27B 也行了，而且是在本地。

但也别把"持久性"浪漫化。同一个 HN 帖子里，开发者们集体贡献了它的另一面：

**① 持久 ≠ 正确，可能只是固执。** 有人翻出 Linus Torvalds 最近一次内核调试提交（kernel commit `818bebeb`）里的话："这是一场地狱般的调试，AI 帮我干了很多苦活。我想叫它不知疲倦的帮手，**但它好几次斩钉截铁地说这不可能、无解，我们应该写个报告算了**。我怀疑训练这些模型的人，可能没有我这么倔。"——当模型判断"不可能"时，它不会像人一样因为面子或责任继续坚持；它会礼貌地放弃。**持久性是有方向的，而这个方向来自奖励设计。**

**② 过度验证是真实成本。** criemen 观察到："新模型不管改动是否已经正确，都会多走一英里做验证——于是那些老模型一次就答对的简单问题，新模型反而要更久、花更多钱。"在 token 计价的世界里，verify loop 不是免费的。**持久性和成本之间需要一个新的旋钮**，就像 reasoning_effort 那样——这大概率是下一轮 harness 和 API 设计的必争之地。

**③ 场景分裂：greenfield 过度验证，brownfield 过早放弃。** tonyarkles 的田野观察："新代码上它们倾向于过度验证；老代码和数据分析上它们有时放弃得太早，需要一点鼓励才继续挖。"他把原因归结为模型对"数值统计"的偏好——**文本模型天然倾向于计算可量化的指标，而对时域/频域图这种"画出来才知道对不对"的验证方式摸不着头脑**。这意味着"持久性"不是通用的，它是被训练数据塑形的：**模型会坚持验证它"知道怎么验证"的东西。**

这三条加在一起，勾勒出"持久性工程"的完整图景：

| 维度 | 好消息 | 坏消息 |
|------|--------|--------|
| verify loop | 模型主动检查自己的产出（本案例：哈希不匹配→重来） | 验证成本线性上涨，需要 effort 旋钮 |
| 环境反馈 | 官方明确训练了"处理环境反馈"（exit code、日志、报错） | 反馈信号稀疏时（如"画图看"），持久性失效 |
| 放弃判断 | 模型会在确认无解时止损 | "不可能"的判断可能是错的（Linus 案例） |

这也是为什么我们认为 **harness 的下一轮竞争点是"验证基础设施"**：给模型提供可执行的自检通道（测试、哈希、类型检查、回归基准），比单纯堆模型智力更便宜、更可控。我们 7 月 13 日写 token 开销危机（[2026-07-13-coding-agent-harness-token-overhead-crisis.md](2026-07-13-coding-agent-harness-token-overhead-crisis.md)）时说 harness 是隐性税；现在它有了正面的定义：**harness 是持久性的杠杆。**

---

## 六、当固件成为战场：同一项能力的两种用法

把视角拉回安全。昨天到今天，固件安全连续刷屏：Android 车机头单元固件被植入恶意软件（Kaspersky）；斯洛伐克测速摄像头被塞进国家级后门。这些事件的共同点在于：**固件是供应链里最黑、最没人审计的盒子**——厂商倒闭就失传，被植入后门可能要十几年才被发现。

而 Agent 正在把这个领域从"考古学"变成"流水线"。一个 27B 本地模型 + Bash 工具 + 30 分钟，就能完成一次固件 license 机制的完整逆向——同样的能力，白帽可以用来自动化供应链审计（拉取固件→自动提取文件系统→分析启动链→检测可疑签名），黑帽可以用来批量制造针对老旧设备的攻击。**本地可跑、无审查、无 TOS，让这道门槛降到了几乎为零。** 这正是我们 6 月 15 日分析 Agent 对抗性攻击面（[2026-06-15-ai-agent-adversarial-attack-surface-prompt-injection-warfare.md](2026-06-15-ai-agent-adversarial-attack-surface-prompt-injection-warfare.md)）时预测的"能力民主化"的又一实例——而且这次是在物理世界。

对于防御方，结论很直接：**把 Agent 固件分析纳入你的供应链安全流水线，因为攻击方已经在用了。** 具体来说：固件哈希与签名验证的自动化、CVE 影响面自动评估、以及用同样的 RE agent 做"红队演练"——攻击者能用 30 分钟解开的 license 机制，防御者应该用 3 分钟先看一遍。

---

## 七、结论：智商竞赛正在变成韧性竞赛

回到开头的问题：一个 27B 模型 30 分钟逆向一个固件，这件事的分量在哪里？

我的答案：**它标志着 Agent 竞赛的主赛道，从"谁更聪明"切换到了"谁更能把任务跑完"。** 单点智力上，27B 依然打不过万亿参数旗舰（HLE 30.8 vs 40.0 摆在那里）；但在"端到端完成一个真实的、脏的、需要反复验证的长任务"这个维度上，它已经和闭源旗舰同场竞技，甚至因为本地部署、零边际成本、无审查约束而在某些任务上更实用。

这背后是一条清晰的技术主线：**2025 年的模型卖"推理"，2026 年的模型卖"完成"。** 训练目标从"下一个 token 更聪明"延伸到"环境反馈处理得更好、验证循环更彻底、长任务不半途而废"——Qwen3.8-27B 的模型卡、Claude Opus 5 的自验证架构、以及 HN 上那句"有效智能是持久性的函数"，都在指向同一个方向。

HN 讨论里 saidinesh5 的预言值得放在结尾，它把这条主线推到了最远处：

> "我越来越相信，未来会是**大型前沿模型负责生成和更新 skills/输入，'够用就好'的本地模型负责解决我们的日常问题**。很多只需要一点智能的任务，其实不需要那么多算力——只需要够好的文档、工具调用，和一个够好的本地模型。我不确定这对正在疯狂建设的数据中心意味着什么，但这是个激动人心的时代。"

这句话和 mark_l_watson 的做法（"我用最强模型写 harness、写 skill 文件，然后交给本地小模型执行"）互为印证，也和我们 5 月 2 日写的 Agent Skills 范式转移（[2026-05-02-agent-skills-paradigm-shift-engineering.md](2026-05-02-agent-skills-paradigm-shift-engineering.md)）严丝合缝：**前沿模型负责"想"，本地模型负责"做"，而 harness 负责"盯住别停"**。至于数据中心——当"够用就好+持久"成为默认配置，算力的需求重心会从"单次推理的峰值智力"转向"海量并行的小任务吞吐"。那是另一个故事了，但今天这个 30 分钟的固件逆向，就是那个故事的第一个注脚。

---

*参考来源：XDA Developers（Adam Conway）、Hacker News 讨论帖 #49407507、Qwen3.8-27B 官方模型卡、Linus Torvalds kernel commit 818bebeb、Kaspersky Securelist、risky.biz。文中引用均为转述/节译，观点为作者独立判断。*