# 记忆是剂量，不是开关：IBM 的 8 模型校准实验与"上下文数据库"范式崛起

> **摘要**：2026 年 8 月 18 日，IBM Research 在 Hugging Face 博客发表了一项可能是今年最重要的 Agent 记忆研究——ALTK-Evolve HMM（How Much Memory）。在 8 个模型、585 个多步任务上，他们发现了一个反直觉的结论：**Agent 记忆不是"打开就生效"的功能，而是必须按模型能力校准的剂量**。强模型需要全量注入，弱模型被全量注入"淹死"，已饱和的模型则完全无增益。与此同时，GitHub Trending 上 24 小时内出现了三个记忆基础设施项目（akitaonrails/ai-memory 日增 648 星、字节跳动 OpenViking、munder-difflin），Agent 记忆正在从论文问题变成产品战场。本文深度拆解 IBM 的剂量实验，分析其背后的机制，并解读"上下文数据库"这一新范式的设计逻辑与产业含义。

---

## 一、引言：记忆成为 2026 下半年 Agent 基础设施的主战场

先看一组今天（2026-08-19）的观察：

- **GitHub Trending** 上，Agent 记忆相关项目出现了罕见的"三连击"：`akitaonrails/ai-memory`（Rust 编写的跨厂商 Agent 长期记忆与交接方案）单日新增 648 星；字节跳动开源的 `volcengine/OpenViking`（自称"Self-evolving Context Database for AI Agents"）冲入热门榜；`munder-difflin`（本地多 Agent harness）也以记忆为核心卖点。
- **Hugging Face 博客**昨天（08-18）的头条，是 IBM Research 的 ALTK-Evolve HMM 研究：*How Much Memory Does Your Agent Actually Need?*（技术报告 [arXiv:2603.10600](https://arxiv.org/abs/2603.10600)）。
- 上个月，`context-mode` 类工具与 Claude Code 的 memory 插件生态仍在高速膨胀；本博客在 [4 月 8 日](https://github.com/kejun/blogpost/blob/main/2026-04-08-ai-agent-memory-state-2026-architecture-benchmark.md) 曾总结记忆系统已从边缘组件演变为独立工程学科——而四个月后的今天，这个学科正在经历**第二次分裂**：从"架构选型"问题，变成"剂量校准"与"数据范式"问题。

为什么说 IBM 这项研究重要？因为它第一次用**跨能力谱系的受控实验**回答了记忆领域最朴素也最致命的问题：**给 Agent 更多记忆，真的总是更好吗？**

答案是：不是。有时甚至相反。

---

## 二、剂量实验：ALTK-Evolve HMM 拆解

### 2.1 方法学：记忆是"模型外"的学习

ALTK-Evolve 的记忆机制与常见的"回放对话历史"或"微调"完全不同：

1. Agent 执行任务，产生轨迹（trajectories）
2. 系统从成功和失败的轨迹中**自动蒸馏行为准则**（guidelines）——哪些策略有效、哪些错误要避免、哪些边界情况要记住
3. 准则被合并为可复用的准则集（guideline set）
4. 推理时，将全部准则或按任务检索的子集注入上下文

全程**不更新任何模型权重、不需要任何人工标注**。用作者的话说："学习发生在模型周围，而不是模型内部。"这使它成为研究"记忆剂量"的理想载体——因为记忆的增量是纯上下文层面的，剂量可以精确控制。

实验基准是 AppWorld：585 个多步任务（168 个 test_normal + 417 个 test_challenge），覆盖日历、消息、支付等 9 个模拟应用。评分用两个指标：

- **TGC（Task Goal Completion）**：单个任务是否完整正确完成——"活儿干没干成"
- **SGC（Scenario Goal Completion）**：同一场景的所有变体（不同数据、措辞、边界条件）全部通过才算过——**衡量的是可靠性，不是平均表现**

### 2.2 三种模式：剂量取决于能力

研究横跨 8 个模型——从 30B 稠密模型到前沿闭源系统——结果出现了三个清晰可复现的模式：

| 模式 | 代表模型 | 基线 TGC / SGC | 最佳记忆 TGC / SGC | 最佳配置 | Δ TGC | Δ SGC |
|------|----------|---------------|-------------------|----------|-------|-------|
| 弱模型 / 需精选 | gpt-oss-120b (117B MoE) | 39.9 / 21.4 | 56.0 / 37.5 | 精选检索 | **+16.1** | +16.1 |
| 强模型 / 有余量 | DeepSeek-V3.2 (671B MoE) | 79.8 / 64.3 | 89.3 / 80.4 | 全量注入 | +9.5 | **+16.1** |
| 强模型 / 有余量 | Claude Opus 4.6 | 90.5 / 87.5 | 94.6 / 94.6 | 全量注入 | +4.1 | +7.1 |
| 强模型 / 近上限 | GPT-5.5 | 92.3 / 82.1 | 95.2 / 89.3 | 全量注入 | +2.9 | +7.2 |
| 已饱和 | GLM-5 (745B MoE) | 87.5 / 80.4 | 87.5 / 80.4 | 全量注入 | **0.0** | **0.0** |

三个模式的规律性值得细读：

**模式一：弱模型会被记忆"淹死"。**gpt-oss-120b 是 117B 的 MoE，能力中等。给它全量准则集，增益有限且 token 成本暴涨约 50%；但改成"高置信度核心准则 + 每任务检索几条相关准则"的精选方案，任务完成率直接 **+16.1 个百分点**（39.9% → 56.0%），token 开销只增加 **5%**。大多数 Agent 应用跑的就是这类模型——这个数字意味着，**对绝大多数生产系统，检索式精选记忆在精度和成本上双重碾压全量注入**。

**模式二：强模型想要"全部"。**DeepSeek-V3.2 跑全量准则集时 TGC +9.5、SGC +16.1——包括那些罕见边界情况的准则，它都能吸收和应用。739B 级别的模型有足够的"容量余量"来处理全量注入，检索反而会损失信息。

**模式三：饱和模型完全无增益。**GLM-5（745B）在 AppWorld 上无论注入什么都纹丝不动（0.0 / 0.0）。作者谨慎地将其归因为"标签描述而非已证实的因果"：可能是任务已接近模型能力天花板，可能是准则没有命中其真实失败模式，也可能是模型没有有效应用准则。但工程含义是明确的：**给一个已饱和的系统继续堆记忆，是纯浪费。**

关键结论：决定一个模型属于哪种模式的不是参数数量的单调函数——还有基准余量、上下文窗口、架构、准则质量、任务分布共同作用。但实践结论不依赖这些因素的精确划分：**记忆的正确剂量是可校准的，而校准信号就是模型在目标任务上的剩余失败空间。**

### 2.3 成本数据：记忆不一定会烧钱

| 模型 | 配置 | 基线 token/任务 | 记忆后 token/任务 | 开销 |
|------|------|----------------|------------------|------|
| DeepSeek-V3.2 | 全量注入 | 148K | 263K | +78% |
| gpt-oss-120b | 全量注入 | 110K | 166K | +51% |
| gpt-oss-120b | 精选检索 | 110K | 116K | **+5%** |

两个洞察：

1. **"更好的记忆更便宜"是真实存在的**——弱模型场景下，精度最高的方案恰恰是成本最低的方案。
2. **记忆不拖长推理链**。DeepSeek 有/无记忆的 ReAct 步数几乎相同（约 18-19 步），多出来的成本纯粹是输入 token 膨胀——而输入端的静态准则前缀可以被 **prompt caching** 完美命中。作者明确建议在生产中做"缓存感知的提示设计"：把共享准则集放在稳定前缀里，保持可缓存。

### 2.4 SGC 比 TGC 动得更多：记忆的真正价值是可靠性

整张表最容易被忽略的列是 SGC。注意 DeepSeek：TGC 只涨 +9.5，SGC 却涨 +16.1；Opus 4.6 和 GPT-5.5 在 TGC 接近天花板时，SGC 依然分别 +7.1 和 +7.2。**记忆带来的提升在"所有变体都必须过"的严格指标上总是更大。**

这印证了本博客之前反复出现的主题：Agent 生产环境的问题从来不是"平均表现"，而是**长尾失败**——同一个任务换一种数据、换一种措辞就翻车。记忆准则（"这个 API 的支付回调要等 10 秒再验签"）恰好是压缩长尾失败模式的最廉价载体。这也与 [07-25 的 Opus 5 自验证研究](https://github.com/kejun/blogpost/blob/main/2026-07-25-claude-opus5-self-verification-agent-reliability-inflection.md) 形成互补：自验证解决"做完了但做错了"的检查环节，记忆解决"一开始就往错误方向走"的先验环节。

---

## 三、机制分析：为什么"越多越好"是错的

"剂量依赖能力"的现象背后，至少有四个机制在起作用：

**1. 上下文容量是有限工作内存。**全量注入对弱模型是注意力稀释——准则数量超过模型的"有效上下文利用能力"后，每新增一条准则都在压低其他准则的注意力权重。这不是参数多少的问题，而是模型在训练中习得的上下文使用策略的差异。IBM 的假设是：上下文窗口更大的模型可能吸收全量注入更好——这也是他们下一步要做受控实验隔离的变量。

**2. 检索选择器是当前的瓶颈。**IBM 的检索目前用 cosine similarity 排序准则，他们自己承认"相似度并不完美预测哪条准则对当前任务有帮助"。这其实是个被低估的发现：**今天的记忆检索用的还是嵌入相似度这个 RAG 时代的默认武器，而它和"这条经验对完成任务是否有用"之间隔着一层因果关系。**自然的下一步是学习式选择器（learned selector）——用任务结果信号训练，而不是用文本相似度。这个方向的成熟，可能比模型本身的能力提升更能撬动记忆的收益。

**3. 记忆与能力存在"匹配区间"。**可以把模型-记忆关系画成一条倒 U 曲线：能力太弱时，记忆本身（哪怕是精选的）也救不了推理短板（IBM 也观察到低于某个能力基线后自蒸馏缺乏信号，需要教师蒸馏记忆）；能力中等时，精选记忆收益最大；能力很强但任务有余量时，全量最优；能力已饱和时，曲线归零。**校准的本质，就是找到模型在特定任务分布上所处的区间。**

**4. 记忆的边际收益衰减但不会消失。**GPT-5.5 与 Opus 4.6 在 TGC 都 90+ 的情况下 SGC 依然有 +7 的提升——只要还有剩余的失败模式可以瞄准，记忆就继续付钱。这与 08-18 [AI;DR 阅读代理研究](https://github.com/kejun/blogpost/blob/main/2026-08-18-aidr-reading-agents-faithfulness-crisis.md) 的结论殊途同归：Agent 表现的瓶颈越来越多地出现在上下文内容的组织与使用方式上，而不是模型权重里。

---

## 四、范式转移：从"向量库黑盒"到"上下文数据库"

IBM 回答的是"给多少"，而今天同时在 GitHub 上爆发的另一条线回答的是"存在哪、怎么取"。字节跳动开源的 OpenViking 是这条线最激进的代表。

### 4.1 OpenViking：把记忆变成文件系统

OpenViking 的核心命题：**Agent 的上下文（记忆、知识、技能）应该是一个数据库，而且是一个"文件系统形态"的数据库。**三个设计决策尤其值得拆解：

**决策一：`viking://` 虚拟文件系统取代向量库黑盒。**记忆、资源、技能各有一个 URI，Agent 用 `ls`、`tree`、`find` 来浏览自己的上下文，而不是向黑盒向量库丢一个 query 然后信任返回结果。这一设计的深层动机是**确定性**：文件系统导航是确定性的、可预测的、可调试的，而向量检索是概率性的、不可解释的。让 Agent "像开发者操作文件一样"操作记忆，等于把记忆的访问模式从"猜测"变成"工程"。

**决策二：L0/L1/L2 三级分层加载。**每个条目在写入时被处理成三层：L0 是一句话摘要（约 100 token，用于快速相关性判断）、L1 是概览（约 2K token，含结构与要点）、L2 是完整原文（按需读取）。每个目录也带自己的 L0/L1。这本质上是把 IBM 的"剂量"思想做进了存储层——**不是注入时再决定给多少，而是存储时就按层级切好，让 Agent 按任务深度渐进加载**。OpenViking 的 benchmark 显示输入 token 下降 34.3%–91.0%，查询延迟下降 58.45%–66.10%，代价是记忆质量不降反升：

| 指标 | 原生记忆 | + OpenViking |
|------|---------|--------------|
| LoCoMo 长对话用户记忆准确率 | 24–57% | **80–83%**（三个 Agent 集成一致） |
| tau2-bench 零售任务成功率 | 基线 | +6.87pp |
| tau2-bench 航空任务成功率 | 基线 | +11.87pp |

**决策三：可观测的检索轨迹 + 会话自动沉淀。**每次检索都保留"目录浏览轨迹"——当结果看起来不对，你能看到是哪条路径产生了它；会话提交后，系统异步提取用户偏好与 Agent 经验写入长期记忆。这两点分别解决了记忆系统最头疼的两个生产问题：**不可调试**和**写不进去**（大多数记忆系统死于开发者不敢让 Agent 自动写记忆，因为写入不可控）。

### 4.2 为什么是"数据库"而不是"向量库"？

OpenViking 团队在 [The Database Paradigm for Context Engineering](https://blog.openviking.ai/post/openviking-context-database/) 里给出的论证值得认真对待：

- **向量库是索引，不是存储**。向量相似度检索适合"模糊召回"，但 Agent 的上下文访问大部分是结构化的（"这个项目的 API 文档在哪""用户对代码风格的要求是什么"）——结构化的访问需要结构化的组织。
- **记忆需要可浏览性**。人类记忆的重要特征是"我知道我知道什么"（元认知）。文件系统给了 Agent 一个廉价的元认知层：`tree` 一下就知道自己有什么，而不必盲目检索。
- **分层即剂量**。L0/L1/L2 把"剂量控制"内置进数据模型，与 IBM 的发现形成精确呼应：不同能力的模型、不同深度的任务，加载不同深度的层级。

值得注意的是，这个"数据库化"方向并非孤例。`ai-memory` 把记忆做成会话式交接文档（handoff），`munder-difflin` 把记忆做进多 Agent harness，`context-mode` 类工具在做压缩。整个行业正在从"memory 是一个 feature"走向"memory 是一层基础设施"——而基础设施的第一性问题就是**数据模型**：向量？文档？文件系统？还是 MCP 上的协议？

### 4.3 产业侧的另一信号：记忆的可移植性

`akitaonrails/ai-memory` 日增 648 星说明了一个此前被忽视的需求：**开发者不想被锁死在某个 Agent 厂商的记忆格式里**。它的卖点是"Claude Code 干到一半，换 Codex 继续，不用重新解释架构、失败的方案和未决问题"——支持 15+ 个 Agent 的 MCP 配置与生命周期钩子，用 Rust 写，自带 Docker 与各平台原生二进制。

这背后是 2026 年 Agent 互操作性危机的延伸（见本博客 [07-07 的文章](https://github.com/kejun/blogpost/blob/main/2026-07-07-agent-interoperability-crisis-codex-plugin-herdr-cross-agent-protocol.md)）：工具调用协议（MCP）统一之后，**记忆格式是下一个必须统一的层**。今天每个 Agent 的记忆都是私有格式、私有存储、私有生命周期钩子——这意味着用户积累的经验资产不可携带。`ai-memory`、OpenViking（同样提供跨 Agent 集成）以及 agentskills.io 这类技能标准，都在争夺"记忆/上下文标准"的生态位。**记忆很可能成为继 MCP 之后的下一个协议级战场**——而这一次的赢家标准不是"谁的能力最强"，而是"谁的数据模型最接近正确"。

---

## 五、工程实践：如何为你的 Agent 校准记忆剂量

把 IBM 的实验结论和 OpenViking 的分层思想落地，一个务实的校准流程大概是这样的：

1. **先测量，再记忆**。在目标任务分布上跑基线，确认模型是否还有失败空间。如果 TGC/SGC 已接近天花板（如 GLM-5 的 87.5/80.4），先别加记忆，去找别的瓶颈。
2. **按模型能力选择注入策略**：
   - 中小模型（<100B）：精选检索——高置信核心准则 + 每任务检索 3-5 条，目标 token 开销 <10%。
   - 大模型（>300B）且任务有余量：全量注入，但做缓存感知设计，把静态准则放在可缓存前缀。
   - 不确定时：A/B 测试两种配置，用 SGC（而不是 TGC）作为决策指标——记忆优化的是可靠性。
3. **存储层分层**。不要把所有东西塞进一个向量库。至少分成：结构化偏好（KV/文档）、经验准则（检索集）、原始轨迹（归档）。OpenViking 的 L0/L1/L2 是一个可以直接抄的分层模板。
4. **让记忆可观测、可回滚**。每次检索记录轨迹，每次写入可审计。不可调试的记忆系统最终会被开发者关掉——这是记忆系统在生产环境的第一死因。
5. **警惕检索选择器的天花板**。Text 相似度检索只是起点；用任务结果信号训练选择器，是当前记忆系统 ROI 最高的改进方向之一。

---

## 六、展望：记忆工程的三个开放问题

**其一，学习式选择器。**IBM 自己列出的 next step 是 trained selector——用任务完成信号代替 cosine similarity。这会把"记忆检索"从 RAG 的继承品变成 Agent 原生的组件（类似 [05-13 那篇](https://github.com/kejun/blogpost/blob/main/2026-05-13-beyond-semantic-similarity-direct-corpus-interaction-agentic-search.md) 讨论的"超越语义相似度"）。

**其二，弱模型的记忆来源。**自蒸馏的记忆在极弱模型上缺乏信号——教师蒸馏（teacher-distilled）记忆是下一个研究前线。这暗示记忆系统可能走向"模型越弱，外部记忆系统要越强"的分工。

**其三，上下文数据库的标准之争。**`viking://` 会不会成为记忆层的 `http://`？还是会被 agent 厂商的私有格式、或是 MCP 的扩展吃掉？OpenViking 用 benchmark 数据和开源许可证参与这场竞争，而 ai-memory 用"跨厂商交接"从另一个角度切入。无论谁赢，"记忆即基础设施"这个判断已经不需要争论了。

---

## 结语

IBM 的剂量实验给出了 2026 年记忆工程最锋利的一句话：**"Agentic memory is not a feature you switch on. It's a dose you calibrate to the model."（记忆不是打开就生效的功能，而是需要按模型校准的剂量。）**

叠加今天 GitHub 上记忆基础设施的集体爆发，可以得出一个更完整的图景：Agent 记忆正在经历从"有没有"到"给多少"再到"存在哪、怎么取"的三级跳。剂量校准解决的是上下文效率的上限问题，上下文数据库解决的是记忆资产的组织与沉淀问题，跨厂商交接解决的是记忆的流动性问题——三者合起来，就是 Agent 长期记忆从研究玩具走向生产基础设施的完整拼图。

而这一轮变革最有趣的地方在于：**瓶颈不在模型，不在算力，而在"记忆的组织方式"这个看似平凡的软件工程问题上。**卷了三年模型之后，行业终于开始卷记忆的数据模型了——这是数据库工程师们的时代。

---

**参考来源：**

- IBM Research, *How Much Memory Does Your Agent Actually Need?*（Hugging Face Blog, 2026-08-18）：https://huggingface.co/blog/ibm-research/altk-evolve-hmm
- ALTK-Evolve 技术报告（arXiv:2603.10600）：https://arxiv.org/abs/2603.10600
- Volcengine OpenViking：https://github.com/volcengine/OpenViking
- akitaonrails/ai-memory：https://github.com/akitaonrails/ai-memory
- OpenViking Benchmark 报告：https://blog.openviking.ai/post/openviking-benchmark-results/