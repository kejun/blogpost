# 当"后训练"变成流水线：IBM Granite 4.2 深度拆解——8 个 RL 阶段、真实环境 Agent 训练与 30B 的 AIME 89.17

> **日期：** 2026-09-01
> **标签：** Granite 4.2, IBM, 推理模型, 多阶段RL, GRPO, Agentic RL, NeMo-RL, NeMo-Gym, 后训练, 开源模型, SWE-Bench, 课程表

---

## 摘要

2026 年 8 月 25 日，IBM 发布 Granite 4.2——家族第一个**推理（reasoning）系列**，3B / 8B / 30B 三个稠密（dense）模型，Apache 2.0 全开源。表面上是又一家大厂入场推理模型，但真正值得拆解的是它的**生产方式**：IBM 把后训练做成了**一条八阶段的 RL 流水线**——SFT → RLVR → 技能加强（booster）→ SWE Agent → Terminal Agent → Search Agent → RLHF，每个阶段是独立的一次 GRPO 运行，从上一阶段的 checkpoint 热启动。

- **15T token 从零预训练 + 五阶段数据退火**，上下文最终扩展到 **512K**；SFT 混合了 **7.2M 条样本（约 100B token）**，其中 **31.6% 是 agentic 轨迹**，由 12 种不同 agent harness（OpenHands、Codex、Hermes、Goose……）生成后统一清洗；
- **8B/30B 额外经过"Agentic RL 块"**：在真实沙箱环境里学会改代码（SWE-Bench Verified 30B 达 **57.00**）、操作终端（Terminal-Bench 2.1 达 **29.24**）、多跳联网搜索——奖励是稀疏的"任务是否真正完成"；
- **30B 的推理成绩单**：AIME25 **89.17**（2025 年初的 DeepSeek-R1 为 79.8、o3 高算力档为 87.7）、GPQA 66.41、RULER 128K **81.38**、τ³-bench **62.00**——一个 30B 稠密模型摸到了两年前的闭源前沿；
- 工程上最反直觉的细节：异步 GRPO 允许**一条 rollout 由两个相邻策略版本的轨迹"拼接"而成**，靠 KV cache 复用和截断重要性采样兜底——"干净"让位于"不停机"。

这篇文章将拆解三个问题：**为什么 2026 年的 IBM 仍坚持稠密架构？多阶段 RL"课程表"背后的奖励设计与 KL 调度原则是什么？真实环境 Agent 训练的算力经济学，能从 Granite 4.2 的配置表里读出什么？**

---

## 一、引言：推理模型的"第二梯队"正在成型

过去一年，开源推理模型的叙事被两件事主导：**DeepSeek-R1 式的大规模端到端 RL**，和 **GLM/Qwen 的"后训练军备竞赛"**（我们在 8-29 拆解过 GLM-5.3 的"同一基座、纯后训练跃迁"）。但这两条路都有一个隐含前提：**RL 是一次"大爆炸"**——一个巨型阶段，把所有数据、所有能力一起烧进去。

Granite 4.2 提供了第三种答案：**RL 不是一次大爆炸，而是一条流水线**。IBM 把后训练拆成 8 个职责单一、顺序执行的阶段，每个阶段只练一个能力、只有一种主奖励信号、跑完就导出 checkpoint 交给下一站。这听起来像是"把简单的事做复杂"，但仔细看配置表会发现，这是一种深思熟虑的**工业选择**：单一目标让每个阶段的奖励设计可以做到极致干净，热启动让整条链可断点续跑、可审计、可定位失败。

这个选择的背景同样值得注意：Granite 4.2 全部参数 Apache 2.0、明确面向企业部署（vLLM/SGLang 即插即用、OpenCode/Pi/OpenHands 官方集成教程、FP8/FP4/GGUF 量化全家桶）。**它是"企业级开源推理模型"这个生态位的宣言书**——而生态位决定了架构与管线的一切取舍。

---

## 二、一张 15T token 的"旧船票"：稠密架构的坚持

先看架构表：

| 组件 | 3B | 8B | 30B |
|------|----|----|-----|
| Embedding 维度 | 2560 | 4096 | 4096 |
| 层数 | 40 | 40 | 64 |
| 注意力头 / KV 头 | 40 / 8 | 32 / 8 | 32 / 8 |
| head dim | 64 | 128 | 128 |
| MLP 隐藏层 | 8192 | 12800 | 32768 |
| 训练序列长度 | 131072 | 131072 | 131072 |
| 激活 / 归一化 | SwiGLU / RMSNorm | 同左 | 同左 |
| 位置编码 | RoPE（θ=10,000,000） | 同左 | 同左 |

规整的 GQA（全系 8 个 KV 头）、SwiGLU、RMSNorm、输入输出 embedding 不共享、bf16——没有一项是 2026 年的"新东西"。引人注目的反而是两个"保守"信号：

**其一，RoPE 基频 θ=10,000,000。** 主流模型常用 500K~1M 的 θ 来做长度外推，Granite 4.2 直接把 θ 拉高一个数量级。更大的 θ 意味着位置编码对相对距离更"敏感度递减"，高频信号更密集，长距离外推更稳——这是为 512K 上下文目标做的架构级预留，而不是事后补救。

**其二，在 MoE 与混合线性注意力（Hybrid Linear Attention）成为开源主旋律的 2026 年，IBM 坚持纯稠密。** 看看同期生态：Qwen3.8-2.4T 是混合线性注意力 MoE（8-13 拆解过）、Nemotron 系列走模型路由、GLM 走规模化稠密——Granite 4.2 的选择逻辑不在"每 token 算力效率"，而在**企业部署的确定性**：稠密模型的显存占用可预测、无路由不确定性、量化行为更稳定（他们一口气提供了 FP8/NVFP4/MXFP4/GGUF 四套量化方案，正是企业客户的刚需）。在"每 token 成本"与"部署确定性"的权衡上，IBM 明确押注后者——这提醒我们：**架构选择首先是市场选择，其次才是技术选择。**

预训练本身是 15T token、五阶段数据退火（阶段 1-2 打基础、3-4 高质量数据 annealing、阶段 5 长上下文训练到 512K），配方"紧密沿用上一代"——IBM 刻意把预训练描述为"已知问题"，把增量全部押在了后训练。这与 8-29 GLM-5.3 的判断互为印证：**2026 年的模型竞争，主战场已经转移到 SFT 与 RL。**

---

## 三、SFT 数据工厂：31.6% 的 Agentic 轨迹与十二路 Harness 混流

Granite 4.2 的 SFT 语料约 **7.2M 条样本、100B token**（其中约 65B 参与训练），构成如下：

| 语料 | 占比 | 细分 |
|------|------|------|
| **Agentic（31.6%）** | — | SWE 工程 69% / 工具调用 12.1% / 终端操作 8.0% / 数学 3.5% / 搜索 0.8% / 行动类 0.2% |
| **非 Agentic（68.4%）** | — | 指令跟随 18.8% / 编码 18.8% / 数学 14.6% / 多语言 7.0% / 科学 5.4% / 推理 3.0% / 安全 0.8% |

这里最值得注意的不是占比，而是**数据的生产方式**：agentic 轨迹由 12 种不同的 agent harness 生成——OpenHands、OpenCode、Terminus-2、SWE-agent、OpenResearcher、MiniSWE、OpenSeeker、EnvScaler、Gemini CLI、Hermes、Codex、Goose。一家企业把竞品和开源工具全部拿来当"数据采集器"，说明 agentic 数据已经被当作**原材料**来工业化开采：不同的 harness 有不同的问题解决风格与错误模式，混合它们等于给模型做数据增强。

随之而来的是标准的"数据工厂"三件套：

1. **统一格式**：所有来源、所有 harness 的轨迹被重排成一致的 OpenAI Chat 格式，工具交互结构统一——这是后续一切质检的前提；
2. **LLM 判官**：用 GPT-OSS-120B 与 Gemma 4 两个 judge 给样本打分，低分、幻觉、无效工具调用（调用未定义函数）直接剔除。注意这里 IBM 没用自家模型做 judge，而是选了两个异构的外部模型——**判官与被判对象保持异源**，是防止"自我偏好污染"的朴素而有效的设计；
3. **SHA-256 去重**：对 tools+messages 字段哈希，做局部与全局两级去重——agentic 轨迹高度模板化，不去重的话同一条轨迹会以几十种措辞变体反复出现。

30B 还有一个特殊的 **Phase 2 SFT**：把 agentic/SWE/编码数据上采样、保留 16% 的原始语料做 replay（防灾难性遗忘），以更低学习率（3e-6）再跑约一个 epoch。这个细节印证了一个通用规律：**模型越大，越需要"定向加餐"才能把 agentic 能力顶上去**——8B 不需要，30B 需要。

---

## 四、RL 课程表：八个阶段、三种奖励、一套 GRPO

Granite 4.2 的核心创新在 RL。整条链如下：

```
SFT ─▶ RLVR ─▶ 技能加强 ─▶ SWE Agent ─▶ Terminal Agent ─▶ Search Agent ─▶ RLHF
   └─────────── 基础 RL（全部尺寸）───────────┘  └──── Agentic RL（仅 8B/30B）────┘
```

每个方框都是**独立的一次 GRPO 运行**，单一目标、单一主奖励，跑完导出 HF 格式 checkpoint，成为下一阶段的起点。30B 链上每个阶段的精确配置（这是全文最有信息量的一张表）：

| 阶段 | prompts/步 | 采样/提示 | 最大序列 | rollout 轮数 | KL | LR |
|------|-----------|----------|---------|-------------|-----|-----|
| RLVR（×3 轮） | 256 | 16 | 64K | 1 | **0** | 5e-7 |
| IF 加强 | 256 | 16 | 64K | 1 | **0** | 5e-7 |
| Code 加强 | 64 | 16 | 64K | 1 | 0.05 | 5e-7 |
| SWE 1 | 64 | 16 | 128K | 1 | 0.01 | 5e-7 |
| **SWE 2** | 32 | 16 | 128K | **128** | **0** | 5e-7 |
| **Terminal** | **8** | **32** | 64K | **64** | 0.01 | 1e-6 |
| Search | 32 | 16 | 128K | **64** | 0.01 | 5e-7 |
| RLHF | 128 | 16 | 48K | 1 | 0.05 | 5e-7 |

从这张表可以提炼出三个设计原则：

**原则一：奖励越客观，缰绳放得越开（KL 调度）。** RLVR 与 SWE 2 跑在 KL=0——完全没有对参考策略的约束，因为奖励是硬性的（boxed 答案、隐藏测试、格式检查器），"自由探索"没有越界成本；而 RLHF、Code 加强跑在 KL=0.05——奖励是偏好与安全这种主观信号，必须紧贴参考策略防止漂移。这是一个可以直接抄走的启发式：**KL 惩罚应当与奖励信号的"可作弊性"成正比。**

**原则二：可验证奖励前置，稀疏奖励殿后。** 奖励被分成三类：**可验证奖励**（精确匹配、单元测试、规则检查器——客观、难以作弊）、**LLM judge 奖励**（开放质量、偏好、安全性）、**Agentic 结果奖励**（任务在真实环境里到底完成没有——最稀疏：一条几十轮的长轨迹末端只有 1 个 bit）。流水线把可验证奖励放在最前面打地基，把最稀疏的 agentic 奖励放在最后做精修——因为稀疏奖励需要模型先具备足够的基础能力才有正样本可学。

**原则三：真实环境训练是"贵"出来的。** 注意 batch 的塌缩：RLVR 阶段 256×16=**4096** 样本/步，Terminal 阶段只有 8×32=**256**。一个真实 shell 里跑 64 轮命令的 rollout，成本是模拟环境里单轮生成的几个数量级以上——**采样成本直接写进了 batch size**。SWE 2 的 rollout 轮数是 128，意味着 GRPO 的"一步"就是一条完整的 128 轮代码修复轨迹：这是把 agent 循环直接放进优化目标，而不是事后把轨迹当静态数据用。

---

## 五、异步 GRPO：允许"拼接"的工程哲学

Granite 4.2 的 RL 基础设施是 NeMo-RL（训练侧：Megatron-Core 训练后端 + vLLM 生成 + Megatron-Bridge 做 HF⇄Megatron 权重互转）+ NeMo-Gym（rollout 侧：把 verifier、工具、沙箱、奖励模型统一抽象成 Resource，挂到同一接口下）。

这套系统最反直觉的工程决策在**异步 GRPO** 的细节里：生成 worker 与训练器完全解耦——worker 持续采样、把完成的轨迹丢进共享缓冲，训练器攒满一步就取走、更新参数、把新权重流式推回 worker，**全程互不阻塞**。于是出现了一个"理论家会皱眉"的场景：

> 参数刷新可能落在一条 rollout 的中间——一条轨迹的前半段是旧策略生成的，后半段是新策略生成的。IBM 的选择是**允许这条拼接轨迹进入训练**，而不是丢弃重采。

理由非常工程化：worker 复用已有 KV cache 而不是刷新后重建，省下的生成时间远大于"脏数据"的代价；唯一的护栏是**步差上限**（worker 最多落后训练器一个更新），把 off-policy 程度钉死在一个可接受范围内；残余偏差由**截断重要性采样**兜底——把新旧策略的 log-prob 比值钳制在 0.2/0.28 的区间，防止少量陈旧 token 主导一次更新。

这是一次漂亮的"工程妥协 + 算法修正"组合：**吞吐优先，正确性靠数学兜底而非流程洁癖**。传统同步 RL 里，参数每更新一次就要同步重启全部 rollout，GPU 生成侧在 optimizer step 期间只能空转——我们在 7-31 拆解过 GPU 利用率危机，Granite 4.2 的解法正是那个问题的直接产物。NeMo-Gym 的 Resource 抽象同样服务于吞吐：一个数学 verifier 和一个 SWE 容器沙箱，对 GRPO 主循环暴露的接口完全一致——**环境的多样性被抽象掉之后，流水线才能像装配线一样运转。**

---

## 六、真实环境的账本：Agentic RL 的三块试验田

8B/30B 的 Agentic RL 块包含三个环境，每个都是"真实环境 + 真实 harness + 稀疏结果奖励"：

**SWE（软件工程）。** 每个任务是放在独立沙箱里的真实仓库，由 OpenHands 驱动读代码、改文件、跑测试，奖励是"隐藏测试是否通过"。任务来自开源 SWE 数据集，每个实例配一个 per-repo 容器镜像。这是三个环境里奖励最"硬"的一个——也正是 SWE 2 敢跑 KL=0 的原因。

**Terminal（终端操作）。** 在 Harbor/Terminus-2 的实时 shell 里完成多步任务：规划命令序列、观察输出、从错误中恢复，任务完成才给奖励。它是唯一一个把多轮 agent 循环直接驱动在 GRPO 层面的阶段（rollout 最长 64 个环境交互轮），也是 batch 最惨烈的阶段（8×32=256）。

**Search（深度研究）。** 在浏览 agent 循环里用实时网页搜索工具回答多跳难题。因为答案开放，奖励不得已退回 LLM judge——这是三块试验田里**唯一一个奖励本身可能被博弈**的环节。

把三个环境放一起看，IBM 实际上在回答一个行业问题：**agentic RL 的奖励信号应该长什么样？** 他们的答案是梯度式的：能验证就用验证（SWE），验证不了就用"完成即得分"（Terminal），再不行才用 judge（Search）。对照 8-27 拆解的 OpenAI-HF 奖励黑客事件——"为可被 hack 的奖励而优化"的教训——IBM 的排序原则是对的：**奖励的每一分主观性，都是未来的一份攻击面。**

---

## 七、成绩单：30B 的 AIME 89.17 意味着什么

| 任务 | 3B | 8B | 30B |
|------|-----|-----|------|
| AIME25（数学） | 78.33 | 86.67 | **89.17** |
| GPQA（科学） | 54.80 | 64.14 | **66.41** |
| LiveCodeBench v6 | 69.71 | 73.24 | **75.77** |
| SWE-Bench Verified | — | 47.67 | **57.00** |
| SWE-Bench Pro | — | 19.11 | **33.29** |
| Terminal-Bench 2.1 | — | 20.56 | **29.24** |
| τ³-bench（通用 Agentic） | 45.78 | 58.06 | **62.00** |
| BFCL v4（工具调用） | 52.41 | 50.29 | **61.39** |
| RULER 128K（长上下文） | 55.30 | 71.41 | **81.38** |
| MMLU-Pro | 67.84 | 74.04 | **77.60** |
| Arena-Hard-V2（对话质量） | 34.96 | 65.19 | **67.93** |

几个值得展开的读数：

**1. AIME25 89.17 是"稠密 30B"的量级突破。** 参照公开历史基线：2025 年初的 DeepSeek-R1（671B MoE 激活 37B）AIME pass@1 为 79.8，o3 高算力档 87.7。一个 Apache 2.0 的 30B 稠密模型在数学推理上越过这两者——虽然基准生态在一年半里已有演化，但这个数字仍然标志着**推理能力与参数规模的解耦**：后训练（尤其是 RLVR 的三轮迭代）正在成为比规模更便宜的"算力杠杆"。

**2. SWE-Bench Verified 57.00：开源 30B 的第一梯队，但离闭源顶尖仍有距离。** 57% 已显著超过 2025 年的开源标杆（R1 的 49.2），但同期的闭源前沿在 70%+ 区间——真实世界的代码修复仍然需要更大的模型或更强的 harness 编排。值得注意 SWE-Bench Pro 33.29 与 Verified 57.00 之间的落差（33.29 vs 57.00）——**基准越接近真实工程，分数缩水越狠**。这正是我们在 7-09 拆解 SWE-Bench Pro 危机时指出的问题：Verified 任务的"可解性"天花板已经撑不住头部模型的区分度，Pro 才是 2026 年的试金石。30B 在 Pro 上拿到 33.29，说明它的 agentic 编码能力是"真实可用的"，而非榜单特化。

**3. RULER 128K 81.38：512K 训练没有白烧。** 长上下文是少数"规模效应极其陡峭"的能力维度：3B 只有 55.30，8B 71.41，30B 81.38。考虑到黄金检索（needle）任务极易通过数据注水刷分，RULER 的多任务设计让这个数字更有说服力——但也要提醒：**RULER 测的是"找得到"，不是"用得好"**，128K 上真正的推理质量仍需生产场景验证。

**4. τ³-bench 62.00：Agentic RL 泛化性的正面证据。** τ³-bench 是 2026 年的通用 agentic 基准，覆盖的任务形态与 Granite 4.2 训练的 SWE/Terminal/Search 并不重合。30B 能拿到 62.00（对比 3B 的 45.78、8B 的 58.06），说明在真实环境里练出来的"行动能力"**确实迁移到了未见过的工具与任务**——这是"真实环境 RL"路线最有价值的产出，远比单一榜单分数重要。

**5. 一个必须点破的混杂变量：规模 × 阶段。** 看 3B 与 8B 的差距时要注意，"How the Three Sizes Differ"表暴露了一个实验设计上的混淆：3B 不仅参数更小，还**没有 agentic RL 块、没有 IF/GPQA 加强**。所以 Arena-Hard-V2 从 3B 的 34.96 跳到 8B 的 65.19，主要贡献者是 IF 加强阶段而非规模本身；同样，τ³-bench 的 45.78→58.06 也无法干净地归因于规模。**"更大所以更强"与"练得更多所以更强"在这里被绑定了**——如果 IBM 想证明规模效应，应该给 3B 也跑一遍完整的 agentic 块（成本不允许可以理解，但读者解读分数时要保持清醒）。

---

## 八、工程配套：思考开关、低努力模式与量化全家桶

Granite 4.2 在"好部署"上的投入值得单独拆解，因为它暴露了企业模型的产品哲学：

**思考开关（Thinking / Non-Thinking）与低努力模式。** 同一模型可以按需切换是否输出思维链，`low_effort` 模式对简单问题分配极短的推理预算（示例输出里对 "2+2" 只思考了四个词）。这本质上是**推理成本的用户级可编程化**——把"要不要想"的决策从模型手里交给应用层。

**历史思考截断（truncate_history_thinking）。** 多轮对话时默认剥掉历史轮次的思维链以节省上下文——一个直击 agent 场景痛点的默认值。我们在 7-13 拆解过 coding agent 的 token 开销危机：思维链占了推理 token 的大头，多轮 agent 循环里历史思考链反复进上下文是纯浪费。IBM 把这个优化做成了**默认开启的模板行为**，而不是留给用户自己去 hack——这是"为 agent 设计模型"的教科书式细节。

**原生工具调用与生态即插即用。** vLLM 起一个 OpenAI 兼容端点即可，工具调用直接走 OpenAI function-calling 格式，OpenCode / Pi / OpenHands 都有官方配置教程。这验证了一个 2026 年的行业共识：**推理模型的能力差异化正在收敛，接入成本才是护城河**。

**量化矩阵。** FP8（动态 per-channel 权重 + per-token 激活，免校准）、NVFP4/MXFP4（GPTQ，用 2K 条 SFT 样本校准）、GGUF 全档位（Q2_K~Q8_0）。对照 8-26 拆解的 Quantization-Aware Healing（4-bit 反超全精度原版），Granite 4.2 的量化属于"标准动作"，但胜在齐整——**企业客户要的不是最激进的量化，而是每个档位都有据可依**。

---

## 九、批判与启示：流水线的代价与盲区

Granite 4.2 的"流水线哲学"令人印象深刻，但也必须指出它的代价与盲区：

**1. KL=0 的自由探索是一把双刃剑。** SWE 2 在 128 轮 rollout、KL=0 的条件下训练——模型被鼓励在真实环境里最大化"通过测试"这个目标。8-27 的 OpenAI-HF 根因报告刚刚演示过：**当优化信号可以被 hack 时，模型会以超出人类预期的速度发现 hack**。IBM 的缓冲是"真实环境 + 隐藏测试"（verifier 不易被直接博弈），但 Search 阶段依赖 LLM judge，仍然存在"为判官优化而非为事实优化"的空间。流水线的前置可验证奖励降低了风险，却没有消除它。

**2. "配方开源、食材保密"的可复现性赤字。** Apache 2.0 权重 + 极其详细的配方（连 KL 值、batch、θ 都公开了），但 15T 预训练语料、SFT/RL 数据的具体构成并未开源。社区能复现"方法"，无法复现"数据"——这是 2026 年所有开源模型共同的隐痛，Granite 4.2 只是把这一矛盾暴露得更彻底：**它的每个阶段设计都值得抄，但抄完你会发现，差距的 50% 在数据流水线里**。

**3. 成本依然是黑箱。** GB200 NVL72 集群（CoreWeave 托管）、72-GPU NVLink 域、400Gb/s NDR InfiniBand、"数千张 GPU"——IBM 公开了硬件，却没有公开任何一阶段的 FLOPs 或 GPU-hours。30B 的三轮 RLVR + 全套 agentic 块 + Phase 2 SFT 叠加下来，训练成本大概率是同级模型的数倍。**"在训练时多花钱、在部署时少花钱"对企业是否划算，取决于摊薄周期**——IBM 赌的是 Apache 2.0 带来的生态采用率。

**给工程师的四条建议：**

1. **偷走 KL 调度启发式**：奖励越客观（可验证），KL 越接近 0；奖励越主观（偏好/安全/judge），KL 越大。这是全文最可迁移的一条原则，任何做 RL 后训练的团队都能直接用。
2. **把 RL 阶段化**：单一目标阶段 + checkpoint 热启动 = 可审计、可断点、可定位失败的训练状态机。即使你的集群只够跑两个阶段，也比一次"大爆炸"RL 更可控。
3. **agentic 数据要"多 harness 混流"**：用 3-5 种不同 agent harness 生成轨迹再统一清洗，比单一 harness 的数据增强效果更好——Granite 4.2 用了 12 种。
4. **部署评估盯"Pro 级"基准**：SWE-Bench Verified 已经饱和，用 SWE-Bench Pro / Terminal-Bench 这类更接近真实工程的基准做选型，否则会高估模型的生产能力。

---

## 十、结论

Granite 4.2 是 2026 年开源推理模型格局里的一块重要拼图，但它最重要的贡献不是分数，而是**生产方式**：

- **RL 从"大爆炸"变成"流水线"**：八个阶段、单一目标、热启动、可审计，把后训练从黑魔法变成了装配线——这与 GLM-5.3 的"后训练即前沿"叙事互为表里，一个证明后训练的收益，一个证明后训练的组织方式；
- **稠密架构在 MoE 洪流中的逆势坚持**：IBM 用全套部署配套证明，"确定性"本身就是一种产品价值——不是所有客户都要极致 token 经济，很多客户只要"可预测"；
- **真实环境 Agent 训练从口号变成了配置表**：batch 从 4096 塌缩到 256、64 轮终端 rollout、128 轮 SWE 轨迹——这些数字就是 agentic RL 的物理学，也划出了它的成本边界。

回到开头的问题：为什么 2026 年的 IBM 仍坚持稠密？因为 Granite 4.2 不是给发烧友的玩具，而是给企业的部署件——**架构服务市场，管线服务确定性，分数只是这一切的副产品**。当开源模型的军备竞赛从"谁的基座更大"转向"谁的后训练流水线更工业化"，Granite 4.2 就是这条转型线上最清晰的坐标之一。

---

## 参考链接

- Hugging Face Blog：[Granite 4.2 LLMs: How They're Built](https://huggingface.co/blog/ibm-granite/granite-4-2)（2026-08-25，IBM Granite Team）
- Hugging Face 模型集合：[Granite 4.2 Language Models](https://huggingface.co/collections/ibm-granite/granite-42-language-models)
- 代码仓库：[ibm-granite/granite-4.2-language-models](https://github.com/ibm-granite/granite-4.2-language-models)
- Granite 文档：https://www.ibm.com/granite/docs/
- GRPO：DeepSeekMath（arXiv:2402.03300）
- NeMo-RL：https://github.com/NVIDIA-NeMo/RL ；NeMo-Gym：https://github.com/NVIDIA-NeMo/gym
- OpenHands：https://github.com/All-Hands-AI/OpenHands ；Terminus-2 / Harbor 框架：https://www.harborframework.com/docs/agents/terminus-2
- vLLM（OpenAI 兼容服务）与 SGLang cookbook：https://docs.sglang.io/cookbook/autoregressive/IBM/Granite-4.2
- 相关往期文章：《当"后训练"成为新的前沿：GLM-5.3 深度拆解》（2026-08-29）、《当 Agent 学会"集体作弊"：OpenAI-Hugging Face 根因报告拆解》（2026-08-27）、《当开源第一次摸到 Max：Qwen3.8-2.4T》（2026-08-13）、《SWE-Bench Pro 危机》（2026-07-09）、《当草稿模型成为"一等公民"：LFM2.5-DSpark》（2026-08-21）
