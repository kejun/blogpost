# 当草稿模型成为"一等公民"：LFM2.5-DSpark 深度拆解——从 H100 的 3.2× 加速到 MacBook 上的 Agent

**日期：** 2026-08-21
**标签：** DSpark, Speculative Decoding, LLM Inference, LFM2.5, 端侧 Agent, DeepSeek-V4, llama.cpp, SGLang

---

## 一、引言：我们已经"不差钱"，但 Agent 还是慢

2026 年的推理基础设施有一个奇怪的割裂：**算力过剩与体验落后并存**。

数据中心里 H100/B200 的利用率依然挣扎在低位（我们 7 月底写过：GPU 利用率危机是调度问题，不是硬件问题）；而另一边，端侧 Agent——跑在 MacBook、手机、边缘盒子上的那些——每生成一个 token 依旧要等几十毫秒，函数调用链一长，用户能明显感觉到"它在思考"。

问题的根源从未改变：**解码阶段是内存带宽瓶颈，不是算力瓶颈**。

```
一次 decode 前向传播的时间去向（典型小模型，batch=1）：

┌──────────────────────────────────────────────┐
│  从 DRAM 搬运权重到 SRAM   ████████████████░ ~85% │
│  实际矩阵计算             ███░░░░░░░░░░░░░ ~15% │
└──────────────────────────────────────────────┘

串行生成 N 个 token = 搬运权重 N 次
```

权重搬运是固定成本，计算是边际成本。所以过去两年所有推理加速工作的本质，都在回答同一个问题：**能不能让一次权重搬运，产出更多 token？**

投机解码（Speculative Decoding）是这个问题的标准答案——让一个轻量草稿模型先猜一串 token，目标模型一次前向传播批量验证，猜得越多、搬得越值。而 2026 年 8 月 20 日，Liquid AI 发布的 **LFM2.5-DSpark**，把这件事推到了一个新的阶段：**草稿模型不再是内部优化细节，而成为与主模型并驾齐驱的"产品"**——有独立的 checkpoint、独立的 GGUF、独立的生态支持（llama.cpp / SGLang day-one），甚至有专门为它设计的训练框架（DeepSeek 开源的 DeepSpec）。

这篇博客拆解三件事：DSpark 到底比前人强在哪；LFM2.5-DSpark 的数据说明了什么；以及"草稿模型产品化"对 Agent 基础设施意味着什么。

---

## 二、投机解码谱系：从 EAGLE-3 到 DSpark，三代人的接力

投机解码的演进脉络我们在 DFlash 那篇里梳理过，这里只做个快速定位：

| 世代 | 代表 | 草稿生成方式 | 核心瓶颈 |
|------|------|-------------|---------|
| 第一代 | 独立小模型（2023） | 纯自回归 | 小模型"盲猜"，接受率低 |
| 第二代 | EAGLE-3（2025） | 自回归 + 目标模型特征注入 | 草稿延迟随块长度线性增长 |
| 第三代 | DFlash（2026） | **并行**：单次前向传播出全部草稿 token | 块内 token 相互独立 → 后缀接受率快速衰减 |
| 3.5 代 | **DSpark（2026.07）** | **半自回归**：并行骨干 + 马尔可夫序列头 | 解决并行草稿的"后缀衰减"与"验证浪费" |

DFlash 证明了"并行草稿"的可行性：草稿延迟不再随块长线性增长，理论上可以生成很长的草稿块。但并行预测每个位置相互独立，导致一个实际问题——**多模态碰撞**：多个位置的分布相互冲突，前面的 token 猜对了，后面的接不上，接受率随位置迅速衰减。

DSpark（arXiv 2607.05147，DeepSeek-AI 与北大合作）用三个组件回应了这个问题：

**① DFlash 式并行骨干**：以目标模型的上下文特征为条件，单次前向传播产出所有草稿位置的隐藏状态——继承并行草稿的低延迟优势。

**② 轻量马尔可夫序列头**：在并行骨干之上，加一个只建模相邻 token 依赖的序列模块。它不重新计算全部隐藏状态，只是给块内 token 补上"前后文关系"。论文的关键洞察是：**你不需要完全自回归才能建模依赖，一阶马尔可夫就够了**——大多数文本的局部连贯性来自相邻 token，而不是深层的 long-range 依赖。

**③ 置信度调度验证器（Confidence-Scheduled Verification）**：这是 DSpark 最"系统化"的创新，也是之前所有草稿方法忽略的地方。传统投机解码对每个请求一视同仁地验证整块草稿；DSpark 则对每个草稿 token 预测"生存概率"，**当某条后缀的验证成本超过它可能带来的收益时，主动剪枝**。验证长度不再是静态的块大小，而是根据前缀生存概率和引擎吞吐特性动态决定。

生产数据的说服力在这里：部署在 DeepSeek-V4 的在线服务中，对比既有生产基线 MTP-1，**在相同吞吐下每用户生成速度提升 60%–85%**；更重要的是，它把服务系统推到了以前够不着的"严格交互性约束"区间——论文原话是"shifting the Pareto frontier of our serving system"。

一句话总结 DSpark 论文的贡献：**前人的投机解码优化的是"草稿质量"，DSpark 第一次把"验证成本"也纳入了优化目标**——这是从纯算法问题走向系统工程问题的标志。

---

## 三、LFM2.5-DSpark：把草稿模型当产品做

8 月 20 日 Liquid AI 的发布，是 DSpark 论文的第一次大规模工业落地：为 LFM2.5 家族三个模型（1.2B-Instruct、2.6B、8B-A1B）训练并发布了配套草稿模型。

### 3.1 训练细节里的"反直觉"选择

草稿模型全部是 **5 层、约 300M 参数**的纯注意力架构（无 MoE），块大小固定为 9：

| 组件 | LFM2.5-1.2B | LFM2.5-8B-A1B | LFM2.5-2.6B |
|------|------------|--------------|------------|
| 解码骨干（5 层） | 241.2M | 241.2M | 241.2M |
| 隐藏状态投影 | 21.0M | 21.0M | 21.0M |
| 马尔可夫头 | 33.6M | 65.5M | 65.5M |
| Norms + 置信度头 | 27.5K | 27.5K | 27.5K |
| **总计** | **295.7M** | **327.7M** | **327.7M** |

最值得注意的是训练策略：在覆盖 SFT、chat、代码、函数调用的混合数据上跑 15 个 epoch，然后**选择接受率最高、而不是 loss 最低的那个 epoch**。

这是一个很典型的"指标对齐"教科书案例。草稿模型的 loss 与投机解码的收益并不单调相关——loss 低说明它拟合得好，但拟合得好不等于"和目标模型的分布重合度高"。接受率才是直接决定 tok/s 的指标。Liquid AI 没有想当然，而是直接以最终目标（接受率）作为模型选择的依据。这个细节值得所有做推理优化的团队抄作业：**优化什么，就选什么指标**。

### 3.2 质量对等：不是"几乎无损"，是"构造性无损"

投机解码常被质疑"加速是不是拿质量换的"。LFM2.5-DSpark 的回答很硬：**贪心解码下，草稿 token 只有与目标模型分布一致才会被接受，被拒时由目标模型自己的 token 顶上——因此输出序列与纯目标模型贪心解码逐 token 相同**。benchmark 上的 pass@1 / exact match 不变，不是"实测没变"，而是数学上就不可能变。

这一点对生产环境极其重要：你不用为引入投机解码重新跑一遍评测、重新做回归测试。加速是白捡的。

---

## 四、数据深读：三个关键发现

先看完整基准（SGLang + 单卡 H100 80GB BF16；llama.cpp + Metal，M4 Max MacBook Pro FP16；batch=1，块大小 9，温度 0，五个数据集取均值）：

| 模型 | 平均接受率 (of 10) | H100 加速 | M4 Max 加速 |
|------|------------------|----------|------------|
| LFM2.5-2.6B | 4.81 | **2.67×**（323→864 tok/s） | **2.27×**（61→139 tok/s） |
| LFM2.5-1.2B-Instruct | 5.02 | **2.10×**（656→1384 tok/s） | **2.54×**（138→350 tok/s） |
| LFM2.5-8B-A1B (MoE) | 6.95 | **2.54×**（418→1074 tok/s） | **1.18×**（90→106 tok/s） |

单点峰值：8B-A1B 在 MATH500 上拿到 **3.18×**（428→1362 tok/s），2.6B 在 HumanEval 上 M4 端 2.63×（61→161 tok/s）。

### 发现一：MoE 的"验证成本陷阱"

8B-A1B 的数据最有信息量：接受率全场最高（6.95/10，MATH500 高达 8.27），H100 上 2.54× 表现亮眼——**但到了 M4 Max 上只有 1.18×，几乎等于没有加速**。

原因有两个层面：

1. **llama.cpp Metal 后端的 MoE 实现尚不成熟**——Liquid AI 原话："the current MoE implementation in llama.cpp's Metal backend"。
2. **更本质的：验证 k 个 token 会激活更多专家**。MoE 的 decode 延迟与激活的专家数成正比；投机解码一次验证 9 个位置的草稿，等于一次前向传播要带上 9 个位置的专家权重流量，比单步 decode 的权重搬运多得多。投机解码的收益公式是"省下的步数 × 每步权重流量"，对 MoE 来说，**每步省下的多，但验证步骤本身也更贵**——当硬件带宽不够时（端侧），验证成本直接吃掉收益。

这是一个普适的教训：**投机解码的收益高度依赖"验证一步"与"生成一步"的成本比**，而这个比例是硬件相关的。在数据中心 GPU 上成立的加速比，不能直接外推到端侧——MoE + 投机解码的组合尤其要实测。

### 发现二：接受率的方差比均值更重要

1.2B-Instruct 的平均加速 2.10× 看着稳定，但单看数据集：GSM8K 只有 1.67×，HumanEval/MATH500 却有 2.56×/2.87×——**方差高达 52%**。原因：1.2B 的接受率在不同文本分布上波动极大（MT-Bench 3.90 vs MATH500 6.02）。

对生产者的含义：**投机解码的加速是一个分布，不是一个数字**。如果你的负载集中在接受率低的分布上（比如大量逻辑推理类 prompt），收益会显著低于 benchmark 均值。上线前应该拿自己的流量分布做 A/B，而不是信 README 上的平均加速比。

### 发现三：端侧交互性阈值——139 tok/s 意味着什么

2.6B 在 M4 Max 上从 61 提到 139 tok/s，Liquid AI 特意点了一句：这超过了大多数专有云模型给用户的体验（~140 tok/s 量级）。也就是说，**端侧本地模型第一次在"交互性"这个维度追平了云端**——用户感知的"流畅度"不再有云/端之分。

配合函数调用场景的 **57% 平均延迟削减**（多工具场景），这直接改变了端侧 Agent 的产品形态：一个跑在用户自己电脑上的 Agent，工具调用链的每一步等待都能被感知，延迟从 100ms 级压到 40ms 级，是从"能用"到"愿意用"的差别。

---

## 五、生态信号：草稿模型成为"一等公民"

这次发布最值得关注的不是数字，而是**配套动作**：

- **llama.cpp day-one 支持**（PR #27383，含实验性 Metal kernel #27441）
- **SGLang day-one 支持**（PR #31041，基于官方 DSpark 实现）
- 草稿 checkpoint 同时发布 **Safetensors 和 GGUF** 两种格式
- 启动方式即插即用，块大小直接从 draft 的 config 读取：

```bash
# SGLang
python -m sglang.launch_server \
  --model-path LiquidAI/LFM2.5-2.6B \
  --speculative-algorithm DSPARK \
  --speculative-draft-model-path LiquidAI/LFM2.5-2.6B-DSpark \
  --speculative-draft-attention-backend flashinfer \
  --disable-radix-cache --mem-fraction-static 0.75 --port 30000

# llama.cpp
llama-server -m LFM2.5-2.6B-F16.gguf \
  -md LFM2.5-2.6B-DSpark-F16.gguf \
  --spec-type draft-dspark --spec-draft-n-max 10 --spec-draft-n-min 0 \
  -fa on -ngl 99
```

加上 DeepSeek 开源了 **DeepSpec**（speculative decoding 的算法驱动训练仓库）和 DSpark checkpoint，草稿模型从"实验室内部件"变成了**有训练框架、有格式规范、有推理后端、有独立版本号的开源资产**。

这是一个被低估的范式信号：**推理加速正在从"框架内置优化"走向"模型生态的一部分"**。就像量化格式（GGUF/AWQ）当年成为模型发布的标准附件一样，"官方草稿模型"可能正在成为开源模型发布清单里的默认项。对推理引擎开发者来说，这意味着投机解码的支持不再是"加分项"，而是兼容性红线；对模型厂商来说，草稿模型是体验竞争力的一部分——**速度第一次成为模型的"产品功能"而非"部署细节"**。

---

## 六、批判性审视：三个没被回答的问题

### 1. batch=1 的测量与现实的距离

所有加速数据都是在 batch=1、temperature=0 下测的。但 DSpark 论文自己指出，**在高并发服务系统中，验证长草稿块会占用宝贵的批处理容量**——这正是 DSpark 设计置信度调度验证器的动机（论文里 DeepSeek-V4 的 60-85% 加速是在真实流量下测的，但 LFM2.5 这边的数字还停留在单请求实验室环境）。线上 batch 大、有 shared prefix（radix cache 被显式禁用了）时，加速比会如何衰减？Liquid AI 还没给出答案。

### 2. "吞吐"不等于"延迟"

tok/s 翻倍听着很美，但对单次交互来说，用户感知的是 TTFT + 完成时间，而 tok/s 是稳态吞吐。对短输出（工具调用参数、代码补全片段），投机解码的收益会被草稿生成的额外开销稀释——1.2B 在 MT-Bench 上只有 1.66× 已经暗示了这一点。**短线场景要用延迟指标重新测，别拿吞吐数字拍板**。

### 3. 谁为草稿模型付钱？

300M 的草稿模型训练成本不高，但它是**每发布一个主模型就要配套训练一次**的持续成本，加上多一套 checkpoint 的分发、存储、兼容性维护。对开源社区这不是问题（价格为零），但对商业 API 厂商，这意味着推理栈里多了一个需要持续投入的组件。更微妙的是：**草稿模型的训练数据要贴合目标模型的分布**，如果主模型频繁迭代（现在前沿模型几乎每月一更），草稿模型的追赶本身就是一个工程负担。

---

## 七、对 Agent 基础设施的启示

回到开头的问题：Agent 为什么慢？答案正在从"模型不行"转向"推理栈没有为交互性优化"。

LFM2.5-DSpark 这次发布给出了 Agent 基础设施的三个明确方向：

1. **端侧 Agent 的体验线已经清晰**：130-140 tok/s 是"追平云端"的交互性阈值，投机解码（尤其配合块长度动态调度）是跨过这条线成本最低的手段。函数调用延迟 -57% 意味着"多步工具链"的端侧产品从 demo 变成可用。

2. **加速技术必须按硬件分层设计**：MoE 模型在端侧不该直接套用数据中心的投机解码配置——8B-A1B 在 M4 上的 1.18× 是最好的反面教材。设计原则是：**先测"验证一步/生成一步"的成本比，再决定要不要投机、投机多长**。

3. **速度正在成为模型生态的竞争维度**：当质量差距缩小（开源模型连续多月追平前沿），"官方草稿模型 + day-one 引擎支持"会成为开源模型发布的标准配置。推理优化从"部署者的活"变成"模型厂商的活"——这对所有做推理基础设施的人来说，既是威胁（框架必须跟上），也是机会（草稿模型训练本身成了一门手艺）。

投机解码诞生三年，终于走到了"开箱即用"的阶段。下一个值得关注的问题是：**当草稿模型本身也变成可路由、可调度的资源时，它与模型路由、负载感知调度之间会发生什么化学反应**——那可能是推理基础设施的下一场戏。

---

## 参考

- Liquid AI: [LFM2.5-DSpark: Up to 3.2x Faster Inference from H100 to MacBook](https://huggingface.co/blog/LiquidAI/lfm25-dspark)（2026-08-20）
- Yu et al. (DeepSeek-AI / Peking University): [DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation](https://arxiv.org/abs/2607.05147)（arXiv 2607.05147）
- EAGLE-3（arXiv 2503.01840）、DFlash（arXiv 2602.06036）
- DeepSeek-AI: [DeepSpec](https://github.com/deepseek-ai/DeepSpec) 训练仓库与 [DeepSeek-V4-Pro-DSpark](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro-DSpark) checkpoint
- llama.cpp PR [#27383](https://github.com/ggml-org/llama.cpp/pull/27383)、SGLang PR [#31041](https://github.com/sgl-project/sglang/pull/31041)