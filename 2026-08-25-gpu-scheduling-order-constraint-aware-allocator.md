# 当"顺序"成为算力：同一集群、同一负载，只改分配顺序就多出 33 个百分点利用率——Dharma-AI 约束感知 GPU 分配器深度拆解

**日期：** 2026-08-25
**标签：** GPU Scheduling, Constraint-Aware Allocator, AI Infrastructure, Utilization Economics, FIFO, NP-hard, Demand Forecasting, Yield Management, Dharma-AI, Real-time Inference

---

## 一、引言：诊断之后，处方来了

三周前（7 月 31 日），我写过一篇《GPU 利用率危机》（[2026-07-31-gpu-utilization-crisis-ai-infrastructure-scheduling-paradigm.md](2026-07-31-gpu-utilization-crisis-ai-infrastructure-scheduling-paradigm.md)），拆解了 Dharma-AI 在 Hugging Face 博客发布的第一篇文章 *"GPU Management: Why Idle GPUs Are the New Grounded Aircraft"*。那篇文章的核心论点只有一句：**利用率，而非智能，才是企业 AI 的下一个真正约束。** 当时的结论是：这个行业不缺卡、不缺模型，缺的是一套成熟的"GPU 管理实践"（GPU Management practice）——而作者在文末坦诚，**还没有人写出来这本 playbook**。

8 月 17 日，续篇来了。标题本身就是一则新闻：

> **"Same Cluster, 33 Points More Utilization: What Changed Was the Order"** —— 同一个集群，利用率多出 33 个百分点，改变的只是"顺序"。

开篇三句话，每一句都值得划线：

> "我们构建了一个**约束感知的 GPU 分配器（constraint-aware GPU allocator）**，在七个基准场景中与 FIFO 调度器对比。**同样的硬件、同样的负载**，GPU 利用率最高提升了 33 个百分点，优先级加权产出（priority-weighted output）在每一个场景中都有提升，最高达 105%。**硬件什么都没变。变的只是分配决策被做出的顺序。**"

这不是一篇吐槽贴，也不是理论推演——它是一份**处方**：把 GPU 分配问题写成数学，用一个 1-2 毫秒就能跑完的启发式求解器，在真实基准上把"半闲置的集群"变成"87% 利用率的集群"。而这背后藏着一个更深的命题：**当模型能力不再稀缺，算力成为固定折旧资产，调度顺序就成了新的算力来源。** 这就是今天这篇文章要拆的东西。

---

## 二、"让 GPU 忙起来"不是决策：问题的形式化

先看作者怎么重新定义问题。他们说了一句反直觉的话：

> "'让 GPU 忙起来'（Keep the GPUs busy）不是一个系统可以执行的决策。决策更窄、也难得多：**哪块 GPU 在哪个时间步（timestep）跑哪个任务、以什么优先级。**"

形式化之后，这个问题是一个**布尔网格**：每个 (GPU, job, timestep) 组合对应一个二元选择，输出是一整张表——调度视野内的每一块 GPU、每一个时间步，格子里要么是一个任务名，要么是空。调度器不是在"排队"，它是在**填一张网格**。

难点在负载的异质性。有四种负载在争夺这张网格：**训练、实时推理、批量推理、量化**。它们分成两种互不相容的"形状"：

- **批式形状（batch-like）**：训练、批量推理、量化一旦启动，就需要一块**连续的 GPU 块**，占用期间不可打断，直到任务结束。
- **弹性形状（elastic）**：实时推理恰恰相反，它由一条**逐时间步变化的需求曲线**驱动，流量涨它就涨、流量跌它就跌。

"两种不相容的形状在同一时间步争抢同一块硬件"——这是整个问题的核心。而且异质性不止跨类型存在，类型内部也有：同一个基础模型的训练任务，时长从几小时到几天、规模从 1 卡到几十卡不等。

记住这个形式化，后面所有设计都从它推导出来。

---

## 三、FIFO 的两笔隐性账单：保留与排序

作者对 FIFO（先来先服务）的批评非常克制：在集群有闲余时，FIFO 是合理策略——反正什么都放得下，顺序无关紧要。**只有在争抢（contention）发生时，排序的代价才从隐形变成实打实的容量损失。** 这笔代价分两笔账单：

**账单一：保留（reservation）。** 实时推理不能等容量——流量来的那一刻 GPU 必须在。FIFO 没有机制在低谷释放 GPU、在高峰前收回，所以唯一的保底方案是：**按每个实时应用当天的最高需求，把 GPU 锁一整天。** 一个中午要 6 卡、凌晨 4 点只要 2 卡的应用，会 24 小时占着 6 卡——那 4 张闲置卡，对任何批式任务来说一整天都不存在。"它们既没被使用，也不自由。"

这就是为什么两个基准场景的基线利用率只有 **51.6%（混合负载）和 53.6%（训练密集）**——**接近一半的集群，不是"空闲"，而是"被保留"**。而且这笔账不管你争不争抢都得付，争抢只是让它显形。

**账单二：排序（ordering）。** 在真实争抢下，"哪些任务能放进去"取决于放置顺序，而不只是容量够不够。作者的原话值得裱起来：

> **"顺序不是一个容量问题解决之后才应用的平局决胜器（tiebreaker）。顺序本身就是容量决策。"**

FIFO 按到达顺序放置任务：不衡量这个任务值多少钱，也不检查视野内还有没有别的任务要放。于是高优先级工作排在"先来的"后面，容量被先到者以"后来者无法使用"的方式commit掉。作者用了一个延续前篇的比喻：**这就像航空公司把飞机分配给先打电话的包机客户，结果真正赚钱的航线一架飞机都不剩。** 而按峰值需求锁一整天的 GPU，就是上一篇文章里"停飞的飞机"最字面意义的再现——随时待命、不赚一分钱、别人也用不了。

两笔账单叠加：为"一天的最高峰"锁住的块，每小时都在把批式任务挡在门外，而剩下的容量又按"谁先到"发放。**这就是半闲置集群的完整成因。**

---

## 四、把调度写成数学：五条约束、两项目标、一个数字

作者的立场很明确：**答案不是"更长的启发式规则清单"。** 有些约束只在全局层面有意义，任何局部规则都表达不了：连续块、整个调度视野内的 GPU 变动（churn）预算、运行中任务不可抢占。要兑现这些约束，"问题必须被写成一件东西"——一个形式模型。

合法的分配由五条约束定义：

1. 每块 GPU 每个时间步最多服务一个任务；
2. 每个任务遵守其需求范围，已在运行的任务继承并保持；
3. 批式任务占用**连续** GPU 块，块大小取 2 的幂（power of two）；
4. 实时任务在相邻时间步之间可交换的 GPU 数量有**硬上限**；
5. **已启动的任务不可中断。**

目标函数只有两项：

- 把 GPU 分配给批式任务，获得 **优先级 × 时间衰减权重** 的奖励；
- 实时需求未满足，按**缺口大小**计入惩罚。

注意那个时间衰减权重——作者解释得很清楚：这是一个在线系统，下一次调度运行会有新任务到达，**"现在能被使用的容量，比将来被承诺的容量更值钱"**。这是把"新任务会来"这个事实直接编码进优化目标。

但整篇文章里最漂亮的一句话在这里：

> **"两个权重的相对大小，就是全部的服务级策略（service-level policy），被表达成一个数字。"**

实时惩罚权重是分配权重的 **5 到 10 倍**——一个单位的实时需求未满足，代价等于 5 到 10 个 GPU-时间步的同优先级批式工作。这个不对称是故意的：延迟义务被**放进同一个优化里执行**，而不是由一个跟调度器抢 GPU 的独立 autoscaler 来保证。也正是这个惩罚，让"弹性对待实时需求"变得安全——调度器敢在低谷把 GPU 给批式任务，因为"之后亏待实时需求"的代价被定价得远高于那点批式收益。**保护可用性的不是静态保留，而是惩罚条款本身。**

这就是整篇文章最反直觉、也最深刻的一点：**服务等级协议（SLA）不是一堆规则，而是一个数字。**

---

## 五、架构：热路径启发式 + 形式模型，"合法"是设计出来的

问题被写成了数学，但形式模型不能直接上生产：这是 **NP-hard 的组合分配问题**，而调度器**每来一个任务就要重新求解一次**——决策必须在一个 API 请求和下一个请求之间的间隙里返回。这个延迟预算，成了整个架构的固定约束，也决定了分层：

- **热路径上跑一个启发式求解器**，1-2 毫秒返回；
- **形式模型退居幕后**，作为启发式必须满足的"规范说明书"。

关键在这里：这个启发式**不是通用的贪心算法**。它的规则就是形式模型的结构性约束，所以**它产生的每一张网格，构造上就是合法的分配**。作者玩了个文字游戏但非常精准：*"不是通常合法（not usually valid）——是设计上合法（valid by design）。"*

那利用率提升到底从哪来？作者的解释是"视野"：启发式**在放置任何任务之前，能看到队列里的每一个任务**；它可以把空闲池**保持成剩余工作实际能占据的形状**——一个需要特定大小连续块的批式任务，轮到它时依然有位置。优先级决定谁先claim这块空间。FIFO 两个视野都没有：它把容量commit给先来的任务，后来者需要的形状可能已经不存在了，于是任务排不上、它本会消耗的 GPU 小时无人认领。

性能数据：五个争抢场景下 1-2 毫秒，64 GPU / 30 任务的扩展测试 15 毫秒——**足以在每一个请求上运行**。系统还暴露双模式：fast 模式只跑启发式返回网格（热路径）；full 模式把网格作为起点交给形式模型尝试改进（适合周期性复查，而不是逐请求决策）。

---

## 六、数据说话：七个场景，一个故事

先上全表（数据来自原文，全部为相对 FIFO 基线的提升）：

| 场景 | 利用率 | 优先级加权价值 | 价值增益 | 延迟 |
|------|--------|--------------|---------|------|
| 混合控制 (8 GPU, 10 jobs) | 51.6% → 72.4% | 7,093 → 10,980 | **+54.8%** | 1 ms |
| 实时争抢 (8 GPU, 8 jobs) | 75.0% → 80.2% | 3,233 → 4,029 | +24.6% | 1 ms |
| 训练密集 (8 GPU, 16 jobs) | 53.6% → 87.0% | 8,553 → 17,545 | **+105.1%** | 2 ms |
| 大型混合 (14 GPU, 16 jobs) | 76.8% → 82.7% | 13,977 → 20,101 | +43.8% | 2 ms |
| 超额认购 (8 GPU, 9 jobs) | 85.4% → 87.5% | 4,311 → 5,760 | +33.6% | 1 ms |
| 扩展测试 (64 GPU, 30 jobs) | 44.9% → 44.9% | 44,233 → 51,248 | **+15.9%** | 15 ms |
| 统一优先级 (14 GPU, 16 jobs) | 76.8% → 87.5% | 25,219 → 31,052 | +23.1% | 2 ms |

七个场景，利用率只在一个场景持平，价值全部提升。三个特别值得 mark 的点：

**① 最强案例：训练密集场景，价值翻倍。** 利用率 53.6% → 87.0%，优先级加权价值 +105.1%——"一个固定的、已经在折旧的资产（a fixed, already-depreciating asset），被找回了 33 个百分点"。注意作者的措辞：**"already-depreciating"**——GPU 从买入那一刻起就在折旧，闲置一天不是"没赚"，是"纯亏"。

**② 扩展测试：利用率持平，价值照样 +15.9%。** 30 个任务、64 块 GPU，FIFO 和分配器的利用率一模一样（44.9%）、吞吐一模一样（27/30 完成），但分配器多交付了 15.9% 的优先级加权价值。**每一个仪表盘读出来都一样，集群产出的东西却实质性地不同。** 作者在这里给出了全篇最锋利的断言：*"一个不给优先级定价的目标函数，可以把集群填到完全相同的水平、完成完全相同的任务数，交付却更少。占用率不是集群是否在赚钱的良好指标——这是上一篇文章那个主张的实测版本。"*

**③ 消融实验：统一优先级，收益依然存在。** 这是作者预判了"你的增益就是按优先级排序"这个最显而易见的质疑：把所有任务的优先级设成完全相同，分配器依然把利用率从 76.8% 推到 87.5%、价值 +23.1%。**结论：增益不纯是"按优先级排序"的产物——跨整个时间视野做规划（planning placements across the horizon）本身就有贡献。**

也就是说，收益来自两个正交的来源：**把容量留给正确的形状（视野规划）+ 把容量给正确的人（优先级）**——缺一不可，且可分离验证。这种消融设计，在工程博客里是教科书级的严谨。

---

## 七、被低估的一半：调度器只是预测器的仆人

文章最后一部分的标题值得单独翻译：*"None of it works if the demand numbers are wrong"*——**如果需求数字是错的，这一切都不成立。**

调度器假设它知道每个任务需要多少 GPU 小时、实时流量会来多少。但**这两者都是预测，不是输入**。而且一个通用的估计器行不通——四种负载的成本驱动因素在性质上就不同。这正是上一篇文章"专业化"论点的回归：**让任务专用模型击败通用模型的逻辑，同样适用于喂给调度器的估计器。**

- **训练不是一种负载。** 它沿两条独立、可自由组合的轴变化：策略（全量微调 vs LoRA 等参数高效方法——LoRA 最多能把可训练参数砍掉 **10,000 倍**、显存省约 **3 倍**）和技术（SFT、DPO、RLHF、RLVR、CPT——DPO 直接去掉奖励模型和 RLHF 的采样循环）。**只按模型大小估计，会把相差数量级的运行平均在一起**——恰好平均掉调度器最需要决策的两个量：时长和卡数。他们的训练预测器条件在 **22 个特征**上，其中包含一个区分 **10 种具体训练变体**的分类变量。

- **量化是可调度任务，不是后台杂务。** 量化一个大模型可能消耗数小时 GPU 时间，而这些硬件正是别的任务在等的。它有自己的预测器：按参数量的校准层级（calibration tiers）构建，按算法分别处理（bitsandbytes、AWQ、GPTQ），并在取整到整 GPU 小时之前加上安全边际。作者补了一句：**此前的相关工作把量化完全排除在调度范围之外。**

- **实时推理根本不按任务估计。** 它是一个**持续重校准的每周需求画像**：从每小时流量历史重建，并在形式模型强制执行的同一个 swap 成本假设下映射到 GPU 数量。**预测器和优化器因此对"变动（churn）值多少钱"达成一致，而不是各说各话、互相打架。** 这个画像，就是替代"峰值保留"的东西——一条逐时间步的需求曲线，是让第三节那笔"保留账单"消失的唯一前提。

这一节的潜台词是：**调度器的上限由预测器的质量决定。** 优化做得再好，预测错一天的需求，一切归零。AI 基础设施的工程瓶颈正在从"求解"转移到"预测"——这和我们 7 月 14 日写微软遥测时说的"数据成为新瓶颈"（[2026-07-14-microsoft-cli-agent-telemetry-roi-data-bottleneck.md](2026-07-14-microsoft-cli-agent-telemetry-roi-data-bottleneck.md)）是同一个故事的两个侧面。

---

## 八、从占用率到收益率：GPU 调度正在重演航空业 40 年的剧本

现在把视角拉远。作者用航空业做比喻，但这个比喻比他们自己意识到的还要贴切——因为**航空业不仅遇到了同样的问题，还在 40 年前就发明了完整的答案，名字叫"收益管理"（revenue management / yield management）**。

1980 年代，美国航空率先部署计算机化的收益管理系统（Super Sabre）：核心洞察是**同一个座位，卖给不同的人值不同的钱**，所以"把座位填满"不是目标，"把座位卖给最愿意付钱的人"才是。为此他们开发了超额预订（overbooking）、容量控制（capacity control）、随后又演化出**投标价格（bid price）**——对每一单位剩余容量，算出一个"最低可接受的边际价值"，低于它就不卖。据行业广泛引用，这套系统在 1989-1991 三年间为美航带来了约 14 亿美元的增量收入。今天全球航空业每年靠收益管理多赚的钱以百亿计，"先到先得"的订票时代早已成为历史。

把这张对照表放在一起看：

| 维度 | 航空收益管理 | Dharma-AI 分配器 |
|------|-------------|-----------------|
| 核心洞察 | 座位填满 ≠ 收入最大化 | 利用率 ≠ 价值最大化 |
| 定价对象 | 同一座位的边际价值 | 同一 GPU 时间步的边际价值 |
| 工具 | 投标价格、容量控制 | 优先级 × 时间衰减权重、实时惩罚 |
| 关键前提 | 需求预测 | 需求预测（22 特征训练预测器、周需求画像） |
| 旧范式 | 先到先得 | FIFO |

这不是巧合，是同一道数学题在另一个行业重演：**固定、不可存储、按时折旧的产能（座位/GPU）+ 波动需求 + 多种价值等级的工作 = 必须给"顺序"定价。** 扩展测试里"利用率相同但价值差 15.9%"的案例，就是航空业那句老话的 GPU 版：**"负载因子（load factor）是必要的，收益率（yield）才是赚钱的。"**

顺着这个类比往下推，能看到接下来的剧本：航空业在 bid price 之后，走向了**动态定价和收益管理系统与机票分销渠道的全面整合**。GPU 调度大概率会沿着同样的弧线演进：**从"按优先级排队"到"给 GPU 时间步算投标价格"，再到 GPU 时间本身的内部市场**——训练任务的 owner 和实时推理的 owner 在同一个调度器里用"价值信号"竞价，而不是靠一个静态权重。事实上，目标函数里"5-10 倍惩罚权重"就已经是 bid price 的雏形：**它在给"满足实时需求"这件事定一个影子价格。**

而对我们这些做 Agent 基础设施的人，还有一个更近的推论：**Agent 时代的推理负载本身就是弹性的实时负载**——一个 Agent 在思考间隙的 GPU 需求忽高忽低，和"中午 6 卡、凌晨 2 卡"的流量曲线同构。混合负载（批量训练 + 弹性推理）会成为所有 AI 平台的默认形态，谁能先跑通"约束感知 + 预测驱动"的分配器，谁就拥有了第一批"GPU 收益管理系统"。

---

## 九、结论：五条可执行的启示

1. **把"顺序"当资产来管理。** 你不需要买更多卡：同一集群同一负载，只改分配顺序就有 33 个百分点的利用率差距。FIFO 是默认选项，不是最优选项——在争抢场景下，它每时每刻都在为"先来"和"峰值保留"付费。

2. **用"优先级加权价值"替代"利用率"作为集群 KPI。** 扩展测试证明：利用率可以完全一致，产出可以差 15.9%。所有盯着 occupancy 的仪表盘都在测错误的数字——加上 yield（单位 GPU 时间的价值产出）。

3. **把 SLA 写成一个数字，而不是一堆规则。** 实时惩罚权重 vs 批式奖励权重的比值，就是完整的服务级策略。这让延迟义务和批式放置可以在同一个优化里被满足，而不是靠一个和调度器抢资源的 autoscaler。

4. **先投资预测，再投资优化。** 调度器只是预测器的仆人：训练按 22 特征分 10 种变体预测、量化按算法分别预测、实时推理用周级需求画像——通用估计器会把数量级的差异平均掉。预测不准，优化再好也归零。

5. **警惕"忙碌"的幻觉。** "Keep the GPUs busy" 不是一个可执行的决策，就像"把座位填满"不是航空业的决策一样。**占用是手段，价值才是目的。** 航空业花了 40 年学会这件事，GPU 集群不需要再花 40 年——剧本已经写好了。

---

*参考来源：Dharma-AI《GPU Management: Why Idle GPUs Are the New Grounded Aircraft》(2026-07-30) 与《Same Cluster, 33 Points More Utilization: What Changed Was the Order》(2026-08-17, Hugging Face Blog)；本文前篇《GPU 利用率危机》(2026-07-31)。航空收益管理历史数据为业界广泛引用之说法，文中数据均转述自原文，观点为作者独立判断。*