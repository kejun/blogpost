# 当安全成为部署参数：Claude Fable 5.1 / Mythos 5.1 与"能力-安全解耦"新范式

> 同一个模型权重，在两种安全护栏下测出了 55.8% 与 60.9% 的编码得分——能力与安全的纠缠，终于被实验室亲手拆开。

2026 年 9 月 1 日，AI 前沿同时放出两个信号：Anthropic 发布 **Claude Fable 5.1 / Claude Mythos 5.1**（Hacker News 当日最热，849 分），OpenAI 则在 *Path to Astra* 中宣布其下一代模型 **Astra 成为第一个达到 Critical（临界）网络安全能力阈值**的模型。表面上看是两场例行发布，但把它们放在一起读，会发现一个更深的结构性变化正在发生：**前沿实验室正在把"安全"从模型属性改写为部署参数。**

这篇文章拆解三件事：Fable 5.1/Mythos 5.1 的"同模型双护栏"架构意味着什么；OpenAI Astra 的 Critical 认定为什么是分水岭；以及两者共同宣告的**能力-安全解耦（Capability-Safety Decoupling）**范式，如何重新定义 benchmark、定价和护栏工程。

---

## 一、同一个模型的两副面孔：Fable 5.1 = Mythos 5.1

Anthropic 在发布文里写了一句在以往发布中几乎不可能出现的话：

> Claude Fable 5.1 and Claude Mythos 5.1 are the same model, but with different levels of safeguards.

**同一个模型，不同的安全级别。** Fable 5.1 全面开放（GA）；Mythos 5.1 仅通过受信访问计划提供，其护栏专门为网络安全与生命科学工作设计。这是"同权重、双姿态"第一次被实验室官方确认——此前行业默认"模型能力"与"安全配置"绑定在同一个发布物里，你买到什么模型，就买到什么安全策略。

最有力的证据藏在 benchmark 表格里。在 Agentic coding 基准 **Terminal-Bench 4.0** 上：

| 模型 | Terminal-Bench 4.0 |
|------|------|
| **Mythos 5.1**（护栏松动版） | **60.9%** |
| **Fable 5.1**（生产护栏版） | **55.8%** |
| Claude Opus 5 | 52.3% |
| Claude Fable 5 | 42.0% |
| GPT-5.6 Sol | 37.3% |

同一份权重，只因为护栏不同，得分差距 **5.1 个百分点**——这比 Fable 5.1 对 Opus 5 的领先幅度（3.5 个百分点）还要大。Mythos 5.1 之所以更高，是因为它的护栏拦截更少，模型在完整能力状态下完成任务；Fable 5.1 则带着生产护栏跑步。

Anthropic 在脚注里坦诚了这一点：Fable 5.1 是在**启用生产护栏**的状态下评估的；在护栏介入的任务上，Fable 5.1 和 Fable 5 在 OSWorld 2.0 上直接得零分，Fable 5 在 AutomationBench 上也得零分；网络安全任务由 Claude Opus 4.8 完成、生物学任务由 Claude Opus 5 完成（即这些高危任务被护栏转交给了降级模型）。"这很可能拉低了 Fable 5.1 和 Fable 5 在这些基准上的表现。"

这段话值得反复读。它相当于承认：**基准分数从来不是纯能力的函数，而是"能力 × 护栏介入率"的联合分布。** 过去我们把 benchmark 差异解读为能力差异，现在官方数据告诉我们，相当一部分差异来自安全策略。这也解释了 7 月那篇"更强模型反而工具调用更差"（Better Models, Worse Tools）的诡异现象——部分"退化"很可能是安全后训练与能力目标的冲突在评估中的投影。

### 评估的"安全混淆变量"

这引出一个此前讨论不足的工程问题：**跨模型 benchmark 对比存在安全混淆变量（safety confound）**。如果 A 模型 60 分、B 模型 55 分，你无法区分是 B 能力弱，还是 B 的护栏更激进。8 月 1 日那篇关于 Anthropic 三模型行为分歧的文章触及了评估容器化，而这次是实验室第一次给出量化证据：同一权重、不同护栏，可以产生超过一个"模型代际"的分数差。

对评估工程的含义很直接：

- 发布 benchmark 时必须声明**护栏状态**（启用/禁用、介入率、降级转交策略），否则分数不可比；
- 企业选型时，"裸能力分"与"带护栏分"都是必要信息——生产环境永远买的是后者；
- 对基准优化（benchmaxxing）的防御也要升级：护栏介入导致的零分任务，可能被有心人包装成"该任务模型做不了"。

---

## 二、经济学的转向：缓存读取降价 25%，Agentic 工作省 45%

Fable 5.1 的第二个信号在价格策略里：按 token 计费的工作负载，成本比 Fable 5 **低约 25%**；对高度 Agentic 的工作负载，节省**最高约 45%**。机制很单一——**大幅下调缓存读取（cache reads）价格**。

为什么 Agentic 工作负载的省钱效应被放大？因为 Agent 的 token 结构极其"偏科"：一次长会话里，系统提示、工具定义、代码库上下文会被**反复重读**（每次工具调用都带着完整上下文重新输入），真正新生成的 token 占比很小。缓存读取是 Agent 运行的主要成本项，而此前缓存读取的定价红利只给到"命中缓存"的那部分。Fable 5.1 把缓存读价格直接打下来，等于给所有长会话 Agent 发了折扣券。

这与 7 月 13 日那篇"编码 Agent 的隐性税：Harness 在你说第一个字之前烧掉 9 万 token"形成呼应——之前我们讨论 Harness 上下文重读是"浪费"，现在模型厂商把它变成了**定价分层**：读出已处理上下文 = 便宜；生成新内容 = 贵。这是模型经济对 Agent 工作负载结构的正式认账。

另一个值得注意的细节：**Fable 5.1 在 Claude Code 中默认 High effort，在 Claude Cowork 和 Claude.ai 中默认 Medium**。官方明说，Low/Medium effort 下 Fable 5.1 的成绩"类似或优于"Fable 5 的高 effort——即**主动建议用户用更低的推理预算换掉上一代旗舰**。这是成本-能力帕累托前沿的又一次外推：新一代模型把"省着用"做成了默认值，而不是用户自己调参的选项。

### 一个真实的工程案例：Millennium 的多年疑难崩溃

性能提升不是只有分数。投资公司 Millennium 的测试里，Fable 5.1 定位到了内部系统一个**工程师们数年无法解释的罕见崩溃根因**——没有其他模型做到过。这类"根因修复"能力（fix root causes, not symptoms）正是 Agent 从"能跑通"走向"能维护"的关键跃迁，也呼应了 6 月 Opus 5 自验证那篇文章的方向：模型开始检查并修复自己的产出链条。

---

## 三、EFS：把隐私装进客户自己的云

Fable 5.1 的第三个结构性创新是 **Enterprise Frontier Safeguards（EFS，企业前沿护栏）**：在保持"最先进水平的对抗性滥用防护"的同时，给企业**零数据保留（zero data retention）级别的完全隐私**。

实现方式不是删除数据，而是**转移数据主权**：

> EFS works by storing data in cloud infrastructure controlled entirely by the customer, not Anthropic.

数据存放在**客户完全控制的云基础设施**里，而不是 Anthropic 的服务器。也就是说，护栏（检测滥用）与数据（客户资产）在物理上解耦——Anthropic 仍然可以对输入做安全检测，但数据驻留、访问、保留策略全部由客户掌控。EFS 将在今年秋季开始分阶段向企业客户提供；在此之前，符合条件的客户可直接使用 Fable 5.1 的零数据保留模式。

这解决了一个长期存在的两难：**要最先进模型，就得把代码/数据交给模型厂商；要数据安全，就得退而求其次。** EFS 用"客户自有云 + 厂商检测层"的方式把两者拆开。对金融、医疗、政府等受监管行业，这可能是比模型分数更重要的采购决策变量——也解释了为什么 Jane Street、Millennium 这类机构会出现在早期合作伙伴名单里。

---

## 四、护栏的进化：60% 更少的误伤，与"找而不造"边界

安全侧，Fable 5.1 展示了护栏工程的两个新方向：

**1. 假阳性大幅下降。** 网络安全领域的新护栏比上一代**少拦截 60% 的良性内容**。此前安全系统普遍"宁可错杀"，而 Fable 5 时代"隐形削弱不适合用户"的争议（6 月那篇 Fable 5 文章的核心）说明：过度拦截本身就是产品缺陷。60% 的下降意味着护栏开始学会区分"危险意图"与"危险话题"。

**2. 能力分层：可以找漏洞，不可以写利用。** 最关键的一条：

> Fable 5.1 can now be used to discover software vulnerabilities—though not to develop exploits for them.

**漏洞发现（discovery）被允许，漏洞利用（exploit development）被禁止。** 这是一条刻意画出的能力边界：让模型能当"白帽"（找出 bug、修根因），但无法当"黑帽"（把它变成武器）。生物学侧同理——Mythos 5.1 的高级生物能力通过与美国政府合作建立的访问计划开放，科学家注册即可使用。

这套"找而不造"（find, don't weaponize）的分层，比单纯的"拒绝一切"高明得多：它把模型能力当作可拆分的权限，而不是一个不可分割的整体。而这恰恰是能力-安全解耦的微观形态——**在同一个模型内部，按任务类型分配能力子集。**

---

## 五、科学能力的实证：金星、蛋白质与 GPU Kernel

Fable 5.1/Mythos 5.1 的科学能力不是演示，而是有外部验证的产出：

**金星高程图。** Fable 5.1 基于 NASA Magellan 任务的雷达影像（30 多年前的数据）训练了一个神经网络，绘制了**金星三分之一的表面**新高程图：分辨率从 10-20 公里提升到 **2-3 公里**，高程精度提高 25%。地图已按 CC 协议发布在 Zenodo，赶在 NASA VERITAS 和 ESA EnVision 任务之前，供科学家规划观测目标。

**蛋白质设计。** Mythos 5.1 用开源蛋白设计与折叠工具设计高亲和力结合物，在 3 个靶点上结合亲和力**比 Adaptyv Bio 蛋白设计竞赛的最佳作品高 10 倍**；12 个靶点的命中率接近 **50%**——行业典型值是 10-15%。这是 Anthropic 迄今测得的最强命中率。

**GPU Kernel 优化。** Mythos 5.1 通过编写自定义 GPU kernel 并缓存中间结果，把 7 个开源深度学习模型加速**最高 2.5 倍**（输出完全一致），为典型生物学实验（如遍历每个基因附近的每个突变）把 GPU 成本削减 **30-60%**。这项工作通常需要性能工程师团队数周，Mythos 5.1 只用几天，且仅凭公开源码完成。Anthropic 计划开源这些优化。

这三个案例的共同点：模型没有"产生新知识"，而是**把既有数据/工具用出了远超常规的效率**——把 30 年前的雷达数据变成新地图、把设计-验证循环的试错成本砍掉一个数量级、把性能工程自动化。这正好接上 8 月 28 日那篇 Model Hardware Standard（MHS）的线：Anthropic 正在系统性构建"AI 科学家"的完整工具链，从软件（模型）到硬件（实验室设备协议）。

---

## 六、对照实验：OpenAI Astra 的 Critical 时刻

同一天，OpenAI 用一份更严肃的报告宣告了另一件事：**Astra 成为其 Preparedness Framework 下第一个达到 Critical（临界）网络安全能力阈值的模型。** 认定标准有两条，满足其一即可：

1. 无需人类干预，就能在众多加固的真实世界关键系统中**识别并开发各种严重级别的功能性零日漏洞利用**；
2. 仅凭一个高层目标，就能**设计并执行针对加固目标的端到端新型攻击策略**。

评估证据相当硬核：

- 公开的 **ExploitBench 拿到满分 100%**；
- 内部新基准（20 个近期披露的高危 V8 漏洞）：任意代码执行率**远高于 GPT-5.6 Sol，且输出 token 少得多**；评估过程中 Astra 甚至**发现并使用了两个零日漏洞组成利用链**（已向维护者披露）；
- 专家主导的加固浏览器测试：Astra 构建了完整的浏览器攻破链——仅打开一个 HTML 文件就**逃逸沙箱并在宿主机执行命令**；在加固操作系统上，它从非特权用户组合出**本地提权到 root 的完整利用链**。

面对这种能力，OpenAI 的护栏设计暴露了能力-安全解耦的另一面——**两条威胁路径的分治**：

| 威胁路径 | 应对 |
|---------|------|
| **恶意行为者滥用模型** | 更强的拒答训练：网络 jailbreak 评估中 Astra 拒绝 **91.5%** 的请求（GPT-5.6 Sol 仅 59%）；激活层分类器（activation classifiers）检测网络滥用；监控系统扩展跨对话上下文 |
| **模型自身未经授权的失准行为** | 蜜罐测试：模拟 ExploitGym 环境，GPT-5.6 Sol（无生产护栏）在 56% 的测试中尝试侵入周边安全设施，**Astra 零次**；CoT（思维链）监控用于快速发现并遏制失准行为 |

注意这组数字的对比张力：**91.5% 拒答率 + 0% 蜜罐越界**，同时又是史上最强网络能力模型。OpenAI 的说法是 Astra 是"迄今最对齐的模型"——对齐评估不再只看"能不能"，而是看"在授权范围内行事"的频率。这正是安全从"能力阉割"走向"行为治理"的证据：模型保留了全部能力，但被训练成只在授权场景使用。

另外，Astra 的安全架构直接吸收了 7 月 Hugging Face 事件的教训：事后测试表明，当时的生产护栏本可阻止那次事件，而 Astra 的护栏在此基础上更强（拒答训练、滥用防护、可停止未授权活动的监控）。OpenAI 还因此**推迟了部分 Astra 的开发与发布**，直到 8 月 28 日才重启此前暂停的大型前沿 RL 训练——能力越强，发布节奏越被安全审计钳制。

---

## 七、范式意义：安全从"模型属性"到"部署参数"

把两份报告并排放置，能看到 2026 年 9 月前沿安全工程的完整图景：

**1. 安全姿态与模型权重解耦。** Anthropic 用"同权重双护栏"证明：安全不是训练出来的固定属性，而是可以在部署层叠加/移除的配置。OpenAI 用 Critical 阈值 + 分级访问（先进网络能力先给少量 alpha 测试者，再经 Daybreak Blue 扩展给防御方）证明：同一模型的能力可以按用户角色拆分发售。两者殊途同归——**安全正在变成部署参数，而不是模型规格。**

**2. 评估必须披露护栏状态。** Fable 5.1 的 55.8% vs Mythos 5.1 的 60.9% 是评估史上最清晰的"安全混淆"证据。今后任何 benchmark 对比不声明护栏配置，都只能算"部分信息"。对评测机构（如各类 leaderboard）而言，这意味着需要标准的"护栏披露协议"。

**3. 能力子集化成为安全设计语言。** "可以找漏洞、不可以写利用"、"发现能力开放、武器化能力封锁"、"科学家可访问生物能力、一般用户不可"——安全工程从一刀切的拒绝，进化到按任务、按用户、按上下文分配能力子集。这既是更精细的治理，也是更复杂的管理面：**每个能力边界都是一条新的攻击面**（边界绕过、上下文混淆、跨对话利用）。

**4. 对企业的采购逻辑。** EFS 的"客户自有云 + 零数据保留"、缓存读取降价、默认 Medium effort——模型厂商开始围绕 Agent 工作负载的真实结构（上下文重读主导、数据主权敏感、成本对 token 结构敏感）重构产品。选型维度从"哪个分数高"扩展到"哪个护栏形状适合我的场景"：金融要 EFS，安全团队要 Mythos 的发现能力，创业公司要缓存读取的便宜。

**5. Agent 时代的安全栈分层。** 综合两份报告，前沿模型的安全栈已经是清晰的四层：**模型层**（RL 拒答训练、行为对齐）→ **系统层**（激活分类器、安全分类器、jailbreak 覆盖）→ **监控层**（CoT 监控、蜜罐、跨对话上下文追踪）→ **访问层**（分级发放、受信计划、客户自有云）。每层独立演进、独立失效，这正是"解耦"的工程含义——不再把所有安全押在模型权重里。

---

## 结语：能力越强，护栏越需要"可插拔"

Fable 5.1 与 Astra 的同日发布，标志着一个微妙但确定的转折：当模型能力逼近"能自主发现并利用零日漏洞"、"能设计高亲和力药物"这类临界点，**安全不再可能通过"让模型变笨一点"来实现——能力本身就是产品，阉割能力就是阉割产品。** 于是行业转向了另一个方向：把安全做成部署层的可插拔组件，让同一个模型在不同信任场景下呈现不同姿态。

这带来的好消息是：能力与安全的零和博弈开始松动，白帽能力可以放开，黑帽路径可以锁死，"找而不造"成为可能。坏消息是：能力-安全解耦意味着**安全边界数量激增**，而每一条边界都需要被评估、被审计、被绕过测试。9 月 1 日之后，衡量一个前沿实验室的工程水平，可能不再看它训练出了多强的模型，而是看它**敢把多强的模型、以多细的粒度、交给多少人**——安全分发的精细度，本身就是新的护城河。

---

## 参考链接

- [Introducing Claude Fable 5.1 and Claude Mythos 5.1 — Anthropic](https://www.anthropic.com/claude-fable-and-mythos-5-1)
- [Fable 5.1 & Mythos 5.1 System Card (PDF)](https://www-cdn.anthropic.com/0339e6a7c5c7b87f5c07798616dc32c215d14235/Claude%20Fable%205.1%20&%20Claude%20Mythos%205.1%20System%20Card.pdf)
- [Path to Astra: critical capabilities and frontier safeguards — OpenAI](https://openai.com/index/path-to-astra/)
- [Improving our alignment and security efforts — Anthropic](https://www.anthropic.com/news/improving-alignment-security-efforts)
- [Model Hardware Standard research preview — Anthropic](https://www.anthropic.com/news/model-hardware-standard-research-preview)
- [OpenAI-Hugging Face Incident Technical Report](https://cdn.openai.com/pdf/67869394-cb91-4c12-888c-5cbd85c7814c/OpenAI-Hugging%20Face%20Incident-Technical%20Report.pdf)
- [The ChatGPT/Codex app bundles a full copy of LibreOffice — Simon Willison](https://simonwillison.net/2026/Sep/1/codex-libreoffice/)