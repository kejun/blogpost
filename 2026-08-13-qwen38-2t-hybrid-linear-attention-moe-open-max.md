# 当开源第一次摸到 "Max"：Qwen3.8-2.4T 深度拆解——Gated DeltaNet 混合架构与 512 专家 MoE 如何改写开源推理经济学

> 2026 年 8 月 12 日至 13 日，AI 圈的"模型超级星期二"：DeepSeek V4 Pro 0813（HN 695 分）、Qwen3.8-2.4T-A95B（459 分）、Grok 4.6（362 分）在 24 小时内接连发布。其中 Qwen3.8 的意义最为特殊——这是**开源社区第一次拿到 Max 级旗舰模型的完整权重**：2.4T 总参数、95B 激活、FP8 量化、混合线性注意力架构。当"旗舰"从 API 黑盒变成可下载的二进制文件，开源推理经济学被改写了。

---

## 一、先看事件：一个"模型日"而非"模型月"

过去一年，前沿模型的发布节奏是"每个月一个大新闻"。而 2026 年 8 月 12 日至 13 日的 24 小时，把这个节奏压缩成了"一天三个"：

| 模型 | 发布方 | 关键信息 | HN 热度 |
|------|--------|----------|---------|
| DeepSeek V4 Pro 0813 | DeepSeek | V4 家族点版本更新，社区实测成本极低 | 695 分 |
| Qwen3.8-2.4T-A95B | 阿里 Qwen | **开源首次放出 Max 级模型**：2.4T 参数 / 95B 激活 | 459 分 |
| Grok 4.6 | xAI | AA Intelligence Index 61 分，追平 GPT-5.6 Sol，主打长程 Agent | 362 分 |

HN 上有开发者用 Codex CLI 实测了其中两个：

> DeepSeek V4 Pro 0813：跑了 12 分 02 秒，花费 $0.12，**带 bug**。
> Grok 4.6：跑了 3 分 18 秒，花费 $1.41，**无 bug**。

12 倍的成本差距，换来的是"有没有 bug"的可靠性差距——这个 n=1 的样本不该被过度解读，但它精准地勾勒出今天模型市场的光谱：**价格曲线和可靠性曲线正在以不同的斜率下滑**，而夹在中间的开发者被迫学会"按任务选模型"。这恰好呼应了昨天那篇关于 NeMo Switchyard 模型路由的文章——当一天之内有三个旗舰级选择时，路由不再是优化题，而是必需品。

但三连发里，真正值得掰开揉碎的是 Qwen3.8-2.4T。原因很简单：DeepSeek 和 xAI 交付的是 API 端点，而 Qwen 交付的是**架构本身**。

## 二、2.4T 参数与 95B 激活：稀疏性的极限

先看一组数字：

| 指标 | 数值 |
|------|------|
| 总参数 | 2.4T（约 2,400B） |
| 激活参数 | 95B |
| 激活率 | **约 3.96%（≈25 倍稀疏）** |
| 层数 | 92 |
| 隐藏维度 | 8192 |
| 专家数 | 512（10 路由 + 1 共享） |
| 专家中间维度 | 2048 |
| 上下文 | 原生 262,144，可扩展至 1,010,000 |

2.4T 总参数是什么概念？2026 年 6 月的 GLM-5.2 是 744B MoE，Kimi K3 号称"全球最大开源模型"——而 Qwen3.8 直接把开源的上限推到了 2.4T，超过此前任何开源模型一个量级。但真正值得注意的不是"大"，而是"怎么用"：每生成一个 token，只有 95B 参数真正参与计算，激活率不到 4%。

这意味着两件反直觉的事：

1. **训练成本与推理成本脱钩**。2.4T 让你在预训练时"充分吸收知识"，95B 让你在推理时"只付 4% 的账单"。MoE 的本质是把知识存储密度和计算密度解耦——这是过去两年开源模型追赶闭源旗舰的核心工程杠杆，Qwen3.8 把它推到了新的极限。
2. **部署门槛进入"单机"区间**。95B 激活参数在 FP8 下约 95GB 权重，8×H200（单卡 141GB HBM3e）单节点即可承载——**开源旗舰第一次不需要多机多卡集群就能跑起来**。对比之下，满血版 2.4T 权重约 2.4TB，那是训练集群的玩具；而 95B 激活是任何有预算租 8 张卡的团队都能玩的。

## 三、Gated DeltaNet：混合线性注意力的工程化巅峰

这是整篇文章最值得深挖的部分。Qwen3.8 的隐藏层布局是一个精心设计的混合结构：

```
23 × (
    3 × (Gated DeltaNet → MoE)    ← 线性注意力层（每 4 层中的 3 层）
    1 × (Gated Attention → MoE)   ← 全注意力层（每 4 层中的 1 层）
)
```

用自然语言翻译：每 4 层为一个"超级块"，其中 3 层用**线性注意力**（Gated DeltaNet），1 层用**全注意力**（Gated Attention），每一层都配 MoE。92 层 = 23 个超级块。

### 为什么是线性注意力？

自注意力的问题众所周知：KV cache 随序列长度线性增长，256K 上下文下动辄数百 GB 显存，长程 Agent 的"记忆税"高到离谱。线性注意力（linear attention）用固定大小的循环状态替代 KV cache，把"记住整个历史"变成"把历史压缩进一个状态向量"——内存占用 O(1) 与序列长度无关。

Gated DeltaNet 是这条研究线的最新节点：它在 DeltaNet（基于 delta rule 的线性注意力）的基础上引入门控机制，让模型能选择性地写入和遗忘状态。具体到 Qwen3.8 的配置：

| 组件 | 配置 |
|------|------|
| Gated DeltaNet | 128 个 V 头（head dim 128）、16 个 QK 头（head dim 128）、RoPE 维度 64 |
| Gated Attention | 64 个 Q 头、**4 个 KV 头**（GQA）、head dim 256 |

注意 QK 头只有 16 个——线性注意力的"检索"被刻意压缩，因为状态空间是固定的，过多的 QK 头只会增加冗余。而全注意力层的 KV 头压到 4 个（GQA），说明这一层追求的是"精确但不奢侈"的内存检索。

### 3:1 的混合哲学

为什么不是 100% 线性注意力？因为线性注意力的已知短板是**精确召回**：状态压缩会丢失细节，比如"第 137 行代码的变量名"这种需要逐字精确的信息。Qwen3.8 的解法是工程上非常优雅的折中：

- **3 层线性注意力**负责"便宜地读完整个上下文"——长程建模的广度；
- **1 层全注意力**负责"在关键位置做精确检索"——关键信息的深度。

这与 2025-2026 年混合架构的主流共识（如 Mamba-2 混 Transformer、DeepSeek 的 NSA 稀疏注意力）方向一致，但把"线性注意力 + 门控"的组合打磨到了当前开源界最成熟的工程形态。**每 4 层插 1 层全注意力**的比例也不是拍脑袋——太少则精确召回崩，太多则长上下文优势消失，3:1 是成本与能力的经验最优区间。

对 Agent 的实际意义：262K 原生上下文 + 可扩展 1M，配合线性注意力的低内存增长，意味着长程 Agent 轨迹（几十万 token 的思考链 + 工具输出 + 代码 diff）不再需要激进压缩。这正是 Grok 4.6 发布稿里反复强调的"long-running agents"能力——只不过 Qwen 把实现路径开源了。

## 四、Benchmark 深读：PaperBench 93 分的含金量与水分

Qwen3.8-Max 的官方评测数字相当亮眼（下表为与闭源旗舰的对比，第三方位列取各家公开最优）：

| Benchmark | Qwen3.8-Max | Opus 4.8 | Fable 5 | GPT-5.6 Sol |
|-----------|-------------|----------|---------|-------------|
| Terminal Bench 2.1 | **86.6** | 84.6 | 84.6 | 88.8 |
| SWE-bench Pro | 67.7 | 69.2 | **80.0** | 64.6 |
| DeepSWE 1.1 | 56.6 | 59.0 | 70.0 | **73.0** |
| FrontierSWE | 73.5 | 70.0 | **88.8** | — |
| PaperBench | **93.0** | 80.3 | 88.8 | 90.5 |
| AndroidBench | 75.1 | 69.8 | **84.5** | 74.0 |
| Agents' Last Exam | 27.0 | — | — | **30.6** |
| GPQA Diamond | 92.6 | 92.0 | **92.6** | **94.1** |
| OneMillion-Bench | 52.5 | 41.8 | **55.9** | 53.8 |

几个值得细读的信号：

**第一，PaperBench 93.0 是全场最高**，甚至在 GPT-5.6 Sol（90.5）之上。PaperBench 要求智能体复现论文实验——这是典型的长程、多工具、需要自主设计实验的任务。一个刚开源当天的模型在这个基准上登顶，说明"开源模型只能做短程任务"的刻板印象已经过时。

**第二，Terminal Bench 2.1 反超 Opus 4.8 和 Fable 5**（86.6 vs 84.6），这是终端环境下的真实工程任务，含金量高。

**第三，但请务必注意评测脚注**：Qwen3.8 的所有 Agent 基准几乎都是用 **Claude Code harness** 跑的。这不是黑点——第三方 harness 评估自家模型已经成为行业标配（xAI 也在 Cursor 里测 Grok 4.6）——但它说明一个被反复验证的事实：**Agent 时代的分数是"模型 × harness"的联合体，不是模型单独的成绩**。同样的模型换 harness，分数可以差出十几个点。去年的 SWE-Bench Pro 信任危机（34% 任务被证实有问题）已经给整个行业上过一课：benchmark 数字要读，但不能当教义读。

**第四，自家 benchmark（QwenSWEBench、QwenReactBench 等）和 Strong 的通用能力分数（GPQA 92.6、HLE 43.6）说明这是全能型旗舰，不是偏科选手。** OneMillion-Bench 52.5 分反超 Opus 4.8（41.8）尤其有意义——百万 token 上下文下的专家级任务，正是混合线性注意力架构的主场。

## 五、开源版与 Max 版的"阉割清单"

一个必须诚实指出的细节：这次开源的是 **Qwen3.8-2.4T-A95B-FP8**（文本版），而 API 上的 Qwen3.8-Max 有四个额外能力：

| 能力 | Qwen3.8-Max (API) | Qwen3.8-2.4T-A95B (开源) |
|------|-------------------|--------------------------|
| 视觉输入 | ✅ | ❌（纯文本） |
| 关闭思考 | ✅ | ❌（强制思考） |
| 1M 上下文 | ✅（默认） | ✅（可扩展至 1,010,000） |
| 内置工具 | ✅ | ❌（需自行接入） |

这是一份精心设计的"商业护栏"：**架构、权重、推理能力全给，但多模态、非思考模式、内置工具留作 API 独家**。强制思考模式意味着开源版每个请求都要付"推理税"（reasoning tokens 计入成本），而 API 版可以切到 non-thinking 模式做低成本高频调用。

这种"开源核心、闭源体验"的策略，实际上是今天所有中国大模型厂商的共同剧本（DeepSeek 开源权重但闭源工具链，Kimi K3 开源权重但闭源多模态）。它聪明的地方在于：**开源版承担"信任建立 + 生态卡位"的职能，API 版承担商业变现的职能**，两者通过能力差而非质量差来区隔。

不过对开发者来说，这份清单里有三个真正的礼物：

1. **reasoning_effort 分级**（xhigh / medium / low）——思考深度可调，成本可控。这是把"推理预算"变成 API 一等公民，Agent 可以在 planner 用 xhigh、executor 用 low，按需分配思考深度。
2. **preserve_thinking**——历史消息的思考链默认保留，多轮 Agent 循环不用重新想一遍。这两个参数放在一起，就是去年那篇"Hybrid Deterministic LLM"文章里呼吁的"推理税工程化"的官方实现。
3. **MTP（多 token 预测）**——训练时多步预测，推理时提速的经典技巧，现在开源了实现。

## 六、对 Agent 基础设施的启示：路由从优化题变成必需品

把三连发放在一起看，今天对 Agent 基础设施最大的冲击是什么？

**模型供给从"寡头 API"变成了"日更的商品货架"。** 当 DeepSeek 和 Qwen 以 1/10 的价格提供 90% 的能力，当 Grok 4.6 在 Cursor 里免费一周，当开源权重让任何人可以私有化部署旗舰级模型——Agent 的底座不再绑定任何单一供应商。这让三件事成为刚需：

1. **模型路由层**：昨天的 Nemotron Switchyard、更早的模型路由优化文章都指向同一结论——多目标优化（成本 × 质量 × 延迟 × 可靠性）必须由基础设施层解决，不能靠开发者手写 if-else。
2. **Harness 中立化**：既然分数是"模型 × harness"的联合体，Agent 框架就必须像编译器一样支持多后端。Claude Code 能跑 Qwen3.8，Codex 能跑 DeepSeek，OpenCode 能跑 Kimi——今天这已经是现实。
3. **私有化部署的阈值下移**：95B 激活的 FP8 权重 + 单机 8 卡部署，意味着数据敏感的行业（金融、医疗、政务）第一次可以把"旗舰级"模型放进自己的机房，而不是退而求其次用 30B 级别的小模型。

## 结语：开源第一次把"旗舰"变成了"基础设施"

回看 7 月 19 日那篇关于 Kimi K3 的文章，当时的结论是"开源模型跨过了足够好的临界点"。今天 Qwen3.8-2.4T 给出的答案是**跨过了"足够好"之后，开源开始定义"旗舰"本身**：

- 2.4T 参数证明开源社区有能力训练 Max 级模型；
- Gated DeltaNet 混合架构证明开源拿到了最前沿的架构工程；
- FP8 权重证明旗舰模型可以以"基础设施"的形态存在（可下载、可部署、可审计）；
- 而 Grok 4.6 和 DeepSeek V4 Pro 0813 的同日发布，证明竞争已经进入"日级"节奏。

对开发者而言，2026 年 8 月 13 日之后的世界是：**选模型不再是信仰问题，而是工程问题**。当你能以 $0.12 完成一次 12 分钟的编码任务（哪怕带一个 bug），或以 $1.41 换 3 分钟的无 bug 输出，或以 2.4TB 的硬盘把旗舰装进自己的机房——真正稀缺的不再是模型，而是围绕模型的路由、评估、补救与治理能力。这与本博客一直以来的主线完全一致：Agent 时代的主战场，从来不在模型本身，而在模型之上的那一层工程。

---

## 参考

- Qwen3.8-2.4T-A95B-FP8 模型卡：https://huggingface.co/Qwen/Qwen3.8-2.4T-A95B-FP8
- Qwen3.8-Max 官方博客：https://qwen.ai/blog?id=qwen3.8
- xAI Grok 4.6 发布稿：https://x.ai/news/grok-4-6
- Grok 4.6 基准分析（Artificial Analysis）：https://artificialanalysis.ai/articles/grok-4-6-benchmarks-and-analysis
- HN: DeepSeek V4 Pro 0813：https://news.ycombinator.com/item?id=49274600
- HN: Qwen3.8-2.4T：https://news.ycombinator.com/item?id=49273478
- 本博客相关阅读：[当开源模型跨过了"足够好"的临界点](/2026-07-19-kimi-k3-open-model-tipping-point-china-us-ai-economics.md)、[Nemotron 3.5 Lightning 与模型路由](/2026-08-12-nemotron-35-lightning-nemo-switchyard-model-routing.md)、[混合确定性架构](/2026-07-24-hybrid-deterministic-llm-agent-architecture-production-reliability.md)