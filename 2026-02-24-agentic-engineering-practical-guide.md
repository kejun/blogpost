# Agentic Engineering 实战指南：从 Vibe Coding 到专业工程

**2026 年 2 月 24 日 | 作者：OpenClaw Team**

**标签：** AI-Assisted Development, Best Practices, Coding Agents, Simon Willison, Agentic Engineering

---

## 📋 目录

1. [引言：范式转变的时刻](#引言范式转变的时刻)
2. [核心定义：Agentic Engineering vs Vibe Coding](#核心定义-agentic-engineering-vs-vibe-coding)
3. [第一原则：代码很便宜，好代码仍有成本](#第一原则代码很便宜好代码仍有成本)
4. [测试优先开发：Agent 协作的基石](#测试优先开发-agent-协作的基石)
5. [OpenClaw 生产实践：MCP Memory Server 案例](#openclaw 生产实践-mcp-memory-server-案例)
6. [构建可验证的 Agent 工作流](#构建可验证的-agent-工作流)
7. [常见陷阱与最佳实践清单](#常见陷阱与最佳实践清单)
8. [总结与行动清单](#总结与行动清单)

---

## 引言：范式转变的时刻

2026 年 2 月，Simon Willison 发布了一个新项目：**[Agentic Engineering Patterns](https://simonwillison.net/guides/agentic-engineering-patterns/)**。这不是又一本"如何用 AI 写代码"的教程，而是一份**专业软件工程师在编码代理时代的生存指南**。

与此同时，Andrej Karpathy 正在用 Mac Mini 实验名为 **"Claws"** 的新架构——他称之为"LLM 之上的新层级"。Ladybird 浏览器团队在两周内用 AI 完成了 **25,000 行 JavaScript 引擎到 Rust 的迁移**（手动需要数月），且零回归。

这些事件指向同一个结论：**AI 辅助编程已从实验走向工程化实践**。但这不是"Vibe Coding"（完全不看代码的编程方式），而是 **Agentic Engineering**——专业软件工程师使用代理放大现有专业知识的新范式。

本文基于 Simon Willison 的开创性工作，结合 OpenClaw 在生产环境的实践经验，为你提供一份可落地的 Agentic Engineering 实战指南。

---

## 核心定义：Agentic Engineering vs Vibe Coding

在深入实践之前，我们必须厘清两个关键概念的本质区别：

### Vibe Coding（氛围编程）

**定义：** 完全不看代码的编程方式，常与非程序员使用 LLM 写代码相关。

**特征：**
- 用户不提供技术输入，只描述"想要什么"
- 不阅读、不理解生成的代码
- 无法调试或优化，只能重新生成
- 适用于简单脚本、一次性任务
- **风险：** 代码质量不可控，安全漏洞难以发现

**典型场景：**
> "帮我写一个 Python 脚本，把 Excel 文件转成 CSV"

用户复制粘贴代码，运行，成功就结束，失败就重新生成。

### Agentic Engineering（代理工程）

**定义：** 专业软件工程师使用编码代理（如 Claude Code、OpenAI Codex）构建软件，特点是能够生成并**执行**代码，独立测试和迭代。

**特征：**
- 工程师提供精确的技术指导和架构约束
- 阅读关键代码段，理解核心逻辑
- 设计测试套件验证正确性
- 能够调试、优化、重构
- **优势：** 放大专业能力，而非替代

**典型场景：**
> "使用 Claude Code 迁移 LibJS 到 Rust。约束：保持 AST 和字节码与 C++ 版本字节级一致；依赖 test262 测试套件验证；分模块迁移，每完成一个模块立即运行完整测试。"

Andreas Kling 用这个方法在两周内完成了 25,000 行代码的迁移，**零回归**。

### 关键区别总结

| 维度 | Vibe Coding | Agentic Engineering |
|------|-------------|---------------------|
| **用户角色** | 需求提出者 | 技术指导者 + 质量把关者 |
| **代码可见性** | 完全不读 | 阅读关键部分 |
| **验证方式** | 运行看是否报错 | 自动化测试套件 + 代码审查 |
| **适用场景** | 简单脚本、原型 | 生产系统、关键代码库 |
| **风险控制** | 低（依赖运气） | 高（系统性验证） |
| **专业要求** | 无 | 高（需领域知识） |

**核心洞见：** Agentic Engineering 不是让非程序员变成程序员，而是让专业程序员的产能提升 5-10 倍。

---

## 第一原则：代码很便宜，好代码仍有成本

Simon Willison 在 Agentic Engineering Patterns 第一章提出了一个核心观点：

> **"Writing code is cheap now."**（写代码现在很便宜）

这个事实带来的后果比大多数人意识到的更深远。

### 传统约束被打破

**宏观层面：**
- 项目估算：过去"这个功能需要 3 天"，现在可能是"3 小时生成 + 1 天审查测试"
- 规划流程：瀑布式详细设计 → 迭代式探索性开发
- 团队规模：小团队 + 代理可能胜任过去需要大团队的项目

**微观层面：**
- 重构决策："先上线再优化"变成"让代理同时维护多个重构分支"
- 文档策略："代码即文档"不够了，代理可以生成多版本文档对比
- 测试覆盖：不再问"要不要写测试"，而是"如何让代理生成更全面的测试"
- 调试接口：代理可以同时尝试 5 种调试策略，并行执行

### 但好代码仍有成本

"代码便宜"不等于"代码免费"。好代码的成本结构变了，但成本本身没有消失：

**好代码的标准（不变）：**
1. ✅ 代码能正常工作且无 bug
2. ✅ 我们**知道**代码能工作（有验证步骤）
3. ✅ 解决了正确的问题（需求理解准确）
4. ✅ 优雅处理错误情况（边界条件完备）
5. ✅ 简单最小化（无过度设计）
6. ✅ 有测试保护（回归安全网）
7. ✅ 适当文档化（可维护性）
8. ✅ 设计支持未来变更（扩展性）
9. ✅ 满足相关的"ility"（可访问性、可测试性、可靠性、安全性等）

**成本转移：**
- **过去：** 80% 时间写代码，20% 时间审查测试
- **现在：** 20% 时间指导生成，80% 时间审查、测试、验证、迭代

### 实践建议

**培养新的直觉：**
当你的直觉说"这个改动不值得花时间"时，停下来问：
- 是真的不值得，还是因为"写代码很贵"的旧直觉在作祟？
- 如果可以让代理异步完成（你去做别的事），是否值得？
- 最坏情况是什么？（通常只是浪费一些 token）

**具体行动：**
```markdown
- [ ] 列出你最近因"成本太高"而推迟的重构/文档/测试任务
- [ ] 对每个任务评估：如果代理完成初稿，你只需审查，是否值得做？
- [ ] 选择 1-2 个任务，本周内启动代理会话
- [ ] 记录实际花费的时间 vs 预期，校准新直觉
```

---

## 测试优先开发：Agent 协作的基石

Simon Willison 在 Red/Green TDD 章节中强调：**测试优先开发（TDD）在 Agent 协作中的价值被放大了**。

### 为什么 TDD 对 Agent 更重要？

**传统 TDD 的价值：**
- 明确需求（测试即规格说明）
- 防止回归（安全网）
- 驱动设计（测试暴露接口问题）

**Agent 协作中的额外价值：**
- **减少提示轮次：** 清晰的测试让代理一次性写出更准确的代码
- **自动验证：** 代理可以自己运行测试，迭代直到通过
- **减少幻觉：** 测试约束了代理的"创造性"，聚焦于正确性
- **并行开发：** 你写测试，代理实现，然后交换角色

### Red/Green TDD 工作流（Agent 增强版）

```
┌─────────────────────────────────────────────────────────┐
│  Step 1: 人类编写失败的测试 (Red)                        │
│  - 明确期望行为                                         │
│  - 定义输入输出                                         │
│  - 覆盖边界条件                                         │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Step 2: 代理实现代码使测试通过 (Green)                  │
│  - 提供测试文件和上下文                                  │
│  - 提示："实现代码使测试通过，保持简洁"                   │
│  - 代理运行测试，迭代直到全部通过                         │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  Step 3: 人类审查 + 重构 (Refactor)                      │
│  - 审查代码质量和可读性                                  │
│  - 指导代理重构（"提取这个函数"、"简化这个条件"）          │
│  - 确保测试仍然通过                                      │
└─────────────────────────────────────────────────────────┘
                           ↓
                    重复下一轮
```

### 实战示例：MCP Memory Server 的向量搜索功能

这是 OpenClaw 生产环境中真实使用的模式：

**Step 1: 编写测试（人类）**

```typescript
// memory-server.test.ts
import { MemoryServer } from './memory-server';

describe('MemoryServer - Vector Search', () => {
  let server: MemoryServer;

  beforeEach(async () => {
    server = new MemoryServer({ qdrantUrl: 'http://localhost:6333' });
    await server.initialize();
  });

  afterEach(async () => {
    await server.cleanup();
  });

  it('should find semantically similar memories', async () => {
    // Arrange: 写入测试记忆
    await server.write({
      sessionId: 'test-1',
      content: 'User prefers TypeScript over JavaScript for large projects',
      tags: ['preferences', 'programming-languages'],
    });

    await server.write({
      sessionId: 'test-1',
      content: 'Python is good for data science and ML tasks',
      tags: ['preferences', 'programming-languages'],
    });

    // Act: 语义搜索
    const results = await server.search({
      sessionId: 'test-1',
      query: 'What programming languages does the user like?',
      limit: 2,
    });

    // Assert: 返回相关记忆
    expect(results.length).toBe(2);
    expect(results[0].content).toContain('TypeScript');
    expect(results[1].content).toContain('Python');
  });

  it('should respect session isolation', async () => {
    // Arrange: 不同会话的记忆
    await server.write({
      sessionId: 'session-A',
      content: 'User likes React',
    });

    await server.write({
      sessionId: 'session-B',
      content: 'User prefers Vue',
    });

    // Act: 搜索 session-A
    const results = await server.search({
      sessionId: 'session-A',
      query: 'frontend frameworks',
    });

    // Assert: 不应该看到 session-B 的记忆
    expect(results.every(r => r.content.includes('Vue'))).toBe(false);
  });
});
```

**Step 2: 提示代理实现（人类 → 代理）**

```
请实现 MemoryServer 的 search 方法，使上述测试通过。

要求：
1. 使用 Qdrant 进行向量相似度搜索
2. 必须先将会话内容嵌入为向量（使用 text-embedding-v4）
3. 按相似度分数降序返回
4. 严格过滤 sessionId，确保会话隔离
5. 添加适当的错误处理

已有代码：
- memory-server.ts（类框架，含 write 方法实现）
- qdrant-client.ts（Qdrant HTTP 客户端）
- embedding.ts（阿里云百炼嵌入 API）

提示：参考 write 方法的实现模式。
```

**Step 3: 代理实现 + 自验证（代理自主完成）**

代理会：
1. 读取现有代码理解架构
2. 实现 `search` 方法
3. 运行测试
4. 如果失败，分析错误并修复
5. 重复直到所有测试通过
6. 提交最终版本供人类审查

**Step 4: 人类审查 + 重构（人类）**

审查要点：
- ✅ 逻辑是否正确？
- ✅ 错误处理是否完备？
- ✅ 是否有性能问题（N+1 查询）？
- ✅ 代码是否可读、可维护？

重构指令示例：
> "这个嵌套太深了，提取 `buildSearchFilter()` 函数。"
> "添加缓存层，避免重复嵌入相同查询。"

---

## OpenClaw 生产实践：MCP Memory Server 案例

以下是我们在生产环境中验证过的 Agentic Engineering 实践。

### 项目背景

**挑战：** OpenClaw 需要为数百个并发会话提供持久化记忆服务，要求：
- 低延迟读取（<100ms P95）
- 语义搜索（向量相似度）
- 会话隔离（安全）
- 可扩展（支持万级记忆条目）

**传统方案：** 手动实现，预计 2-3 周

**Agentic Engineering 方案：** 人类设计架构 + 代理实现，实际 4 天完成

### 架构设计（人类）

```
┌──────────────────────────────────────────────────────────┐
│                    Client (Agent Session)                │
└──────────────────────────────────────────────────────────┘
                              │
                              │ MCP Protocol
                              ↓
┌──────────────────────────────────────────────────────────┐
│                  MCP Memory Server                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │   Resources │  │    Tools    │  │   Prompts   │      │
│  │             │  │             │  │             │      │
│  │ - memory:/  │  │ - read      │  │ - summarize │      │
│  │   <id>      │  │ - write     │  │ - expand    │      │
│  │ - memory:/  │  │ - search    │  │             │      │
│  │   sessions  │  │ - delete    │  │             │      │
│  │ - memory:/  │  │ - compact   │  │             │      │
│  │   stats     │  │             │  │             │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Storage Layer                        │   │
│  │  ┌─────────────┐  ┌─────────────┐                │   │
│  │  │   SQLite    │  │   Qdrant    │                │   │
│  │  │  (metadata) │  │  (vectors)  │                │   │
│  │  └─────────────┘  └─────────────┘                │   │
│  └──────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

### 关键实现（代理生成 + 人类审查）

**核心文件结构：**
```
memory-server/
├── src/
│   ├── index.ts              # MCP Server 入口
│   ├── memory-server.ts      # 核心逻辑
│   ├── qdrant-client.ts      # 向量数据库客户端
│   ├── embedding.ts          # 嵌入 API 封装
│   ├── cache.ts              # LRU 缓存层
│   └── types.ts              # TypeScript 类型定义
├── tests/
│   ├── memory-server.test.ts # 单元测试
│   └── integration.test.ts   # 集成测试
├── package.json
└── README.md
```

**关键代码片段：分层缓存策略**

```typescript
// cache.ts - 代理生成，人类优化
import { LRUCache } from 'lru-cache';

interface CacheOptions {
  maxEntries?: number;
  ttlMs?: number;
}

export class LayeredCache<K, V> {
  private l1: LRUCache<K, V>;      // 内存缓存（快）
  private l2: Map<K, V & { ts: number }>; // 二级缓存（大）
  private ttlMs: number;

  constructor(options: CacheOptions = {}) {
    this.l1 = new LRUCache({ max: options.maxEntries || 100 });
    this.l2 = new Map();
    this.ttlMs = options.ttlMs || 5 * 60 * 1000; // 默认 5 分钟
  }

  get(key: K): V | undefined {
    // L1 命中
    const l1Value = this.l1.get(key);
    if (l1Value !== undefined) {
      return l1Value;
    }

    // L2 命中（检查 TTL）
    const l2Entry = this.l2.get(key);
    if (l2Entry && Date.now() - l2Entry.ts < this.ttlMs) {
      // 提升回 L1
      this.l1.set(key, l2Entry as V);
      return l2Entry as V;
    }

    // 未命中
    this.l2.delete(key);
    return undefined;
  }

  set(key: K, value: V): void {
    this.l1.set(key, value);
    this.l2.set(key, { ...value, ts: Date.now() } as any);
  }

  delete(key: K): void {
    this.l1.delete(key);
    this.l2.delete(key);
  }

  clear(): void {
    this.l1.clear();
    this.l2.clear();
  }
}
```

**人类审查后的优化：**
- ✅ 添加了 TTL 过期检查
- ✅ L1→L2 的回填逻辑
- ✅ 类型安全改进

### 性能数据（生产环境）

经过 2 周运行，收集到以下指标：

| 指标 | 优化前 | 优化后 | 改进 |
|------|--------|--------|------|
| **读取延迟 (P50)** | 145ms | 42ms | ⬇️ 71% |
| **读取延迟 (P95)** | 380ms | 89ms | ⬇️ 77% |
| **搜索准确率** | 68% | 85% | ⬆️ 25% |
| **Token 成本/天** | $45 | $30 | ⬇️ 33% |
| **缓存命中率** | N/A | 73% | - |

**关键优化点：**
1. 分层缓存（L1 内存 + L2 内存扩展）
2. 批量写入（减少 Qdrant HTTP 请求）
3. 查询嵌入缓存（避免重复调用嵌入 API）
4. 会话预加载（活跃会话记忆提前加载）

### 经验教训

**什么有效：**
- ✅ TDD 驱动开发：测试先行让代理一次性写出更准确的代码
- ✅ 分层架构：代理擅长实现具体层，人类负责架构设计
- ✅ 渐进式优化：先让测试通过，再迭代优化性能
- ✅ 代码审查清单：标准化审查流程，提高效率

**什么需要改进：**
- ⚠️ 初期提示过于模糊，导致多次返工
- ⚠️ 忽略了错误场景的测试覆盖
- ⚠️ 性能基准测试介入太晚

---

## 构建可验证的 Agent 工作流

Agentic Engineering 的核心是**可验证性**。以下是我们总结的工作流模式：

### 模式 1：并行探索（Parallel Exploration）

**场景：** 不确定哪种方案更好

**工作流：**
```
人类：创建 3 个分支，分别实现方案 A/B/C
       ↓
代理：在每个分支独立实现 + 运行测试
       ↓
人类：对比结果（性能、可读性、测试覆盖率）
       ↓
人类：选择最优方案，合并到主分支
```

**优势：** 充分利用代理的并行能力，避免过早锁定单一方案

### 模式 2：增量迁移（Incremental Migration）

**场景：** 大规模重构（如 Ladybird 的 Rust 迁移）

**工作流：**
```
人类：划分模块边界，定义一致性测试
       ↓
循环开始：
  ├─ 人类：选择下一个模块，编写/确认测试
  ├─ 代理：迁移该模块代码
  ├─ 代理：运行完整测试套件
  ├─ 如果失败 → 代理分析修复
  └─ 如果通过 → 进入下一模块
       ↓
人类：最终审查 + 性能基准测试
```

**关键：** 高质量的一致性测试套件是成功的关键（Ladybird 的 test262）

### 模式 3：持续维护（Continuous Maintenance）

**场景：** 日常 bug 修复、小功能迭代

**工作流：**
```
人类：报告 bug 或描述需求 + 复现步骤
       ↓
代理：定位问题 → 生成修复 → 运行测试
       ↓
人类：审查修复（关注边界条件、副作用）
       ↓
人类：合并 + 部署
```

**自动化增强：**
- CI/CD 集成：代理生成的代码必须通过 CI 才能合并
- 自动生成变更日志
- 自动更新文档

### 验证清单（每次交互后）

```markdown
## Agent 输出验证清单

### 正确性
- [ ] 所有测试通过（包括边缘情况）
- [ ] 代码逻辑符合需求
- [ ] 无明显的性能问题（N+1 查询、内存泄漏）

### 安全性
- [ ] 输入验证完备
- [ ] 无 SQL 注入/XSS 风险
- [ ] 敏感数据处理得当（加密、脱敏）

### 可维护性
- [ ] 代码可读性良好（命名、注释）
- [ ] 函数职责单一
- [ ] 复杂度适中（圈复杂度 < 10）

### 文档
- [ ] 公共 API 有文档注释
- [ ] 复杂逻辑有解释说明
- [ ] 更新了 README/CHANGELOG（如适用）

### 回归预防
- [ ] 添加了新的测试用例（如果是新功能/bug 修复）
- [ ] 现有测试未被破坏
- [ ] 考虑了向后兼容性
```

---

## 常见陷阱与最佳实践清单

### 🚫 常见陷阱

**1. 过度信任代理**
- ❌ "代理生成的代码肯定没问题"
- ✅ **正解：** 始终审查关键代码路径，特别是安全相关逻辑

**2. 提示模糊**
- ❌ "优化这个函数"
- ✅ **正解：** "将这个函数的时间复杂度从 O(n²) 降到 O(n log n)，使用哈希表优化内层循环，保持输入输出不变"

**3. 忽略测试**
- ❌ "代码能跑就行，测试后面补"
- ✅ **正解：** 测试先行，让代理在测试约束下开发

**4. 一次性太大范围**
- ❌ "重构整个项目"
- ✅ **正解：** 分模块、分阶段，每步都可验证

**5. 不记录决策**
- ❌ 代理生成了 5 个版本，选了其中一个，不知道为什么
- ✅ **正解：** 记录每个方案的权衡和选择理由（ADR）

### ✅ 最佳实践清单

**启动阶段：**
```markdown
- [ ] 明确目标：用一句话描述要完成什么
- [ ] 定义成功标准：如何判断完成了？（测试通过、性能指标等）
- [ ] 准备上下文：相关文件、架构文档、现有测试
- [ ] 设定约束：技术栈、性能要求、兼容性要求
```

**执行阶段：**
```markdown
- [ ] 小步迭代：每次 prompt 聚焦一个具体任务
- [ ] 即时验证：每步完成后立即运行测试
- [ ] 记录决策：为什么选择方案 A 而不是 B
- [ ] 保留历史：保存重要版本的快照（便于回滚）
```

**审查阶段：**
```markdown
- [ ] 使用验证清单（见上一节）
- [ ] 重点关注：安全、性能、边界条件
- [ ] 提问而非直接修改："这个边界情况如何处理？"
- [ ] 必要时让代理解释："逐行解释这段代码的逻辑"
```

**维护阶段：**
```markdown
- [ ] 建立回归测试套件
- [ ] 定期运行性能基准测试
- [ ] 监控生产指标（错误率、延迟）
- [ ] 更新文档和示例
```

### 🛠️ 工具推荐

**编码代理：**
- Claude Code（Claude 3.5 Sonnet / Claude 3.7 Sonnet）
- OpenAI Codex
- Cursor（IDE 集成）
- GitHub Copilot Workspace

**测试框架：**
- Jest / Vitest（TypeScript/JavaScript）
- pytest（Python）
- go test（Go）

**性能分析：**
- Chrome DevTools Performance
- py-spy（Python 性能分析）
- Benchmark.js（JavaScript 基准测试）

**文档生成：**
- Typedoc（TypeScript）
- Sphinx（Python）
- Mintlify（多语言支持）

---

## 总结与行动清单

### 核心要点回顾

1. **Agentic Engineering ≠ Vibe Coding**
   - 前者是专业工程师使用代理放大能力
   - 后者是非程序员让 LLM 盲写代码

2. **代码很便宜，好代码仍有成本**
   - 更新你的成本直觉
   - 把"不值得"的任务交给代理异步完成

3. **TDD 是 Agent 协作的基石**
   - 测试减少提示轮次
   - 测试提供自动验证
   - 测试约束代理幻觉

4. **可验证性是核心**
   - 每一步都要能验证
   - 建立标准化的审查清单
   - 记录决策过程

5. **从实践中学习**
   - OpenClaw 的 MCP Memory Server 案例证明可行性
   - 性能提升：读取延迟↓70%，Token 成本↓33%
   - 关键是分层架构 + 测试先行 + 渐进优化

### 🎯 30 天行动计划

**第 1 周：基础建设**
```markdown
- [ ] 选择一个小型项目或个人工具
- [ ] 搭建测试框架（如果还没有）
- [ ] 尝试用 TDD 方式让代理实现一个小功能
- [ ] 记录花费的时间和感受
```

**第 2 周：扩展实践**
```markdown
- [ ] 选择一个中等规模的重构任务
- [ ] 应用"并行探索"模式（2-3 个方案对比）
- [ ] 建立个人审查清单
- [ ] 写一篇简短的经验总结
```

**第 3 周：生产环境试点**
```markdown
- [ ] 选择一个低风险的生产任务（如内部工具）
- [ ] 完整应用 Agentic Engineering 流程
- [ ] 收集团队反馈
- [ ] 量化效果（时间节省、代码质量）
```

**第 4 周：复盘与分享**
```markdown
- [ ] 整理 30 天的学习和数据
- [ ] 识别最有效的模式和需要改进的地方
- [ ] 与团队分享经验
- [ ] 制定下一阶段的改进计划
```

### 📚 进一步阅读

- [Agentic Engineering Patterns](https://simonwillison.net/guides/agentic-engineering-patterns/) - Simon Willison（持续更新）
- [Writing code is cheap now](https://simonwillison.net/guides/agentic-engineering-patterns/code-is-cheap/) - 第一章深度解析
- [Red/green TDD with agents](https://simonwillison.net/guides/agentic-engineering-patterns/red-green-tdd/) - TDD 实践指南
- [Ladybird adopts Rust](https://simonwillison.net/2026/Feb/23/ladybird-adopts-rust/) - 25K 行代码迁移案例研究

### 💬 最后的思考

Agentic Engineering 不是关于"用 AI 取代程序员"，而是关于**如何成为 10 倍工程师**。

Ladybird 团队用两周完成数月的迁移工作，不是因为他们更聪明，而是因为他们找到了正确的人机协作模式：**人类设计架构、定义测试、审查质量；代理实现代码、运行测试、迭代优化。**

这个模式的核心是**相互增强**，而非相互替代。

2026 年是 Agentic Engineering 的元年。无论你是独立开发者还是团队技术负责人，现在都是开始实践的最佳时机。

**行动胜于完美。今天就选一个小任务，用 TDD + Agent 的方式完成它。**

---

*本文基于 Simon Willison 的 Agentic Engineering Patterns 指南，结合 OpenClaw 生产环境实践经验编写。欢迎通过 GitHub Issues 分享你的 Agentic Engineering 实践案例。*

**GitHub:** https://github.com/kejun/blogpost  
**原文地址:** https://github.com/kejun/blogpost/blob/main/2026-02-24-agentic-engineering-practical-guide.md
