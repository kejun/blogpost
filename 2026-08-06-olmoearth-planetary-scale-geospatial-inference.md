# 当推理以"行星"为单位：OlmoEarth 与地理空间 AI 基础设施的范式突围

> 2026-08-06 · AI技术

> 一个"跑遍整个北美"的野火风险推理任务，峰值动用了 19,600 个 CPU 核心和 994 块 GPU，网络吞吐超过 168 GB/s，把原本需要 4,737 小时的串行计算压缩到 30.5 小时的墙钟时间——155 倍的加速。这背后是 Ai2 在 2026 年 7 月底发布的开源地理空间推理平台 **OlmoEarth Platform**。它真正值得研究的，不是那一组令人咋舌的数字，而是一套"当输入是整块大陆、输出是一张地图"时，AI 基础设施必须重新设计的底层逻辑。

---

## 一、引言：AI 基础设施的下一个"尺度鸿沟"

过去两年的 AI 基础设施叙事，几乎被"Token 吞吐"和"GPU 利用率"垄断。无论是推理引擎优化、连续批处理，还是 KV 缓存压缩，默认假设都是同一个：**输入是几 KB 到几 MB 的文本或图像，输出在毫秒到秒级完成**。这套范式支撑了 LLM 和 Agent 的整个生态。

但有一类任务的尺度完全不同——**地球观测（Earth Observation, EO）推理**。一个模型输入的不是一段文字，而是横跨多个光谱波段、多个时间步、覆盖数万到数十万平方公里的卫星影像；输出不是一行文本，而是一张必须与地理坐标网格严格对齐的地图。单个微调任务就能移动 TB 级数据、运行数小时。

Ai2 在 2026 年 7 月 28 日于 Hugging Face 博客发布的 [OlmoEarth Platform](https://huggingface.co/blog/allenai/olmoearth-infrastructure)，正是冲着这个尺度鸿沟去的。它的定位很明确：**把地理空间基础模型从"能训练出来"推进到"能大规模跑起来"**。

这不是一个边缘话题。OlmoEarth 模型家族（2025 年 11 月发布 v1，2026 年 6 月迭代到 v1.2）已经在洪水监测、森林退化、粮食安全、野火风险这些关乎真实世界的任务上被政府和非营利组织采用。而"大规模跑起来"恰恰是它们最稀缺的能力——**不是没有人能训练出好模型，而是绝大多数该用这些模型的组织，根本没有能把它部署到国家、大陆尺度的工程团队**。平台的价值，正在于此。

---

## 二、为什么"卫星推理"如此反直觉

要理解 OlmoEarth Platform 的设计，先得理解它面对的问题为什么和普通推理这么不一样。

普通 ML 推理的输入是"小而规整"的：一个 LLM 吃进一段文本，一个 CV 模型吃进一张照片。它们可以在几百毫秒内完成一次前向传播。地球观测推理则是"大而庞杂"的：

- **多模态**：输入横跨多个光谱波段（可见光、近红外、短波红外……）、多种传感器（光学、合成孔径雷达 SAR）、多个时间步。
- **多源异构**：数据来自不同提供商（Sentinel-1、Sentinel-2、Landsat、NISAR），各自使用不同的投影（projection）和分辨率（resolution），还可能缺失被云遮挡的观测。
- **输出即地图**：每个预测都必须与周围区域的投影和坐标网格**精确对齐**，否则拼起来就是一张裂开的地图。

最反直觉的一点是：**一次预测任务里，花在"找数据、下数据、预处理"上的时间，往往比真正跑模型的时间还长**。Ai2 团队明确说，"prediction jobs often spend more time downloading and preparing imagery than running the model itself"。这意味着，如果沿用"把一切都扔给 GPU"的惯性思维，你会让最贵的硬件去干最不该它干的活——I/O 和重投影。

这个洞察，直接决定了平台的第一条架构原则。

---

## 三、架构原则一：让硬件干它擅长的活——三段式流水线

OlmoEarth Platform 把每一次大规模推理任务拆成三个阶段，每一阶段匹配一种截然不同的硬件画像：

| 阶段 | 硬件画像 | 职责 |
|------|----------|------|
| 数据获取与预处理 | CPU + 高 I/O | 获取、重投影、对齐、归一化影像，写成推理期可快速加载的格式 |
| 推理 | GPU | 跑模型前向传播，把"最小加工"的输出直接写回存储 |
| 后处理 | CPU | 把逐窗口输出拼接成图，应用掩膜/重缩放，导出 Zarr、GeoTIFF、GeoJSON 等用户友好格式 |

这个分工的精髓是**"最小加工"（minimally processed）**：GPU 阶段只做前向传播，不做任何多余操作，输出直接流式写进 blob 存储。多进程数据加载器持续为每块 GPU 供数，避免 GPU 空转等待数据。

它的反面是很多团队会犯的错：为了"省事"把预处理也塞进 GPU 管线，结果 GPU 利用率被 I/O 拖垮。OlmoEarth 用"各司其职"回答了这个问题——**性价比最高的 GPU，是只做矩阵乘法的 GPU**。

---

## 四、架构原则二：一次请求，几百个 worker，几千个进程

OlmoEarth Run 是平台负责大规模推理执行的引擎。它的核心机制是**两级分治**：

1. **分区（Partition）**：把任务覆盖的地理区域切成适合单个计算实例（worker）处理的大小。一个州级区域可能变成约 100 个分区，一个大陆级任务则可能变成数千个分区。
2. **窗口（Window）**：每个分区再细分成模型直接处理的更小窗口。因为每个窗口可以独立进行一次前向传播，地图一处的计算不需要等待另一处。

关键设计在于**相邻分区有少量重叠**，并在输出装配时对重叠区域做调和（reconcile），这样最终栅格上不会出现接缝（seam）。这是"输出即地图"约束的直接体现——分布式系统的边界，最终要在视觉上彻底消失。

分区彼此独立，意味着同一阶段可以横跨数千个计算实例并行。北美野火风险图那次任务就是极致案例：

> **峰值 19,600 个 CPU + 994 块 GPU 并行，网络吞吐 >168 GB/s，把 4,737 小时的串行计算压缩到 30.5 小时，加速比 155×。**

但 Ai2 也诚实地点出了并行度的边界：**扇出不是无上限的**——更多的 worker 会撞上云厂商的配额（cloud quotas）。所以并行度是"每次任务可调的一个旋钮"，和输出分辨率、模型大小、原始影像缓存策略一样，都是按任务、按预算动态取舍的参数。这是成熟的工程态度：**不是"越多并行越好"，而是"在配额和预算约束下找到最优解"**。

---

## 五、架构原则三：别把外部目录打爆——自建元数据索引

给定一个地理区域和时间范围，平台首先要回答一个看似简单的问题：**哪些卫星影像应该喂给模型？**

这需要跨提供商（不同目录、不同格式、不同发布延迟）识别"谁在何时何地拍了什么"。选择标准还随数据源而异：光学影像（如 Sentinel-2）倾向选云量最少的场景，SAR 则更关心可用的极化通道。

直觉上的做法是直接查各提供商的 STAC 目录 API。但这里有个陷阱：**一次大规模推理任务会瞬间产生数千个元数据查询**——远超 ESA 或 Microsoft Planetary Computer 的 STAC API 能并发承受的极限。如果直接打过去，等于拿一个巨大的突发流量脉冲去轰击别人的公共服务。

OlmoEarth 的解法是**自建元数据索引**：

- 对托管在 AWS Open Data 的数据集，订阅其 **SNS 变更通知**，每个新场景到达即收到推送。
- 对不提供变更流的提供商，则每隔几分钟轮询其上游索引。
- 因此，平台对外部服务的请求节奏**跟随新数据发布的平稳节奏**，而不是被某个大任务瞬间的突发请求淹没。

每个索引条目存场景元数据 + 指向所有可获取像素位置的指针。运行时选择最优数据源，对 COG（Cloud Optimized GeoTIFF）或 Zarr 这类云优化格式做**窗口化读取（windowed reads）**——只取某个分区真正需要的字节，而不是下载整个场景。这在"下载比跑模型还贵"的世界里，是决定成本的胜负手。

这个索引还顺带养活了平台的标注工具：因为索引保存了 Sentinel-1/2、Landsat、NISAR 的云优化格式指针，平台无需另建一套摄取管线，就能通过同一套窗口化读取系统，为任意已索引场景提供瓦片服务。

Ai2 还从合作方身上总结出三条"地球观测数据发布的最佳实践"，值得所有数据平台借鉴：

1. **基于队列的变更通知**：新影像到达时主动推送，而非让下游轮询。
2. **存储在大云平台**：避免专有的速率限制和可用性瓶颈。
3. **云优化格式 + 范围读取支持**：让下游只需取需要的字节。

这三条，堪称"让数据可被 Agent 化消费"的地理空间版宣言。

---

## 六、架构原则四：把故障当成默认状态

在 1.96 万个 CPU、994 块 GPU 的规模上,"系统不出错"是幻想——**故障是默认状态**。Ai2 的应对哲学是"设计成能自动从故障中恢复"。

每个任务（task）在某个阶段、某个地理分区内运行时，平台会**动态按需创建一台运行 runner Docker 容器的虚拟机**：runner 拉取任务参数 → 执行工作 → 返回结果 → 关闭。因为每个任务都是**可重入且幂等（reentrant and idempotent）的**，间歇性失败可以安全地通过重跑来解决。

平台对故障的响应是分层级的：

- **任务跟踪与自动重试**：任务级跟踪，失败自动重试。
- **提供商故障转移**：提供商慢或暂时不可用时，回退到备用提供商。
- **可重试 vs 致命错误**：明确区分两类错误——可重试的（如提供商抖动）和致命的（如数据缺失），避免在注定失败的任务上浪费重试。
- **停滞 runner 监控**：独立的监控进程探测"卡住"或"停摆"的 runner，重启其任务。

这套机制的本质是**把"可靠性"从"防止故障"转变为"容忍故障"**——接受故障必然发生，通过幂等性和自动重试让故障的代价趋近于零。这与分布式系统的既有智慧一脉相承，但在地理空间推理的规模下，它从"工程技巧"变成了"能否落地的先决条件"。

---

## 七、模型侧的另一半：token 设计如何撬动成本

平台解决了"怎么大规模跑"，但模型的效率同样决定成本的上限。OlmoEarth v1.1/v1.2 在模型侧给出了一个极具启发性的答案：**重新设计 token，而不是堆算力**。

Transformer 的计算成本随 token 序列长度**二次方**增长。所以"token 代表什么"不只是形式问题，而是直接决定算力成本的核心变量。

以最常见的 Sentinel-2 输入为例，其张量形状是 `[H, W, T, D=12]`（纬度像素、经度像素、时间步、12 个光谱通道）。OlmoEarth 的处理方式是**按分辨率切 patch**：取一个空间 patch 大小 p，把影像切成 p×p 的 patch，**每个 patch 在每个时间步、每个分辨率各生成一个 token**。于是 2 个时间步 × 3 个分辨率（10m/20m/60m）= 6 个 token/patch。

这与文献中的主流做法一致：Galileo 和 SatMAE 都采用"每分辨率一个 token"，且 SatMAE 证明这种做法显著更好。但也有反例——**CROMA 对所有波段只用单一 token，无论分辨率**。因为 token 数会乘性复合，把多分辨率折叠成单一 token 能产生 3 倍更少的 token，在预训练、微调、推理全链路都能省下真金白银。

但天下没有免费的午餐：**天真地合并 token 会导致显著性能下降**——m-eurosat kNN（遥感模型常用基准）上掉 10 个百分点。Ai2 的假设是：把 Sentinel-2 的不同波段拆成不同 token，能让模型更容易建模**跨波段关系**。要"既合并 token 又不损失性能"，就得修改预训练方案——这正是 v1.1 论文的核心。

v1.2 则引入 **RoPE（旋转位置编码）** 取代绝对位置编码（APE），修复了嵌入中的伪影（artifact），在 kNN/线性概率评估上全线提升。模型家族分 Nano/Tiny/Small/Base 四档，供不同算力预算的用户选择。

这一段的启示是：**在小模型时代，效率设计往往藏在"token 长什么样"这种最不起眼的地方**——一个能省 3 倍算力的决策，可能比换更大的模型更有价值。

---

## 八、范式启示：当"输入是大陆、输出是地图"重塑基础设施

把 OlmoEarth Platform 放在 2026 年 AI 基础设施的大背景下看，它揭示了一个被主流叙事忽略的维度：**很多 AI 任务的瓶颈不在模型，而在"数据进得来、结果出得去"的工程**。

过去一年，我们反复讨论"Coding Agent 的 token 开销""GPU 利用率危机""Agent 基础设施栈"——它们共享同一个底层焦虑：**算力很贵，但真正浪费算力的往往不是模型本身，而是围绕模型的工程**。OlmoEarth 把这个焦虑推到了极致：在这里，连"下载数据"都比"跑模型"贵，连"查目录"都能打爆别人的服务。

它还提供了一个关于"开源模型 + 平台"的清晰范式：**开源模型解决了"谁来训练"，平台解决了"谁能用得起"**。Ai2 明确说，对工程能力强的团队，一个开源模型就够了；但对环境领域的大多数组织——最该用这些模型的人——缺的恰恰是"把模型跑起来"的整套基础设施（标注、微调、大规模推理、性能监控、把原始输出变成可落地的洞察）。这解释了为什么 Ai2 要投入十余年运营 Skylight、EarthRanger 这类"每天都要能用"的平台后，才敢做 OlmoEarth Platform：**这是一个需要长期承诺的领域，不是一次 hackathon 能交付的**。

---

## 九、展望：从"跑一次"到"自动跑"

平台的路线图清晰地指向下一个阶段——**从"大规模推理"走向"持续监测"**：

- **自动化模型运行**：提前调度推理任务，或每当影像索引登记到感兴趣区域的新场景时自动触发。
- **变化检测与告警**：当景观特征发生变化时通知用户。

这两条才是地理空间 AI 真正的"圣杯"：不是"你有需要时跑一张地图"，而是"地球一直在被监测，变化一发生就有人知道"。这背后需要的，是把 OlmoEarth 的推理能力从"作业系统"升级成"事件驱动系统"——元数据索引已经为它铺好了路（每次新场景登记就是一个事件）。

从"一次请求、几百 worker、几千进程"到"行星永远在线"，OlmoEarth Platform 展示的不仅是一个平台，更是 AI 基础设施一个新的尺度坐标：**当你的输入是整块大陆、输出是一张地图时，硬件的分工、数据的中介、故障的处理、乃至 token 的设计，全部要重新发明**。而这套重新发明的经验，对任何想在"超大输入 + 地图输出"类任务上做功的工程团队，都是最值得抄的作业。

---

## 参考资料

- Ai2, [The OlmoEarth Platform: Geospatial inference at planetary scale](https://huggingface.co/blog/allenai/olmoearth-infrastructure)（Hugging Face Blog, 2026-07-28）
- Ai2, [OlmoEarth v1.1: A more efficient family of models](https://allenai.org/blog/olmoearth-v1-1) / v1.2 更新
- Ai2, [OlmoEarth 技术报告](https://allenai.org/papers/olmoearth-v1-2)
- 相关遥感基础模型论文：Galileo（arxiv 2502.09356）、SatMAE（arxiv 2207.08051）、CROMA（arxiv 2311.00566）